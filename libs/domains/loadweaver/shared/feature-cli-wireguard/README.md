# loadweaver-shared-feature-cli-wireguard

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test loadweaver-shared-feature-cli-wireguard` to execute the unit tests via [Jest](https://jestjs.io).
