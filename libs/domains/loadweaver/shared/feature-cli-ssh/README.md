# loadweaver-shared-feature-cli-ssh

Run arbitrary commands on cluster nodes over SSH.

## Running unit tests

Run `nx test loadweaver-shared-feature-cli-ssh` to execute the unit tests via [Jest](https://jestjs.io).
