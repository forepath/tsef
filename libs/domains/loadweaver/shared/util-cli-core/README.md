# loadweaver-shared-util-cli-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build loadweaver-shared-util-cli-core` to build the library.

## Running unit tests

Run `nx test loadweaver-shared-util-cli-core` to execute the unit tests via [Jest](https://jestjs.io).
