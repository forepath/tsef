openapi: 3.1.0
info:
  title: Billing Service API
  version: 1.0.0
  description: |
    HTTP API for billing, subscriptions, and provisioning.

    **Open positions and user billing day:** Recurring and final subscription charges are recorded as "open positions"
    instead of creating invoices immediately. A scheduler runs on each user's billing day (stored on the user;
    default is the registration day capped at 28) and creates one accumulated invoice per user containing all of that
    user's unbilled open positions as line items. This reduces the number of invoices (one per user per billing day).
    Separate from the service plan's billing_day_of_month, which controls subscription period alignment.

    **Multi-tenancy:** Send optional header `X-Tenant` on every request (defaults to `default` when omitted, unless
    `TENANTS_ALLOW_DEFAULT=false` — then missing, blank, or `default` values are rejected). Allowed values come from
    the `TENANTS` environment variable (includes `default` unless disabled). Data is isolated per tenant; the same
    email may exist in different tenants as separate users. Invalid tenant ids return 400.
  contact:
    name: ForePath
    email: hi@forepath.io
    url: https://forepath.io
servers:
  - url: http://localhost:3200/api
    description: Local development
  - url: http://localhost:3200
    description: Root (OpenTelemetry metrics; outside /api prefix)
security:
  - bearerAuth: []
paths:
  /public/service-plan-offerings:
    get:
      summary: List public service plan offerings (unauthenticated)
      description: |
        Returns active plans with fields suitable for external marketing or pricing cards: title, description,
        billing interval, customer total price, and ordering highlights. Does not include provider configuration,
        margins, or internal defaults.
      operationId: listPublicServicePlanOfferings
      security: []
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
          description: Page size (capped at 100; default 50)
        - name: offset
          in: query
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
          description: Number of results to skip
        - name: serviceTypeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Filter by service type ID
      responses:
        '200':
          description: Array of public plan offerings (active plans only)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PublicServicePlanOffering'
  /public/service-plan-offerings/cheapest:
    get:
      summary: Get cheapest public service plan offering (unauthenticated)
      description: |
        Returns the single active plan with the lowest customer total price (same pricing formula as the list endpoint).
        Intended for "from …" / starting-at copy. When multiple plans tie on price, the plan with the lower UUID wins.
      operationId: getCheapestPublicServicePlanOffering
      security: []
      parameters:
        - name: serviceTypeId
          in: query
          required: false
          schema:
            type: string
            format: uuid
          description: Optional filter — lowest price among active plans for this service type only
      responses:
        '200':
          description: Cheapest active plan offering
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicServicePlanOffering'
        '404':
          description: No active plan offerings (or none matching the filter)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /public/withdrawal/addressee:
    get:
      summary: Get statutory withdrawal addressee (unauthenticated)
      description: |
        Returns the legal seller entity (from BILLING_ISSUER_* configuration) to whom statutory withdrawals are addressed.
      operationId: getPublicWithdrawalAddressee
      security: []
      responses:
        '200':
          description: Withdrawal addressee details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicWithdrawalAddressee'
        '503':
          description: Addressee configuration incomplete
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /public/withdrawal/request:
    post:
      summary: Request public statutory withdrawal (unauthenticated)
      operationId: requestPublicWithdrawal
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestPublicWithdrawal'
      responses:
        '200':
          description: Withdrawal request created or resumed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicWithdrawalRequestResponse'
        '400':
          description: Invalid input or no matching subscription
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /public/withdrawal/verify-code:
    post:
      summary: Verify public withdrawal confirmation code (unauthenticated)
      operationId: verifyPublicWithdrawalCode
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VerifyPublicWithdrawalCode'
      responses:
        '200':
          description: Code verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicWithdrawalVerifyCodeResponse'
        '400':
          description: Invalid or expired code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /public/withdrawal/confirm:
    post:
      summary: Confirm public statutory withdrawal (unauthenticated)
      operationId: confirmPublicWithdrawal
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmPublicWithdrawal'
      responses:
        '200':
          description: Withdrawal submitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PublicWithdrawalConfirmResponse'
        '400':
          description: Invalid request or withdrawal not permitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types:
    get:
      summary: List service types
      operationId: listServiceTypes
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Array of service types
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ServiceTypeResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create service type (admin only)
      operationId: createServiceType
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateServiceTypeDto'
      responses:
        '201':
          description: Service type created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceTypeResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types/providers:
    get:
      summary: Get all provider details
      operationId: getProviderDetails
      description: Returns registered provisioning providers with id, display name, and optional config schema for service type creation and subscription config.
      responses:
        '200':
          description: Array of provider details
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProviderDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types/providers/{providerId}/server-types:
    get:
      summary: Get server types for a provider
      operationId: getProviderServerTypes
      description: Returns server types with specs and pricing for the given provider (e.g. hetzner, digital-ocean). Used by the billing console for server type dropdown and to auto-set plan base price when configSchema.basePriceFromField is set.
      parameters:
        - name: providerId
          in: path
          required: true
          schema:
            type: string
            examples: [hetzner]
        - name: serviceTypeId
          in: query
          required: false
          description: When set, resolves platform API credentials from this service type instead of global environment variables.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of server types with id, name, specs, priceMonthly, priceHourly
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ServerType'
        '400':
          description: Bad request (e.g. provider not supported or token missing)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types/providers/{providerId}/locations:
    get:
      summary: Get geography options for a provider
      operationId: getProviderLocations
      description: Returns locations (Hetzner) or regions (DigitalOcean) with human-readable labels. Used by the billing console for geography enum dropdowns. Labels are resolved from the provider API with static fallbacks when the API is unavailable.
      parameters:
        - name: providerId
          in: path
          required: true
          schema:
            type: string
            examples: [hetzner]
        - name: serviceTypeId
          in: query
          required: false
          description: When set, resolves platform API credentials from this service type instead of global environment variables.
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of geography options with technical id and human-readable name
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProviderLocation'
        '400':
          description: Bad request (e.g. provider not supported or token missing)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types/{id}/meters:
    get:
      summary: List meters attached to a service type
      operationId: listServiceTypeMeters
      description: |
        Returns attached meters for the service type. Provider-declared meters are synced
        on read (ensured in catalog and sideloaded as required). Includes manual attachments.
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
      responses:
        '200':
          description: Attached meters
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AttachedMeterResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service type not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Attach a catalog meter to a service type (admin only)
      operationId: attachServiceTypeMeter
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachMeterDto'
      responses:
        '200':
          description: Attached meter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachedMeterResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types/{id}/meters/{meterId}:
    post:
      summary: Update service type meter unit-price override (admin only)
      operationId: updateServiceTypeMeter
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
        - name: meterId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAttachedMeterDto'
      responses:
        '200':
          description: Updated attachment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachedMeterResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Detach meter from service type (admin only)
      description: Required (provider-sourced) meters cannot be detached.
      operationId: detachServiceTypeMeter
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
        - name: meterId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Detached
        '400':
          description: Required meter cannot be detached
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-types/{id}:
    get:
      summary: Get service type
      operationId: getServiceType
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
      responses:
        '200':
          description: Service type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceTypeResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service type not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Update service type (admin only)
      operationId: updateServiceType
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateServiceTypeDto'
      responses:
        '200':
          description: Service type updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceTypeResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service type not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Delete service type (admin only)
      operationId: deleteServiceType
      parameters:
        - $ref: '#/components/parameters/ServiceTypeId'
      responses:
        '204':
          description: Service type deleted
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service type not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-plans:
    get:
      summary: List service plans
      operationId: listServicePlans
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Array of service plans
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ServicePlanResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create service plan (admin only)
      operationId: createServicePlan
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateServicePlanDto'
      responses:
        '201':
          description: Service plan created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServicePlanResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-plans/{id}/cloud-init-configs/{configId}/order-fields:
    get:
      summary: Get order form fields for a CloudInit config on a service plan
      operationId: getServicePlanCloudInitOrderFields
      parameters:
        - $ref: '#/components/parameters/ServicePlanId'
        - name: configId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Fields shown in the customer order form for the selected plan option
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CloudInitConfigOrderField'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service plan or CloudInit config not found for this plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /service-plans/{id}:
    get:
      summary: Get service plan
      operationId: getServicePlan
      parameters:
        - $ref: '#/components/parameters/ServicePlanId'
      responses:
        '200':
          description: Service plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServicePlanResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service plan not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Update service plan (admin only)
      operationId: updateServicePlan
      parameters:
        - $ref: '#/components/parameters/ServicePlanId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateServicePlanDto'
      responses:
        '200':
          description: Service plan updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServicePlanResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service plan not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Delete service plan (admin only)
      operationId: deleteServicePlan
      parameters:
        - $ref: '#/components/parameters/ServicePlanId'
      responses:
        '204':
          description: Service plan deleted
        '400':
          description: Plan cannot be deleted (e.g. still referenced by subscriptions)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Service plan not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /cloud-init-configs:
    get:
      summary: List CloudInit configs (admin only)
      operationId: listCloudInitConfigs
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: CloudInit configs (default values masked in list)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/CloudInitConfigResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create CloudInit config (admin only)
      operationId: createCloudInitConfig
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCloudInitConfigDto'
      responses:
        '201':
          description: CloudInit config created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CloudInitConfigResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /cloud-init-configs/{id}:
    get:
      summary: Get CloudInit config (admin only)
      operationId: getCloudInitConfig
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: CloudInit config including decrypted default values
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CloudInitConfigResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: CloudInit config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Update CloudInit config (admin only)
      operationId: updateCloudInitConfig
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCloudInitConfigDto'
      responses:
        '200':
          description: CloudInit config updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CloudInitConfigResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: CloudInit config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Delete CloudInit config (admin only)
      operationId: deleteCloudInitConfig
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: CloudInit config deleted
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: CloudInit config not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: Config referenced by active service plan
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /meters:
    get:
      summary: List usage meters (admin only)
      operationId: listMeters
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Meters
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MeterResponse'
    post:
      summary: Create usage meter (admin only)
      operationId: createMeter
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMeterDto'
      responses:
        '201':
          description: Meter created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeterResponse'
  /meters/{id}:
    get:
      summary: Get usage meter (admin only)
      operationId: getMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Meter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeterResponse'
    post:
      summary: Update usage meter (admin only)
      operationId: updateMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMeterDto'
      responses:
        '200':
          description: Meter updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MeterResponse'
    delete:
      summary: Delete usage meter (admin only)
      description: Blocked while plan links, addon links, or usage rows reference the meter. Prefer isActive=false.
      operationId: deleteMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Meter deleted
  /addons:
    get:
      summary: List addons (admin only)
      operationId: listAddons
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Addons
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddonResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create addon (admin only)
      operationId: createAddon
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAddonDto'
      responses:
        '201':
          description: Addon created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddonResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /addons/modules:
    get:
      summary: List registered addon modules (admin only)
      operationId: listAddonModules
      description: |
        Returns registered addon modules and their declared required meters.
        Used by the admin UI to preview fixed meters before saving a module addon.
      responses:
        '200':
          description: Registered addon modules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AddonModuleDetail'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /addons/{id}:
    get:
      summary: Get addon (admin only)
      operationId: getAddon
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Addon
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddonResponse'
        '404':
          description: Not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Update addon (admin only)
      operationId: updateAddon
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAddonDto'
      responses:
        '200':
          description: Addon updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AddonResponse'
    delete:
      summary: Delete addon (admin only)
      operationId: deleteAddon
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Addon deleted
  /addons/{id}/meters:
    get:
      summary: List meters attached to an addon
      operationId: listAddonMeters
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Attached meters
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AttachedMeterResponse'
    post:
      summary: Attach a meter to an addon
      operationId: attachAddonMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachMeterDto'
      responses:
        '200':
          description: Attached meter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachedMeterResponse'
  /addons/{id}/meters/{meterId}:
    post:
      summary: Update addon meter unit-price override
      operationId: updateAddonMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: meterId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAttachedMeterDto'
      responses:
        '200':
          description: Updated attachment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachedMeterResponse'
    delete:
      summary: Detach meter from addon
      operationId: detachAddonMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: meterId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Detached
  /service-plans/{id}/meters:
    get:
      summary: List meters attached to a service plan
      operationId: listServicePlanMeters
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Attached meters
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AttachedMeterResponse'
    post:
      summary: Attach a meter to a service plan
      operationId: attachServicePlanMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AttachMeterDto'
      responses:
        '200':
          description: Attached meter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachedMeterResponse'
  /service-plans/{id}/meters/{meterId}:
    post:
      summary: Update plan meter unit-price override
      operationId: updateServicePlanMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: meterId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAttachedMeterDto'
      responses:
        '200':
          description: Updated attachment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AttachedMeterResponse'
    delete:
      summary: Detach meter from service plan
      operationId: detachServicePlanMeter
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: meterId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Detached
  /service-plans/{id}/addons:
    get:
      summary: List orderable addons for a plan
      operationId: listServicePlanAddons
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Plan addon options (empty when provider does not support addons)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PlanAddonOption'
  /availability/check:
    post:
      summary: Check availability
      operationId: checkAvailability
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AvailabilityCheckDto'
      responses:
        '200':
          description: Availability result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailabilityResponse'
        '400':
          description: Invalid request or provider error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /availability/alternatives:
    post:
      summary: Get availability alternatives
      operationId: checkAvailabilityAlternatives
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AvailabilityCheckDto'
      responses:
        '200':
          description: Availability result with alternatives
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AvailabilityResponse'
        '400':
          description: Invalid request or provider error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/login:
    post:
      summary: Login
      description: |
        Interactive console login with account password only.
        Personal access tokens are not accepted; use POST /auth/token instead.
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginDto'
      responses:
        '200':
          description: Login successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: |
            Unauthorized. When the password is valid but the email is not confirmed, the body includes
            `code: EMAIL_NOT_CONFIRMED` so the client can resume confirmation.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/token:
    post:
      summary: Exchange personal access token for machine JWT
      description: |
        Token-only exchange (no email). Returns a JWT with `amr: ["pat"]` and capability `scopes`.
      operationId: exchangePersonalAccessToken
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExchangePersonalAccessTokenDto'
      responses:
        '200':
          description: JWT access token, user info, and scopes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatLoginResponse'
        '401':
          description: Invalid token
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/register:
    post:
      summary: Register
      operationId: register
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDto'
      responses:
        '201':
          description: Registration successful
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '503':
          description: Signup disabled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/confirm-email:
    post:
      summary: Confirm email
      operationId: confirmEmail
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmEmailDto'
      responses:
        '200':
          description: Email confirmed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '400':
          description: Invalid or expired confirmation code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/request-password-reset:
    post:
      summary: Request password reset
      operationId: requestPasswordReset
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestPasswordResetDto'
      responses:
        '200':
          description: Password reset requested
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/reset-password:
    post:
      summary: Reset password
      operationId: resetPassword
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordDto'
      responses:
        '200':
          description: Password reset
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MessageResponse'
        '400':
          description: Invalid or expired reset code
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/change-password:
    post:
      summary: Change password
      operationId: changePassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordDto'
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangePasswordResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/logout:
    post:
      summary: Log out (users auth only; revokes current session by default)
      operationId: logout
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LogoutDto'
      responses:
        '204':
          description: Session invalidated
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/token-scopes:
    get:
      summary: List grantable PAT scopes for the current user
      operationId: listPersonalAccessTokenScopes
      responses:
        '200':
          description: Scope catalog filtered by role
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessTokenScopeDto'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/tokens:
    get:
      summary: List own personal access tokens
      operationId: listPersonalAccessTokens
      responses:
        '200':
          description: Active tokens
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create a personal access token
      operationId: createPersonalAccessToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePersonalAccessTokenDto'
      responses:
        '201':
          description: Token created; plaintext returned once
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /auth/tokens/{id}:
    patch:
      summary: Update own personal access token name and scopes
      operationId: updatePersonalAccessToken
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePersonalAccessTokenDto'
      responses:
        '200':
          description: Token updated (secret unchanged)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Password session required or cannot grant scopes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Token not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Revoke own personal access token
      operationId: revokePersonalAccessToken
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Revoked
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /users/{userId}/tokens:
    get:
      summary: List personal access tokens for a user (admin)
      operationId: listUserPersonalAccessTokens
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Tokens for user
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
  /users/{userId}/tokens/{tokenId}:
    delete:
      summary: Revoke a user's personal access token (admin)
      operationId: revokeUserPersonalAccessToken
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: tokenId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Revoked
  /users:
    get:
      summary: List users (admin only)
      operationId: listUsers
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Array of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create user (admin only)
      operationId: createUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserDto'
      responses:
        '201':
          description: User created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /users/{id}:
    get:
      summary: Get user (admin only)
      operationId: getUser
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '200':
          description: User
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Update user (admin only)
      operationId: updateUser
      parameters:
        - $ref: '#/components/parameters/UserId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserDto'
      responses:
        '200':
          description: User updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    delete:
      summary: Delete user (admin only)
      operationId: deleteUser
      parameters:
        - $ref: '#/components/parameters/UserId'
      responses:
        '204':
          description: User deleted
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions:
    get:
      summary: List subscriptions
      operationId: listSubscriptions
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Array of subscriptions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SubscriptionResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create subscription
      operationId: createSubscription
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSubscriptionDto'
      responses:
        '201':
          description: Subscription created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
        '400':
          description: >-
            Validation error, configuration unavailable, or customer billing profile missing/incomplete.
            A complete profile is required (first name, last name, email, address line, city, country) before ordering.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}:
    get:
      summary: Get subscription
      operationId: getSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Subscription
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/meters:
    get:
      summary: List meter summaries for a subscription
      operationId: listSubscriptionMeters
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Plan- and addon-scoped meter summaries for the current period
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SubscriptionMeterSummary'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/cancel:
    post:
      summary: Cancel subscription
      operationId: cancelSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelSubscriptionDto'
      responses:
        '200':
          description: Subscription cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
        '400':
          description: Cancellation not permitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/resume:
    post:
      summary: Resume subscription
      operationId: resumeSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResumeSubscriptionDto'
      responses:
        '200':
          description: Subscription resumed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
        '400':
          description: Resume not permitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/withdraw:
    post:
      summary: Statutory withdrawal (Widerruf)
      operationId: withdrawSubscription
      description: |
        Exercises the statutory withdrawal right, distinct from commercial cancellation.
        Unprovisioned orders are always withdrawable. Provisioned orders may withdraw within
        BILLING_STATUTORY_WITHDRAWAL_PERIOD_DAYS unless the service type disallows post-provisioning withdrawal.
        Provisioned withdrawals trigger prorated partial refunds for the unused billing period.
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawSubscriptionDto'
      responses:
        '200':
          description: Subscription withdrawn
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionResponse'
        '400':
          description: Withdrawal not permitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/config-change/preview:
    post:
      summary: Preview a subscription configuration change
      operationId: previewSubscriptionConfigChange
      description: |
        Validates a mid-life configuration change (server type resize, addon add/remove) and returns
        the price delta, the prorated immediate adjustment, and the billing disclaimer the customer
        must accept. For prepaid plans, an unbilled **recurring period charge** (open position with
        null `adjustmentNet`) uses an elapsed signed correction; an already-invoiced period uses the
        remaining-period delta (charge OP or partial credit document). Rejections carry a stable
        machine-readable `code`.
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigChangeRequestDto'
      responses:
        '200':
          description: Configuration change preview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigChangePreviewResponse'
        '400':
          description: Configuration change not permitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/config-change:
    post:
      summary: Request a subscription configuration change
      operationId: requestSubscriptionConfigChange
      description: |
        Moves the subscription to `pending_config_change` and records a pending change run.
        The change is applied asynchronously; poll the returned run for its status.
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfigChangeRequestDto'
      responses:
        '201':
          description: Configuration change accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigChangeResponse'
        '400':
          description: Configuration change not permitted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/config-change/eligibility:
    get:
      summary: Get configuration change eligibility
      operationId: getSubscriptionConfigChangeEligibility
      description: |
        Reports whether the subscription can currently be reconfigured, plus the server types and
        addons the plan offers. Provider capabilities are fail-closed.
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Configuration change eligibility
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConfigChangeEligibility'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/items:
    get:
      summary: List subscription items
      operationId: listSubscriptionItems
      description: Returns items for the subscription (id, provisioningStatus, serviceTypeId, serviceTypeName). User must own the subscription.
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: List of subscription items
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SubscriptionItemResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/items/{itemId}/server-info:
    get:
      summary: Get server info for a provisioned subscription item
      operationId: getSubscriptionItemServerInfo
      description: Returns live server info from the provider and updates the cached snapshot. Only available when provisioning is finished (status active). User must own the subscription.
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Subscription ID
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Subscription item ID
      responses:
        '200':
          description: Server info (provider-agnostic)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerInfoResponse'
        '400':
          description: Not provisioned or not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription or item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/items/{itemId}/ssh-access-key:
    get:
      summary: Reveal provisioning SSH private key (one-time)
      operationId: getSubscriptionItemSshAccessKey
      description: >
        Returns the provisioning SSH private key for a subscription item owned by the caller.
        Succeeds only once per item; sets sshAccessGranted on the item and blocks subsequent
        reveals. Viewing may limit technical support for that service. Never log the response body.
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Subscription ID
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
          description: Subscription item ID
      responses:
        '200':
          description: SSH private key (shown once)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubscriptionSshAccessKeyResponse'
        '400':
          description: Not provisioned, key unavailable, or not authenticated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription or item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '409':
          description: SSH access key has already been revealed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/items/{itemId}/actions/start:
    post:
      summary: Start server for a provisioned subscription item
      operationId: startSubscriptionItemServer
      description: User must own the subscription. Item must be provisioned (status active).
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Action accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerActionResponse'
        '400':
          description: Not provisioned or bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription or item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/items/{itemId}/actions/stop:
    post:
      summary: Stop server for a provisioned subscription item
      operationId: stopSubscriptionItemServer
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Action accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerActionResponse'
        '400':
          description: Not provisioned or bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription or item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /subscriptions/{subscriptionId}/items/{itemId}/actions/restart:
    post:
      summary: Restart server for a provisioned subscription item
      operationId: restartSubscriptionItemServer
      parameters:
        - name: subscriptionId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: itemId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Action accepted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerActionResponse'
        '400':
          description: Not provisioned or bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Subscription or item not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /backorders:
    get:
      summary: List backorders
      operationId: listBackorders
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Array of backorders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/BackorderResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /backorders/{id}/retry:
    post:
      summary: Retry backorder
      operationId: retryBackorder
      parameters:
        - $ref: '#/components/parameters/BackorderId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackorderRetryDto'
      responses:
        '200':
          description: Backorder retried
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackorderResponse'
        '400':
          description: Retry failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /backorders/{id}/cancel:
    post:
      summary: Cancel backorder
      operationId: cancelBackorder
      parameters:
        - $ref: '#/components/parameters/BackorderId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BackorderCancelDto'
      responses:
        '200':
          description: Backorder cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BackorderResponse'
        '400':
          description: Cancel failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /pricing/preview:
    post:
      summary: Preview pricing
      operationId: previewPricing
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PricingPreviewDto'
      responses:
        '200':
          description: Pricing preview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PricingPreviewResponse'
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /pricing/tax-preview:
    post:
      summary: Preview tax treatment and rates for the authenticated customer
      operationId: previewTaxForCustomer
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaxPreviewRequest'
      responses:
        '200':
          description: Tax preview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaxPreviewResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /customer-profile:
    get:
      summary: Get customer profile
      operationId: getCustomerProfile
      responses:
        '200':
          description: Customer profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Update customer profile
      operationId: updateCustomerProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerProfileDto'
      responses:
        '200':
          description: Customer profile updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /customer-profile/vat-id/revalidate:
    post:
      summary: Revalidate customer VAT ID via VIES
      operationId: revalidateCustomerVatId
      responses:
        '200':
          description: Customer profile with updated VAT validation status
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          description: Profile missing or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /customer-profile/auto-billing/setup:
    post:
      summary: Start auto-billing payment method setup
      operationId: setupCustomerAutoBilling
      description: Creates a hosted setup session for the default payment processor (requires supportsAutoPayment).
      responses:
        '200':
          description: Setup session URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutoBillingSetupResponse'
        '400':
          description: Processor does not support auto-billing or profile missing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /customer-profile/auto-billing/enable:
    post:
      summary: Enable auto-billing
      operationId: enableCustomerAutoBilling
      description: Requires a payment method on file and a processor that supports auto-payment.
      responses:
        '200':
          description: Updated customer profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          description: Missing payment method or unsupported processor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /customer-profile/auto-billing/disable:
    post:
      summary: Disable auto-billing
      operationId: disableCustomerAutoBilling
      description: Disables auto-billing and immediately unlocks manual payment on open invoices.
      responses:
        '200':
          description: Updated customer profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/summary:
    get:
      summary: Get open and overdue invoices summary
      operationId: getInvoicesSummary
      description: Returns count and total balance of open and overdue invoices for the current user across all subscriptions.
      responses:
        '200':
          description: Open and overdue invoices summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoicesSummaryResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/open-overdue:
    get:
      summary: List open and overdue invoices
      operationId: listOpenOverdueInvoices
      description: Returns all open and overdue invoices for the current user across subscriptions, with balance and optional subscription number.
      responses:
        '200':
          description: Array of open and overdue invoices
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InvoiceResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/history:
    get:
      summary: List invoice history
      operationId: listInvoiceHistory
      description: |
        Returns paid and void invoices for the current user.
        Draft invoices are never included in customer-facing lists.
      responses:
        '200':
          description: Array of history invoices
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InvoiceResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/ref/{invoiceRefId}:
    get:
      summary: Get invoice details by reference (no subscription)
      operationId: getInvoiceDetailsByRef
      description: |
        Returns invoice detail for the authenticated user when the invoice is not tied to a subscription
        (e.g. manual invoices). Access is scoped to invoices owned by the current user.
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Invoice detail with line items and tax breakdown
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceDetailResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/ref/{invoiceRefId}/pdf:
    get:
      summary: Download invoice PDF by reference (no subscription)
      operationId: downloadInvoicePdfByRef
      description: Downloads the invoice PDF for the authenticated user when no subscription context is required.
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: PDF file with embedded e-invoice XML
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized
        '404':
          description: Invoice PDF not found
  /invoices/ref/{invoiceRefId}/void-document/pdf:
    get:
      summary: Download void credit note PDF by reference (no subscription)
      operationId: downloadInvoiceVoidDocumentPdfByRef
      description: Downloads the void document PDF for the authenticated user when no subscription context is required.
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Credit note PDF with embedded e-invoice XML (type 381)
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Invoice is not voided
        '401':
          description: Unauthorized
        '404':
          description: Invoice not found
  /invoices/ref/{invoiceRefId}/time-report/pdf:
    get:
      summary: Download time report PDF by reference (no subscription)
      operationId: downloadInvoiceTimeReportPdfByRef
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Time report PDF for project invoices
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Time report not available
        '401':
          description: Unauthorized
        '404':
          description: Invoice not found
  /invoices/ref/{invoiceRefId}/pay:
    post:
      summary: Initiate payment by reference (no subscription)
      operationId: initiateInvoicePaymentByRef
      description: Starts Stripe Checkout for an invoice owned by the authenticated user without a subscription path segment.
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Checkout session URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InitiatePaymentResponse'
        '400':
          description: Invoice not payable
        '401':
          description: Unauthorized
  /invoices/{subscriptionId}:
    get:
      summary: List invoices
      operationId: listInvoices
      description: Returns non-draft invoices for the subscription owned by the current user.
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Array of invoices (drafts excluded)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/InvoiceResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
    post:
      summary: Create invoice (admin only)
      operationId: createInvoice
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateInvoiceDto'
      responses:
        '200':
          description: Invoice created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateInvoiceResponse'
        '400':
          description: Invoice creation failed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/{subscriptionId}/ref/{invoiceRefId}:
    get:
      summary: Get invoice details (preview)
      operationId: getInvoiceDetails
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Invoice detail with line items and tax breakdown
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceDetailResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '404':
          description: Invoice not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /invoices/{subscriptionId}/ref/{invoiceRefId}/pdf:
    get:
      summary: Download invoice PDF
      operationId: downloadInvoicePdf
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: PDF file with embedded e-invoice XML
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized
        '404':
          description: Invoice PDF not found
  /invoices/{subscriptionId}/ref/{invoiceRefId}/void-document/pdf:
    get:
      summary: Download void credit note PDF
      operationId: downloadInvoiceVoidDocumentPdf
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Credit note PDF with embedded e-invoice XML (type 381)
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Invoice is not voided
        '401':
          description: Unauthorized
        '404':
          description: Invoice not found
  /invoices/{subscriptionId}/ref/{invoiceRefId}/time-report/pdf:
    get:
      summary: Download time report PDF
      operationId: downloadInvoiceTimeReportPdf
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Time report PDF for project invoices
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Time report not available
        '401':
          description: Unauthorized
        '404':
          description: Invoice not found
  /invoices/{subscriptionId}/ref/{invoiceRefId}/pay:
    post:
      summary: Initiate payment (Stripe Checkout)
      operationId: initiateInvoicePayment
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Checkout session URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InitiatePaymentResponse'
        '400':
          description: Invoice not payable
        '401':
          description: Unauthorized
  /invoices/{subscriptionId}/ref/{invoiceRefId}/void:
    post:
      summary: Void invoice (admin only)
      operationId: voidInvoice
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: invoiceRefId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Voided invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceResponse'
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
  /webhooks/payments/stripe:
    post:
      summary: Stripe payment webhook
      operationId: stripePaymentWebhook
      security: []
      responses:
        '200':
          description: Webhook received
  /usage/summary/{subscriptionId}:
    get:
      summary: Usage summary
      operationId: getUsageSummary
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Usage summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageSummary'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /admin/usage/record:
    post:
      tags: [Admin Billing]
      summary: Record usage (admin or API key only)
      description: |
        Records billable usage for a subscription. Restricted to billing administrators and trusted
        `STATIC_API_KEY` automation callers. Customers cannot self-report usage via this endpoint.
      operationId: recordUsage
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUsageRecordDto'
      responses:
        '200':
          description: Usage recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageRecordResponse'
        '400':
          description: Validation error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
        '403':
          description: Admin or API key access required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /admin/billing/subscriptions/{subscriptionId}/meters:
    get:
      tags: [Admin Billing]
      summary: List subscription meter summaries (admin)
      operationId: adminListSubscriptionMeters
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Meter summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SubscriptionMeterSummary'
  /admin/billing/subscriptions/{subscriptionId}/meter-entries:
    get:
      tags: [Admin Billing]
      summary: List metered usage entries for a subscription
      operationId: adminListSubscriptionMeterEntries
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      responses:
        '200':
          description: Meter entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UsageMeterEntryResponse'
    post:
      tags: [Admin Billing]
      summary: Create a metered usage entry for a subscription
      operationId: adminCreateSubscriptionMeterEntry
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSubscriptionMeterEntryDto'
      responses:
        '200':
          description: Entry created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageRecordResponse'
  /admin/billing/subscriptions/{subscriptionId}/meter-entries/{entryId}:
    post:
      tags: [Admin Billing]
      summary: Update a metered usage entry
      operationId: adminUpdateSubscriptionMeterEntry
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: entryId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUsageMeterEntryDto'
      responses:
        '200':
          description: Updated entry
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UsageMeterEntryResponse'
    delete:
      tags: [Admin Billing]
      summary: Delete a metered usage entry
      operationId: adminDeleteSubscriptionMeterEntry
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
        - name: entryId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Entry deleted
  /admin/billing/summary:
    get:
      tags: [Admin Billing]
      summary: Global billing KPI summary (admin only)
      operationId: getAdminBillingSummary
      responses:
        '200':
          description: Global KPI cards
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminBillingSummaryResponse'
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
  /admin/billing/bill-now:
    post:
      tags: [Admin Billing]
      summary: Bill unbilled open positions now (admin only)
      operationId: adminBillNow
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminBillNowDto'
      responses:
        '200':
          description: Bill-now job queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminBillNowResponse'
  /admin/billing/subscriptions:
    get:
      tags: [Admin Billing]
      summary: List all subscriptions (admin only)
      operationId: listAdminSubscriptions
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
        - name: search
          in: query
          description: Filter by subscription number, plan name, user email, status, or ids
          schema: { type: string }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Paginated subscription list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminSubscriptionsResponse'
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
  /admin/billing/subscriptions/{subscriptionId}/cancel:
    post:
      tags: [Admin Billing]
      summary: Cancel subscription (admin only)
      operationId: adminCancelSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CancelSubscriptionDto'
      responses:
        '200':
          description: Subscription cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionListItem'
        '400':
          description: Cancellation not permitted
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
        '404':
          description: Subscription not found
  /admin/billing/subscriptions/{subscriptionId}/withdraw:
    post:
      tags: [Admin Billing]
      summary: Withdraw subscription (admin only)
      operationId: adminWithdrawSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WithdrawSubscriptionDto'
      responses:
        '200':
          description: Subscription withdrawn
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionListItem'
        '400':
          description: Withdrawal not permitted
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
        '404':
          description: Subscription not found
  /admin/billing/subscriptions/{subscriptionId}/instant-cancel:
    post:
      tags: [Admin Billing]
      summary: Instant-cancel subscription (admin only)
      description: |
        Force-remove access immediately, bypassing commercial cancel and statutory withdrawal policy.
        Marks the subscription `pending_instant_cancel` with `instantRemoval=true` for the dedicated teardown job.
        Arrear plans bill used time through cancel; prepaid plans refund unused period like statutory withdrawal.
      operationId: adminInstantCancelSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/InstantCancelSubscriptionDto'
      responses:
        '200':
          description: Subscription marked for instant cancel
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionListItem'
        '400':
          description: Instant cancel not permitted
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
        '404':
          description: Subscription not found
  /admin/billing/subscriptions/{subscriptionId}/resume:
    post:
      tags: [Admin Billing]
      summary: Resume subscription (admin only)
      operationId: adminResumeSubscription
      parameters:
        - $ref: '#/components/parameters/SubscriptionId'
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResumeSubscriptionDto'
      responses:
        '200':
          description: Subscription resumed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminSubscriptionListItem'
        '400':
          description: Resume not permitted
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
        '404':
          description: Subscription not found
  /admin/billing/users/{userId}/subscriptions:
    get:
      tags: [Admin Billing]
      summary: List subscriptions for a user (admin only)
      operationId: listAdminUserSubscriptions
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          schema: { type: integer, default: 100 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Subscriptions for the user
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SubscriptionResponse'
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
        '404':
          description: User not found
  /admin/billing/invoices:
    get:
      tags: [Admin Billing]
      summary: List all invoices (admin only)
      operationId: listAdminInvoices
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
        - name: search
          in: query
          description: Filter by invoice number, subscription number, user email, or user id
          schema: { type: string }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Paginated invoice list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminInvoicesResponse'
  /admin/billing/invoices/open-overdue:
    get:
      tags: [Admin Billing]
      summary: List all invoices (deprecated alias)
      deprecated: true
      operationId: listAdminOpenOverdueInvoices
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
        - name: search
          in: query
          schema: { type: string }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Paginated invoice list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminInvoicesResponse'
  /admin/billing/invoices/manual:
    post:
      tags: [Admin Billing]
      summary: Create manual invoice draft (admin only)
      operationId: createManualInvoice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateManualInvoiceDto'
      responses:
        '201':
          description: Created draft invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManualInvoiceDetailResponse'
  /admin/billing/invoices/{invoiceRefId}:
    get:
      tags: [Admin Billing]
      summary: Get invoice detail (admin only)
      operationId: getAdminInvoiceDetail
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Invoice detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManualInvoiceDetailResponse'
    post:
      tags: [Admin Billing]
      summary: Update manual invoice draft (admin only)
      operationId: updateManualInvoice
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateManualInvoiceDto'
      responses:
        '200':
          description: Updated draft
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManualInvoiceDetailResponse'
    delete:
      tags: [Admin Billing]
      summary: Delete manual invoice draft (admin only)
      operationId: deleteManualInvoice
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Draft deleted
  /admin/billing/invoices/{invoiceRefId}/issue:
    post:
      tags: [Admin Billing]
      summary: Issue manual invoice draft (admin only)
      operationId: issueManualInvoice
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IssueManualInvoiceDto'
      responses:
        '200':
          description: Issued invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManualInvoiceDetailResponse'
  /admin/billing/invoices/{invoiceRefId}/pdf:
    get:
      tags: [Admin Billing]
      summary: Download invoice PDF (admin only)
      operationId: downloadAdminInvoicePdf
      description: Downloads the invoice PDF for any invoice by ID. Supports subscription and manual invoices.
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: PDF file with embedded e-invoice XML
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Invoice not found
  /admin/billing/invoices/{invoiceRefId}/void-document/pdf:
    get:
      tags: [Admin Billing]
      summary: Download void credit note PDF (admin only)
      operationId: downloadAdminInvoiceVoidDocumentPdf
      description: Downloads the void document PDF for any voided invoice by ID.
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Credit note PDF with embedded e-invoice XML (type 381)
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Invoice is not voided
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Invoice not found
  /admin/billing/invoices/{invoiceRefId}/time-report/pdf:
    get:
      tags: [Admin Billing]
      summary: Download stored time report PDF (admin only)
      operationId: downloadAdminInvoiceTimeReportPdf
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Time report PDF for project invoices
          content:
            application/pdf:
              schema:
                type: string
                format: binary
        '400':
          description: Time report not available
        '401':
          description: Unauthorized
        '403':
          description: Forbidden
        '404':
          description: Invoice not found
  /promotions/validate:
    post:
      tags: [Promotions]
      summary: Validate a promotion code (simulated redemption)
      operationId: validatePromotionCode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidatePromotionDto'
      responses:
        '200':
          description: Validation result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionValidationResponseDto'
  /promotions/redeem:
    post:
      tags: [Promotions]
      summary: Redeem a promotion code
      operationId: redeemPromotionCode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RedeemPromotionDto'
      responses:
        '200':
          description: Redemption created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PromotionRedemptionResponseDto'
  /promotions/redemptions:
    get:
      tags: [Promotions]
      summary: List promotion redemptions for current user
      operationId: listPromotionRedemptions
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated redemptions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPromotionRedemptionsResponseDto'
  /promotions/active:
    get:
      tags: [Promotions]
      summary: List active promotion benefits for current user
      operationId: listActivePromotions
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated active benefits
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPromotionRedemptionsResponseDto'
  /admin/billing/promotions:
    get:
      tags: [Admin Billing]
      summary: List promotions (admin)
      operationId: listAdminPromotions
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated promotions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminPromotionsResponseDto'
    post:
      tags: [Admin Billing]
      summary: Create promotion (admin)
      operationId: createAdminPromotion
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAdminPromotionDto'
      responses:
        '201':
          description: Created promotion
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPromotionResponseDto'
  /admin/billing/promotions/{id}:
    get:
      tags: [Admin Billing]
      summary: Get promotion (admin)
      operationId: getAdminPromotion
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Promotion
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPromotionResponseDto'
    put:
      tags: [Admin Billing]
      summary: Update promotion (admin)
      operationId: updateAdminPromotion
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAdminPromotionDto'
      responses:
        '200':
          description: Updated promotion
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPromotionResponseDto'
    delete:
      tags: [Admin Billing]
      summary: Deactivate promotion (admin)
      operationId: deactivateAdminPromotion
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Deactivated promotion
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminPromotionResponseDto'
  /admin/billing/promotions/{id}/redemptions:
    get:
      tags: [Admin Billing]
      summary: List redemptions for a promotion (admin)
      operationId: listAdminPromotionRedemptions
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated redemptions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedPromotionRedemptionsResponseDto'
  /admin/billing/customer-profiles:
    get:
      tags: [Admin Billing]
      summary: List customer billing profiles (admin only)
      operationId: listAdminCustomerProfiles
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated profiles
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminCustomerProfilesResponse'
    post:
      tags: [Admin Billing]
      summary: Create customer billing profile (admin only)
      operationId: createAdminCustomerProfile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAdminCustomerProfileDto'
      responses:
        '201':
          description: Created profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
  /admin/billing/customer-profiles/{id}:
    get:
      tags: [Admin Billing]
      summary: Get customer billing profile (admin only)
      operationId: getAdminCustomerProfile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Profile detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCustomerProfileDetailResponse'
    post:
      tags: [Admin Billing]
      summary: Update customer billing profile (admin only)
      operationId: updateAdminCustomerProfile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CustomerProfileDto'
      responses:
        '200':
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
    delete:
      tags: [Admin Billing]
      summary: Delete customer billing profile (admin only)
      operationId: deleteAdminCustomerProfile
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Profile deleted
  /admin/billing/customer-profiles/{id}/data:
    post:
      tags: [Admin Billing]
      summary: Add custom data key on customer billing profile (admin only)
      description: >
        Adds a new key/value pair to the encrypted admin-only custom data map.
        Fails with 409 if the key already exists. Values are never included in webhook payloads.
      operationId: addAdminCustomerProfileCustomData
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddCustomerProfileCustomDataDto'
      responses:
        '200':
          description: Updated admin profile detail including customData
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCustomerProfileDetailResponse'
        '409':
          description: Custom data key already exists
  /admin/billing/customer-profiles/{id}/data/{key}:
    post:
      tags: [Admin Billing]
      summary: Update custom data value on customer billing profile (admin only)
      description: >
        Updates an existing key in the encrypted admin-only custom data map.
        Fails with 404 if the key does not exist. Values are never included in webhook payloads.
      operationId: updateAdminCustomerProfileCustomData
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: key
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 64
            pattern: '^[a-zA-Z0-9._-]+$'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateCustomerProfileCustomDataDto'
      responses:
        '200':
          description: Updated admin profile detail including customData
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCustomerProfileDetailResponse'
        '404':
          description: Custom data key not found
    delete:
      tags: [Admin Billing]
      summary: Delete custom data key on customer billing profile (admin only)
      description: >
        Removes an existing key from the encrypted admin-only custom data map.
        Fails with 404 if the key does not exist. Values are never included in webhook payloads.
      operationId: deleteAdminCustomerProfileCustomData
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: key
          in: path
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 64
            pattern: '^[a-zA-Z0-9._-]+$'
      responses:
        '200':
          description: Updated admin profile detail including customData
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminCustomerProfileDetailResponse'
        '404':
          description: Custom data key not found
  /admin/billing/customer-profiles/{id}/trust-score:
    get:
      tags: [Admin Billing]
      summary: Get customer trust score detail (admin only)
      operationId: getAdminCustomerProfileTrustScore
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Trust score detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerTrustScoreResponse'
  /admin/billing/customer-profiles/{id}/trust-score/recompute:
    post:
      tags: [Admin Billing]
      summary: Recompute customer trust score (admin only)
      operationId: recomputeAdminCustomerProfileTrustScore
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Recomputed trust score detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerTrustScoreResponse'
  /admin/billing/customer-profiles/{id}/auto-billing/setup:
    post:
      tags: [Admin Billing]
      summary: Start auto-billing payment method setup for a customer (admin)
      operationId: adminSetupCustomerAutoBilling
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Setup session URL
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AutoBillingSetupResponse'
        '400':
          description: Processor does not support auto-billing or profile missing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /admin/billing/customer-profiles/{id}/auto-billing/enable:
    post:
      tags: [Admin Billing]
      summary: Enable auto-billing for a customer (admin)
      operationId: adminEnableCustomerAutoBilling
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Updated customer profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
        '400':
          description: Missing payment method or unsupported processor
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /admin/billing/customer-profiles/{id}/auto-billing/disable:
    post:
      tags: [Admin Billing]
      summary: Disable auto-billing for a customer (admin)
      operationId: adminDisableCustomerAutoBilling
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Updated customer profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CustomerProfileResponse'
  /admin/billing/invoices/{invoiceRefId}/void:
    post:
      tags: [Admin Billing]
      summary: Void invoice (admin only)
      operationId: adminVoidInvoice
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Voided invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminInvoiceListItem'
  /admin/billing/invoices/{invoiceRefId}/mark-paid:
    post:
      tags: [Admin Billing]
      summary: Mark invoice paid manually (admin only)
      operationId: adminMarkInvoicePaid
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MarkInvoicePaymentStatusDto'
      responses:
        '200':
          description: Updated invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminInvoiceListItem'
  /admin/billing/invoices/{invoiceRefId}/mark-unpaid:
    post:
      tags: [Admin Billing]
      summary: Mark invoice unpaid manually (admin only)
      operationId: adminMarkInvoiceUnpaid
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MarkInvoicePaymentStatusDto'
      responses:
        '200':
          description: Updated invoice
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminInvoiceListItem'
  /admin/billing/invoices/{invoiceRefId}/audit-logs:
    get:
      tags: [Admin Billing]
      summary: Audit history for invoice (admin only)
      operationId: listAdminInvoiceAuditLogs
      parameters:
        - name: invoiceRefId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated audit logs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedBillingAuditLogsResponse'
  /admin/billing/statistics/summary:
    get:
      tags: [Admin Billing]
      summary: Turnover time series (admin only)
      operationId: getAdminBillingStatisticsSummary
      parameters:
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
        - name: groupBy
          in: query
          schema: { type: string, enum: [day, month], default: day }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Statistics summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingStatisticsSummary'
  /admin/billing/statistics/by-product:
    get:
      tags: [Admin Billing]
      summary: Turnover by service plan (admin only)
      operationId: getAdminBillingStatisticsByProduct
      parameters:
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Turnover by product
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingStatisticsByProduct'
  /admin/billing/statistics/by-country:
    get:
      tags: [Admin Billing]
      summary: Turnover by buyer country (admin only)
      operationId: getAdminBillingStatisticsByCountry
      parameters:
        - name: from
          in: query
          schema: { type: string, format: date }
        - name: to
          in: query
          schema: { type: string, format: date }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Turnover by buyer country
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingStatisticsByCountry'
  /admin/billing/capabilities:
    get:
      tags: [Admin Billing]
      summary: Billing feature capabilities for admin UI (admin only)
      operationId: getAdminBillingCapabilities
      responses:
        '200':
          description: Feature flags for the current request tenant
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillingCapabilitiesResponse'
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
  /admin/billing/tax/preview:
    post:
      tags: [Admin Billing]
      summary: Preview tax treatment and rates (issuer or customer)
      operationId: previewTaxForAdmin
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TaxPreviewRequest'
      responses:
        '200':
          description: Tax preview
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaxPreviewResponse'
        '401':
          description: Unauthorized
        '403':
          description: Admin access required
  /admin/billing/datev-exports:
    get:
      tags: [Admin Billing]
      summary: List DATEV EXTF exports (admin only; requires BILLING_DATEV_EXPORT_ENABLED)
      operationId: listAdminDatevExports
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 20 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
        - name: year
          in: query
          schema: { type: integer }
        - name: scope
          in: query
          schema:
            type: string
            enum: [tenant, unified]
            default: tenant
          description: Unified scope requires allowlisted X-Tenant
      responses:
        '200':
          description: Paginated DATEV export list
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminDatevExportsResponse'
        '403':
          description: Unified export access denied for this tenant
        '404':
          description: DATEV export feature disabled
    post:
      tags: [Admin Billing]
      summary: Trigger DATEV export for a calendar month (admin only)
      operationId: triggerAdminDatevExport
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TriggerDatevExportDto'
      responses:
        '200':
          description: Export job queued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriggerDatevExportResponse'
        '403':
          description: Unified export access denied for this tenant
        '404':
          description: DATEV export feature disabled
  /admin/billing/datev-exports/{exportId}:
    get:
      tags: [Admin Billing]
      summary: Get DATEV export metadata (admin only)
      operationId: getAdminDatevExport
      parameters:
        - name: exportId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Export metadata
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminDatevExportListItem'
        '403':
          description: Export belongs to another tenant or unified access denied
        '404':
          description: Export not found or feature disabled
  /admin/billing/datev-exports/{exportId}/download:
    get:
      tags: [Admin Billing]
      summary: Download DATEV export ZIP (admin only)
      operationId: downloadAdminDatevExport
      parameters:
        - name: exportId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: ZIP archive (EXTF CSV + optional PDF bundle)
          content:
            application/zip:
              schema:
                type: string
                format: binary
        '403':
          description: Export belongs to another tenant or unified access denied
        '404':
          description: Export not found, not completed, or feature disabled
  /projects:
    get:
      tags: [Projects]
      summary: List projects assigned to the authenticated customer
      operationId: listCustomerProjects
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Paginated customer projects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedProjectsResponse'
  /projects/{projectId}:
    get:
      tags: [Projects]
      summary: Get project detail (customer read-only)
      operationId: getCustomerProject
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
  /projects/{projectId}/summary:
    get:
      tags: [Projects]
      summary: Get project KPI summary
      operationId: getProjectSummary
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project summary KPIs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSummaryResponse'
  /admin/billing/projects:
    get:
      tags: [Admin Billing]
      summary: List all projects (admin)
      operationId: listAdminProjects
      parameters:
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
        - name: search
          in: query
          schema: { type: string }
        - name: userId
          in: query
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Paginated admin projects
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAdminProjectsResponse'
    post:
      tags: [Admin Billing]
      summary: Create project (admin)
      operationId: createAdminProject
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAdminProjectDto'
      responses:
        '201':
          description: Created project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
  /admin/billing/projects/{projectId}:
    get:
      tags: [Admin Billing]
      summary: Get project detail (admin)
      operationId: getAdminProject
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project detail with summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AdminProjectDetailResponse'
    post:
      tags: [Admin Billing]
      summary: Update project (admin)
      operationId: updateAdminProject
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAdminProjectDto'
      responses:
        '200':
          description: Updated project
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectResponse'
    delete:
      tags: [Admin Billing]
      summary: Delete project when no unbilled time entries
      operationId: deleteAdminProject
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '204':
          description: Deleted
  /admin/billing/projects/{projectId}/summary:
    get:
      tags: [Admin Billing]
      summary: Get project KPI summary (admin)
      operationId: getAdminProjectSummary
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Project summary KPIs
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectSummaryResponse'
  /admin/billing/projects/{projectId}/unbilled-time-bounds:
    get:
      tags: [Admin Billing]
      summary: Unbilled time entry range for bill-time defaults
      operationId: getProjectUnbilledTimeBounds
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Oldest unbilled start and newest unbilled end
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectUnbilledTimeBounds'
  /admin/billing/projects/{projectId}/bill-time:
    post:
      tags: [Admin Billing]
      summary: Bill unbilled project time in range to invoice
      operationId: billProjectTime
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BillProjectTimeRequest'
      responses:
        '200':
          description: Billing result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BillProjectTimeResponse'
  /admin/billing/projects/{projectId}/time-report:
    post:
      tags: [Admin Billing]
      summary: Generate live time report PDF (admin only)
      operationId: generateProjectTimeReport
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProjectTimeReportRequest'
      responses:
        '200':
          description: Time report PDF
          content:
            application/pdf:
              schema:
                type: string
                format: binary
  /projects/{projectId}/tickets:
    get:
      tags: [Project Board]
      summary: List project tickets
      operationId: listProjectTickets
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - name: status
          in: query
          schema:
            $ref: '#/components/schemas/ProjectTicketStatus'
        - name: parentId
          in: query
          schema: { type: string }
      responses:
        '200':
          description: Ticket list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectTicketResponse'
    post:
      tags: [Project Board]
      summary: Create ticket (admin only)
      operationId: createProjectTicket
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectTicketDto'
      responses:
        '201':
          description: Created ticket
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTicketResponse'
  /projects/{projectId}/tickets/{ticketId}:
    get:
      tags: [Project Board]
      summary: Get project ticket
      description: Requires `tickets:read`. Set `includeDescendants=true` to embed child tickets.
      operationId: getProjectTicket
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TicketId'
        - name: includeDescendants
          in: query
          schema: { type: boolean, default: false }
      responses:
        '200':
          description: Ticket detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTicketResponse'
    post:
      tags: [Project Board]
      summary: Update project ticket (admin write)
      description: Requires `tickets:write`. Locked tickets reject content mutations.
      operationId: updateProjectTicket
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TicketId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectTicketDto'
      responses:
        '200':
          description: Updated ticket
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTicketResponse'
    delete:
      tags: [Project Board]
      summary: Delete project ticket (admin write)
      description: Requires `tickets:write`.
      operationId: deleteProjectTicket
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TicketId'
      responses:
        '204':
          description: Ticket deleted
  /projects/{projectId}/tickets/{ticketId}/activity:
    get:
      tags: [Project Board]
      summary: List ticket activity
      description: Requires `tickets:read`.
      operationId: listProjectTicketActivity
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TicketId'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
      responses:
        '200':
          description: Activity events
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectTicketActivityResponse'
  /projects/{projectId}/tickets/{ticketId}/comments:
    get:
      tags: [Project Board]
      summary: List ticket comments
      operationId: listProjectTicketComments
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TicketId'
      responses:
        '200':
          description: Comments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectTicketCommentResponse'
    post:
      tags: [Project Board]
      summary: Add ticket comment (customer or admin)
      operationId: createProjectTicketComment
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TicketId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectTicketCommentDto'
      responses:
        '201':
          description: Created comment
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTicketCommentResponse'
  /projects/{projectId}/milestones:
    get:
      tags: [Projects]
      summary: List project milestones
      operationId: listProjectMilestones
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      responses:
        '200':
          description: Milestones
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProjectMilestoneResponse'
    post:
      tags: [Projects]
      summary: Create project milestone (admin write)
      description: Requires `milestones:write`.
      operationId: createProjectMilestone
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectMilestoneDto'
      responses:
        '201':
          description: Created milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectMilestoneResponse'
  /projects/{projectId}/milestones/{milestoneId}:
    post:
      tags: [Projects]
      summary: Update project milestone (admin write)
      description: Requires `milestones:write`. Locked milestones reject mutations.
      operationId: updateProjectMilestone
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/MilestoneId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectMilestoneDto'
      responses:
        '200':
          description: Updated milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectMilestoneResponse'
    delete:
      tags: [Projects]
      summary: Delete project milestone (admin write)
      description: Requires `milestones:write`.
      operationId: deleteProjectMilestone
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/MilestoneId'
      responses:
        '204':
          description: Milestone deleted
  /projects/{projectId}/milestones/{milestoneId}/lock:
    post:
      tags: [Projects]
      summary: Lock project milestone (admin write)
      description: Requires `milestones:write`. Prevents further milestone edits and related ticket moves.
      operationId: lockProjectMilestone
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/MilestoneId'
      responses:
        '200':
          description: Locked milestone
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectMilestoneResponse'
  /projects/{projectId}/time-entries:
    get:
      tags: [Project Board]
      summary: List project time entries
      description: |
        Requires `projects:read`. Assigned customers and admins can list entries for readable projects.
        Optional `ticketId` filters entries linked to a ticket.
      operationId: listProjectTimeEntries
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/Limit'
        - $ref: '#/components/parameters/Offset'
        - name: ticketId
          in: query
          schema: { type: string, format: uuid }
          description: Optional ticket filter
      responses:
        '200':
          description: Paginated time entries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedProjectTimeEntriesResponse'
    post:
      tags: [Project Board]
      summary: Create project time entry (admin write)
      description: |
        Requires `time_entries:write`. Admin-only mutation. Entries with `billedAt` set are immutable after billing.
      operationId: createProjectTimeEntry
      parameters:
        - $ref: '#/components/parameters/ProjectId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateProjectTimeEntryDto'
      responses:
        '201':
          description: Created time entry
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTimeEntryResponse'
  /projects/{projectId}/time-entries/{timeEntryId}:
    post:
      tags: [Project Board]
      summary: Update project time entry (admin write)
      description: |
        Requires `time_entries:write`. Admin-only mutation.
        Entries with `billedAt` set cannot be updated.
      operationId: updateProjectTimeEntry
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TimeEntryId'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateProjectTimeEntryDto'
      responses:
        '200':
          description: Updated time entry
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProjectTimeEntryResponse'
    delete:
      tags: [Project Board]
      summary: Delete project time entry (admin write)
      description: |
        Requires `time_entries:write`. Admin-only mutation.
        Entries with `billedAt` set cannot be deleted.
      operationId: deleteProjectTimeEntry
      parameters:
        - $ref: '#/components/parameters/ProjectId'
        - $ref: '#/components/parameters/TimeEntryId'
      responses:
        '204':
          description: Time entry deleted
  /admin/billing/webhooks/event-types:
    get:
      tags: [Admin Billing]
      summary: List webhook event types (admin)
      operationId: listAdminWebhookEventTypes
      responses:
        '200':
          description: Event catalog
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookEventTypeResponseDto'
  /admin/billing/webhooks:
    get:
      tags: [Admin Billing]
      summary: List webhook endpoints (admin)
      operationId: listAdminWebhookEndpoints
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Webhook endpoints
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookEndpointResponseDto'
    post:
      tags: [Admin Billing]
      summary: Create webhook endpoint (admin)
      operationId: createAdminWebhookEndpoint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWebhookEndpointDto'
      responses:
        '201':
          description: Created endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointResponseDto'
  /admin/billing/webhooks/{id}:
    get:
      tags: [Admin Billing]
      summary: Get webhook endpoint (admin)
      operationId: getAdminWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Webhook endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointResponseDto'
    put:
      tags: [Admin Billing]
      summary: Update webhook endpoint (admin)
      operationId: updateAdminWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWebhookEndpointDto'
      responses:
        '200':
          description: Updated endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointResponseDto'
    delete:
      tags: [Admin Billing]
      summary: Delete webhook endpoint (admin)
      operationId: deleteAdminWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Deleted
  /admin/billing/webhooks/{id}/test:
    post:
      tags: [Admin Billing]
      summary: Send test webhook delivery (admin)
      operationId: testAdminWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Delivery result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookDeliveryResponseDto'
  /admin/billing/webhooks/{id}/deliveries:
    get:
      tags: [Admin Billing]
      summary: List webhook deliveries (admin)
      operationId: listAdminWebhookDeliveries
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated deliveries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedWebhookDeliveriesResponseDto'
  /admin/billing/updates:
    get:
      tags: [Admin Billing]
      summary: Get full application updates state (admin)
      operationId: getBillingUpdatesFullState
      responses:
        '200':
          description: Full updates state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatesFullStateDto'
  /admin/billing/updates/status:
    get:
      tags: [Admin Billing]
      summary: Get application updates status summary (admin)
      operationId: getBillingUpdatesStatusSummary
      responses:
        '200':
          description: Updates status summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatesStatusSummaryDto'
  /admin/billing/updates/check:
    post:
      tags: [Admin Billing]
      summary: Trigger application update check (admin)
      operationId: triggerBillingUpdateCheck
      responses:
        '202':
          description: Update check enqueued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateCheckTriggerResultDto'
  /otel/metrics:
    get:
      summary: Prometheus metrics scrape
      description: |
        OpenTelemetry Prometheus exposition endpoint. Mounted at the HTTP root, **outside** the `/api` global prefix.
        Path is configurable via `OTEL_METRICS_PATH` (default `/otel/metrics`). Requires HTTP Basic authentication
        with `OTEL_USERNAME` and `OTEL_PASSWORD`. Disabled when `OTEL_ENABLED` is not exactly `true`.
      operationId: getOtelMetrics
      servers:
        - url: http://localhost:3200
      security:
        - basicAuth: []
      responses:
        '200':
          description: Prometheus text exposition format
          content:
            text/plain:
              schema:
                type: string
        '401':
          description: Missing or invalid Basic auth credentials
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
    basicAuth:
      type: http
      scheme: basic
  parameters:
    Limit:
      in: query
      name: limit
      schema:
        type: integer
        minimum: 0
      description: Maximum number of results to return (default 10)
    Offset:
      in: query
      name: offset
      schema:
        type: integer
        minimum: 0
      description: Number of results to skip (default 0)
    ServiceTypeId:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: Service type ID
    ServicePlanId:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: Service plan ID
    SubscriptionId:
      in: path
      name: subscriptionId
      required: true
      schema:
        type: string
        format: uuid
      description: Subscription ID
    ProjectId:
      in: path
      name: projectId
      required: true
      schema:
        type: string
        format: uuid
      description: Project ID
    TicketId:
      in: path
      name: ticketId
      required: true
      schema:
        type: string
        format: uuid
      description: Project ticket ID
    MilestoneId:
      in: path
      name: milestoneId
      required: true
      schema:
        type: string
        format: uuid
      description: Project milestone ID
    TimeEntryId:
      in: path
      name: timeEntryId
      required: true
      schema:
        type: string
        format: uuid
      description: Project time entry ID
    BackorderId:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: Backorder ID
    UserId:
      in: path
      name: id
      required: true
      schema:
        type: string
        format: uuid
      description: User ID
    XTentHeader:
      in: header
      name: X-Tenant
      required: false
      schema:
        type: string
        default: default
      description: |
        Tenant id for request scoping. Defaults to `default` when omitted (unless `TENANTS_ALLOW_DEFAULT=false`).
        When default is disabled, missing, blank, or `default` values are rejected. Must be listed in the server
        `TENANTS` allowlist. Invalid values return 400.
  schemas:
    ErrorResponse:
      type: object
      properties:
        statusCode:
          type: integer
        message:
          type: string
        error:
          type: string
        code:
          type: string
          description: Optional machine-readable error code (e.g. EMAIL_NOT_CONFIRMED on login)
    MessageResponse:
      type: object
      properties:
        message:
          type: string
    ChangePasswordResponse:
      type: object
      properties:
        message:
          type: string
        access_token:
          type: string
    LogoutDto:
      type: object
      properties:
        invalidateAllSessions:
          type: boolean
          description: When true, invalidates every active JWT session for the user. Default is false (current session only).
    LoginDto:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    LoginResponse:
      type: object
      properties:
        access_token:
          type: string
        user:
          $ref: '#/components/schemas/UserResponse'
        scopes:
          type: array
          items:
            type: string
    PatLoginResponse:
      $ref: '#/components/schemas/LoginResponse'
    ExchangePersonalAccessTokenDto:
      type: object
      required: [token]
      properties:
        token:
          type: string
    CreatePersonalAccessTokenDto:
      type: object
      required: [name, scopes]
      properties:
        name:
          type: string
        scopes:
          type: array
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
    UpdatePersonalAccessTokenDto:
      type: object
      required: [name, scopes]
      properties:
        name:
          type: string
        scopes:
          type: array
          items:
            type: string
    PersonalAccessTokenScopeDto:
      type: object
      required: [scope]
      properties:
        scope:
          type: string
    PersonalAccessTokenResponseDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        tokenPrefix:
          type: string
        scopes:
          type: array
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
          nullable: true
        revokedAt:
          type: string
          format: date-time
          nullable: true
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        token:
          type: string
    RegisterDto:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
    RegisterResponse:
      type: object
      properties:
        user:
          $ref: '#/components/schemas/UserResponse'
        message:
          type: string
    ConfirmEmailDto:
      type: object
      required: [email, code]
      properties:
        email:
          type: string
          format: email
        code:
          type: string
          pattern: '^[A-Z0-9]{6}$'
    RequestPasswordResetDto:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
    ResetPasswordDto:
      type: object
      required: [email, code, newPassword]
      properties:
        email:
          type: string
          format: email
        code:
          type: string
          pattern: '^[A-Z0-9]{6}$'
        newPassword:
          type: string
          format: password
          minLength: 8
    ChangePasswordDto:
      type: object
      required: [currentPassword, newPassword, newPasswordConfirmation]
      properties:
        currentPassword:
          type: string
          format: password
        newPassword:
          type: string
          format: password
          minLength: 8
        newPasswordConfirmation:
          type: string
          format: password
          minLength: 8
    CreateUserDto:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        role:
          $ref: '#/components/schemas/UserRole'
    UpdateUserDto:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          minLength: 8
        role:
          $ref: '#/components/schemas/UserRole'
    UserResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        role:
          $ref: '#/components/schemas/UserRole'
        emailConfirmedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    UserRole:
      type: string
      enum: [user, admin]
    ProviderDetail:
      type: object
      required: [id, displayName]
      description: A registered provisioning provider (e.g. Hetzner) with id, display name, and optional config schema.
      properties:
        id:
          type: string
          description: Provider identifier (e.g. hetzner, digital-ocean). Used as the value for service type provider field.
        displayName:
          type: string
          description: Human-readable display name (e.g. Hetzner Cloud-Init).
        configSchema:
          type: object
          description: |
            Optional JSON schema for provider-specific configuration when creating subscriptions.
            - `properties`: each property may include `type`, `description`, optionally `enum` (array of allowed values), `scope` (`server` or `product`), and `productServices` (`agenstra-controller`, `agenstra-manager`, `decabill-billing`).
              Server-scoped fields appear under provider defaults in the plan editor; product-scoped fields appear under product defaults when required by selected customer options.
            - `basePriceFromField`: optional string (e.g. "serverType") indicating which field's selected option drives the plan base price;
              the UI should fetch options from GET .../providers/{id}/server-types and use the selected option's price as base price.
        envDefaultFields:
          type: array
          description: Platform environment variables that can be overridden per service type (e.g. HETZNER_API_TOKEN).
          items:
            $ref: '#/components/schemas/ProviderEnvDefaultField'
        supportsAddons:
          type: boolean
          description: |
            When true, plans for this provider may offer customer-selectable addons.
            Omitted or false means the provider lacks required provision/teardown or cloud-init append hooks (fail closed).
        supportsServerTypeUpgrade:
          type: boolean
          description: When true, in-place server type upgrades are supported for this provider.
        supportsServerTypeDowngrade:
          type: boolean
          description: When true, in-place server type downgrades are supported for this provider.
        meters:
          type: array
          description: |
            Required usage meters declared by the provider (or product) implementation.
            Sideloaded onto service types as non-removable attachments (`source=provider`, `required=true`).
          items:
            $ref: '#/components/schemas/DeclaredMeterDefinition'
    DeclaredMeterDefinition:
      type: object
      required: [key, name, aggregator, defaultUnitPriceNet]
      description: Fixed meter declaration from a provider or addon module.
      properties:
        key: { type: string }
        name: { type: string }
        description: { type: string }
        unitLabel: { type: string }
        aggregator:
          {
            type: string,
            enum: [max, min, avg, first, last, sum, sum_positive_deltas],
          }
        defaultUnitPriceNet: { type: number }
    AddonModuleDetail:
      type: object
      required: [key, displayName, meters]
      description: Registered addon module with optional declared required meters.
      properties:
        key: { type: string }
        displayName: { type: string }
        meters:
          type: array
          items:
            $ref: '#/components/schemas/DeclaredMeterDefinition'
    ProviderEnvDefaultField:
      type: object
      required: [envKey, label, sensitive, type]
      properties:
        envKey:
          type: string
          description: Environment variable name (e.g. HETZNER_API_TOKEN)
        label:
          type: string
          description: Human-readable field label for the admin UI
        sensitive:
          type: boolean
          description: When true, value is write-only in API responses
        type:
          type: string
          enum: [string]
    ServerType:
      type: object
      description: Provider server type with specs and pricing (from GET .../providers/{providerId}/server-types).
      properties:
        id: { type: string, description: 'Unique identifier (e.g. cax11)' }
        name: { type: string, description: 'Human-readable name' }
        cores: { type: number }
        memory: { type: number, description: 'RAM in GB' }
        disk: { type: number, description: 'Disk in GB' }
        priceMonthly:
          { type: number, description: 'Price per month (e.g. EUR)' }
        priceHourly: { type: number, description: 'Price per hour (e.g. EUR)' }
        description: { type: string }
    ProviderLocation:
      type: object
      description: Provider geography option (location or region) with human-readable label.
      required: [id, name]
      properties:
        id:
          type: string
          description: Technical slug used for provisioning (e.g. fsn1, fra1)
        name:
          type: string
          description: Human-readable display name from provider API or static fallback
        city:
          type: string
          description: City when provided by the provider API (Hetzner)
        country:
          type: string
          description: ISO country code when provided by the provider API
    CreateServiceTypeDto:
      type: object
      required: [key, name, provider]
      properties:
        key:
          type: string
          description: Unique service type key
        name:
          type: string
          description: Display name
        description:
          type: string
          description: Optional description
        provider:
          type: string
          description: Provider identifier (e.g. hetzner, digital-ocean)
        configSchema:
          type: object
          description: JSON schema for provider configuration
        isActive:
          type: boolean
          description: Whether the service type is active
        disallowStatutoryWithdrawal:
          type: boolean
          description: When true, statutory withdrawal is blocked after provisioning (unprovisioned orders remain withdrawable).
        providerDefaults:
          type: object
          additionalProperties:
            type: string
          description: Write-only platform credential overrides (e.g. HETZNER_API_TOKEN). Omit or leave values blank to use environment defaults.
    UpdateServiceTypeDto:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        provider:
          type: string
        configSchema:
          type: object
        isActive:
          type: boolean
        disallowStatutoryWithdrawal:
          type: boolean
          description: When true, statutory withdrawal is blocked after provisioning.
        providerDefaults:
          type: object
          additionalProperties:
            type: string
          description: Write-only platform credential overrides. Empty values clear overrides on update.
    ServiceTypeResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        provider: { type: string }
        configSchema: { type: object }
        isActive: { type: boolean }
        disallowStatutoryWithdrawal: { type: boolean }
        providerDefaultsConfigured:
          type: object
          additionalProperties:
            type: boolean
          description: Indicates which platform env overrides are configured (never returns secret values).
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ServicePlanOrderingHighlight:
      type: object
      required: [icon, text]
      description: |
        One ordered line for catalog or checkout UI. `icon` is an opaque string (e.g. icon-set key or URL)
        interpreted by the client; `text` is human-readable copy shown beside the icon.
      properties:
        icon:
          type: string
          maxLength: 128
        text:
          type: string
          maxLength: 512
    PublicWithdrawalAddressee:
      type: object
      required:
        - name
        - lines
      properties:
        name:
          type: string
        lines:
          type: array
          items:
            type: string
        vatId:
          type: string
        email:
          type: string
          format: email
    RequestPublicWithdrawal:
      type: object
      required:
        - subscriptionNumber
        - customerName
        - email
        - orderedOn
      properties:
        subscriptionNumber:
          type: string
          pattern: '^SUB-\d{6}$'
        customerName:
          type: string
          maxLength: 255
        email:
          type: string
          format: email
        company:
          type: string
          maxLength: 255
        orderedOn:
          type: string
          format: date
        receivedOn:
          type: string
          format: date
    PublicWithdrawalRequestResponse:
      type: object
      required:
        - requestId
        - resumed
        - resumeStep
        - message
      properties:
        requestId:
          type: string
          format: uuid
        resumed:
          type: boolean
        resumeStep:
          type: string
          enum: [code, acknowledge]
        message:
          type: string
    VerifyPublicWithdrawalCode:
      type: object
      required:
        - requestId
        - code
      properties:
        requestId:
          type: string
          format: uuid
        code:
          type: string
          pattern: '^[A-Z0-9]{6}$'
    PublicWithdrawalVerifyCodeResponse:
      type: object
      required:
        - resumeStep
        - message
      properties:
        resumeStep:
          type: string
          enum: [acknowledge]
        message:
          type: string
    ConfirmPublicWithdrawal:
      type: object
      required:
        - requestId
        - acknowledgeWithdrawal
      properties:
        requestId:
          type: string
          format: uuid
        acknowledgeWithdrawal:
          type: boolean
          enum: [true]
    PublicWithdrawalConfirmResponse:
      type: object
      required:
        - message
      properties:
        message:
          type: string
    PublicServicePlanOffering:
      type: object
      required:
        - id
        - name
        - serviceTypeId
        - serviceTypeName
        - billingIntervalType
        - billingIntervalValue
        - billInAdvance
        - autoRecalculatePriceDaily
        - totalPrice
        - totalGross
        - taxRate
        - orderingHighlights
        - allowCustomerLocationSelection
        - allowCustomerServerTypeSelection
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
          nullable: true
        serviceTypeId:
          type: string
          format: uuid
          nullable: true
          description: >
            Service type UUID, or null when the plan has no service type
            and deploys nothing.
        serviceTypeName:
          type: string
          description: Display name of the service type (no config schema); empty when serviceTypeId is null
        billingIntervalType:
          type: string
          enum: [hour, day, month, year]
        billingIntervalValue:
          type: integer
          minimum: 1
        billInAdvance:
          type: boolean
          description: When true, the billing period is charged at period start (prepaid). When false, debt is created at period end (arrear). Usage-based metering is not available for advance-billed plans.
        autoRecalculatePriceDaily:
          type: boolean
          description: When true, the package may have its price recalculated nightly from the provider catalog. Checkout UIs must disclose this and the statutory withdrawal restart on change.
        totalPrice:
          type: number
          description: Customer-facing net total price (same formula as internal pricing preview; margin breakdown not exposed)
        totalGross:
          type: number
          description: Customer-facing gross total price (incl. VAT)
        taxRate:
          type: number
          description: Applied VAT rate in percent for this plan
        totalPriceFrom:
          type: number
          description: Lowest customer-facing net total across allowed server types when allowCustomerServerTypeSelection is true; omitted when not applicable.
        totalGrossFrom:
          type: number
          description: Lowest customer-facing gross total across allowed server types when allowCustomerServerTypeSelection is true; omitted when not applicable.
        orderingHighlights:
          type: array
          items:
            $ref: '#/components/schemas/ServicePlanOrderingHighlight'
        allowCustomerLocationSelection:
          type: boolean
          description: When true, authenticated checkout may send region or location in subscription requestedConfig (schema must define a string enum for that field on the service type).
        allowCustomerServerTypeSelection:
          type: boolean
          description: When true, authenticated checkout may send serverType in subscription requestedConfig; value must be in allowedServerTypes on the plan.
        withdrawalPolicy:
          $ref: '#/components/schemas/WithdrawalPolicy'
    CreateServicePlanDto:
      type: object
      required: [name, billingIntervalType, billingIntervalValue]
      properties:
        serviceTypeId:
          type: string
          format: uuid
          nullable: true
          description: >
            UUID of an existing service type, or null/omitted for a billing-only plan that deploys
            nothing (default in the admin UI).
        name:
          type: string
        description:
          type: string
        billingIntervalType:
          type: string
          enum: [hour, day, month, year]
        billingIntervalValue:
          type: integer
          minimum: 1
        billingDayOfMonth:
          type: integer
          minimum: 1
          maximum: 31
        cancelAtPeriodEnd:
          type: boolean
        billInAdvance:
          type: boolean
          description: When true, debt is created at period start. Default false (arrear). Incompatible with usage-based metering.
        autoRecalculatePriceDaily:
          type: boolean
          description: When true, nightly automatic price recalculation from the provider catalog is enabled. Default false (opt-in).
        minCommitmentDays:
          type: integer
        noticeDays:
          type: integer
        basePrice:
          type: string
          description: Decimal string
        marginPercent:
          type: string
          description: Decimal string
        marginFixed:
          type: string
          description: Decimal string
        providerConfigDefaults:
          type: object
          description: |
            Provider defaults merged with customer requestedConfig on order.
            Provisioning-enabled plans store customer-selectable stacks in provisioningOptions
            (integrated agenstra-controller/agenstra-manager/decabill-billing and/or custom CloudInit config ids).
            Legacy service and cloudInitConfigId keys are derived at persistence time only for single-option plans.
        orderingHighlights:
          type: array
          maxItems: 50
          items:
            $ref: '#/components/schemas/ServicePlanOrderingHighlight'
          description: Ordered icon/text lines shown with the plan in addition to description.
        isActive:
          type: boolean
        allowCustomerLocationSelection:
          type: boolean
          description: Requires service type schema with region or location string enum. Ignored when false or omitted.
        allowCustomerServerTypeSelection:
          type: boolean
          description: Requires provider schema with basePriceFromField serverType. When true, allowedServerTypes must list at least one server type id.
        allowedServerTypes:
          type: array
          items: { type: string }
          description: Server type ids customers may choose at checkout when allowCustomerServerTypeSelection is true.
        taxCategory:
          type: string
          enum: [standard, reduced]
          description: VAT category applied to automated subscription charges for this plan
    UpdateServicePlanDto:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        billingIntervalType: { type: string, enum: [hour, day, month, year] }
        billingIntervalValue: { type: integer, minimum: 1 }
        billingDayOfMonth: { type: integer, minimum: 1, maximum: 31 }
        cancelAtPeriodEnd: { type: boolean }
        billInAdvance:
          type: boolean
          description: When true, debt is created at period start. Default false (arrear). Incompatible with usage-based metering.
        autoRecalculatePriceDaily:
          type: boolean
          description: When true, nightly automatic price recalculation from the provider catalog is enabled. Default false (opt-in).
        migrateExistingSubscriptions:
          type: boolean
          description: >
            Request-only. When true and basePrice, marginPercent, marginFixed, or taxCategory change,
            enqueue migration of eligible subscriptions (settlement, withdrawal restart, price-change email).
            Default false. Not stored on the plan.
        minCommitmentDays: { type: integer }
        noticeDays: { type: integer }
        basePrice: { type: string }
        marginPercent: { type: string }
        marginFixed: { type: string }
        providerConfigDefaults: { type: object }
        orderingHighlights:
          type: array
          maxItems: 50
          items:
            $ref: '#/components/schemas/ServicePlanOrderingHighlight'
        isActive: { type: boolean }
        allowCustomerLocationSelection: { type: boolean }
        allowCustomerServerTypeSelection: { type: boolean }
        allowedServerTypes:
          type: array
          items: { type: string }
        taxCategory:
          type: string
          enum: [standard, reduced]
    ServicePlanResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        serviceTypeId:
          type: string
          format: uuid
          nullable: true
          description: Service type UUID, or null when the plan has no service type / no deployment
        name: { type: string }
        description: { type: string, nullable: true }
        billingIntervalType: { type: string, enum: [hour, day, month, year] }
        billingIntervalValue: { type: integer }
        billingDayOfMonth: { type: integer, nullable: true }
        cancelAtPeriodEnd: { type: boolean }
        billInAdvance:
          type: boolean
          description: When true, the period is billed at start (prepaid). Usage-based metering is not available.
        autoRecalculatePriceDaily:
          type: boolean
          description: When true, nightly automatic price recalculation from the provider catalog is enabled (opt-in; default false).
        minCommitmentDays: { type: integer }
        noticeDays: { type: integer }
        basePrice: { type: string, nullable: true }
        marginPercent: { type: string, nullable: true }
        marginFixed: { type: string, nullable: true }
        providerConfigDefaults: { type: object }
        orderingHighlights:
          type: array
          items:
            $ref: '#/components/schemas/ServicePlanOrderingHighlight'
          description: Ordered icon/text lines for display alongside description.
        allowCustomerLocationSelection:
          type: boolean
          description: When true, POST /subscriptions may include region or location in requestedConfig to override plan defaults (values must match service type schema enum).
        allowCustomerServerTypeSelection:
          type: boolean
          description: When true, POST /subscriptions may include serverType in requestedConfig; value must be listed in allowedServerTypes.
        allowedServerTypes:
          type: array
          items: { type: string }
          description: Server type ids available for customer selection when allowCustomerServerTypeSelection is true.
        taxCategory:
          type: string
          enum: [standard, reduced]
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        withdrawalPolicy:
          $ref: '#/components/schemas/WithdrawalPolicy'
        meters:
          type: array
          items:
            $ref: '#/components/schemas/AttachedMeterResponse'
    AvailabilityCheckDto:
      type: object
      required: [serviceTypeId, region, serverType]
      properties:
        serviceTypeId:
          type: string
          format: uuid
        region:
          type: string
        serverType:
          type: string
        requestedConfig:
          type: object
    AvailabilityResponse:
      type: object
      properties:
        isAvailable: { type: boolean }
        reason: { type: string }
        alternatives: { type: object }
    RequestedConfig:
      type: object
      description: |
        Optional cloud-init and provisioning config merged with plan defaults.
        May also include provider-specific fields (e.g. serverType, location, firewallId for Hetzner).
        Region or location overrides are accepted only when the plan has allowCustomerLocationSelection and the service type schema defines that field with an enum; otherwise they are ignored server-side.
        serverType overrides are accepted only when the plan has allowCustomerServerTypeSelection and the value is listed in allowedServerTypes; otherwise serverType is stripped from requestedConfig before merge.
        The FQDN (hostname.DNS_BASE_DOMAIN) is used for SSL certificates and CORS; DNS_BASE_DOMAIN
        defaults to spirde.com when not set.
        For service agenstra-manager, disableSignup and provisioning tokens are ignored; authenticationMethod must be api-key or keycloak (not users).
      additionalProperties: true
      properties:
        provisioningOptionKey:
          type: string
          description: |
            Customer choice when the plan offers multiple provisioningOptions entries
            (for example integrated:agenstra-controller or custom:<cloud-init-config-uuid>).
            Required when the plan has more than one option; omitted when the plan has exactly one option.
        service:
          type: string
          enum:
            [agenstra-controller, agenstra-manager, decabill-billing, custom]
          description: Product service kind. Use agenstra-controller for the full Agenstra stack, agenstra-manager for the agent manager only, decabill-billing for the Decabill billing stack, or custom for admin-defined Docker templates.
        env:
          type: object
          additionalProperties: { type: string }
          description: Customer-provided environment variables for custom service plans. Merged with admin defaults server-side.
        region:
          type: string
          description: DigitalOcean-style region; use the key matching the service type schema (region or location).
        location:
          type: string
          description: Hetzner-style location; use the key matching the service type schema (region or location).
        authenticationMethod:
          type: string
          description: Authentication method (users, api-key, keycloak). Default users.
        staticApiKey:
          type: string
          description: Static API key for api-key authentication. Required when authenticationMethod is api-key.
        disableSignup:
          type: boolean
          description: Whether to disable user signup. Default false.
        smtp:
          type: object
          description: SMTP configuration for email.
          properties:
            host: { type: string, description: 'SMTP host (default mailhog)' }
            port: { type: number, description: 'SMTP port (default 1025)' }
            user: { type: string }
            password: { type: string }
            from:
              {
                type: string,
                description: 'From address (default noreply@localhost)',
              }
        keycloak:
          type: object
          description: Keycloak config when authenticationMethod is keycloak.
          properties:
            serverUrl: { type: string }
            authServerUrl: { type: string }
            realm: { type: string }
            clientId: { type: string }
            clientSecret: { type: string }
        hetznerApiToken:
          type: string
          description: Optional Hetzner API token for nested provisioning from the instance. Sensitive.
        digitaloceanApiToken:
          type: string
          description: Optional DigitalOcean API token for nested provisioning from the instance. Sensitive.
        git:
          type: object
          description: Optional Git configuration for manager instances (GIT_* env vars).
          properties:
            repositoryUrl: { type: string, description: 'Git repository URL' }
            username: { type: string, description: 'Git username (HTTPS)' }
            token: { type: string, description: 'Git token (e.g. PAT)' }
            password:
              {
                type: string,
                description: 'Git password (alternative to token)',
              }
            privateKey:
              { type: string, description: 'SSH private key for git@ URLs' }
            commitAuthorName:
              { type: string, description: 'Default commit author name' }
            commitAuthorEmail:
              { type: string, description: 'Default commit author email' }
        cursorApiKey:
          type: string
          description: Optional Cursor API key for manager instances (CURSOR_API_KEY env var). Sensitive.
    CreateSubscriptionDto:
      type: object
      required: [planId]
      properties:
        planId: { type: string, format: uuid }
        requestedConfig:
          $ref: '#/components/schemas/RequestedConfig'
        addonIds:
          type: array
          description: Selected plan addon IDs (must be subset of plan allowedAddonIds; provider must support addons)
          items: { type: string, format: uuid }
        addonConfigs:
          type: object
          description: Per-addon customer config keyed by addon UUID (merged with admin defaults and random fills)
          additionalProperties:
            type: object
            additionalProperties: { type: string }
        preferredAlternatives: { type: object }
        autoBackorder: { type: boolean }
        promotionCode: { type: string }
        promotionBenefitStartsAt: { type: string, format: date-time }
    SubscriptionResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        number: { type: string }
        planId: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        status:
          {
            type: string,
            enum:
              [
                active,
                pending_backorder,
                pending_cancel,
                pending_withdrawal,
                pending_instant_cancel,
                pending_config_change,
                canceled,
              ],
          }
        currentPeriodStart: { type: string, format: date-time, nullable: true }
        currentPeriodEnd: { type: string, format: date-time, nullable: true }
        nextBillingAt: { type: string, format: date-time, nullable: true }
        cancelRequestedAt: { type: string, format: date-time, nullable: true }
        cancelEffectiveAt: { type: string, format: date-time, nullable: true }
        resumedAt: { type: string, format: date-time, nullable: true }
        withdrawnAt: { type: string, format: date-time, nullable: true }
        instantRemoval: { type: boolean }
        instantCanceledAt: { type: string, format: date-time, nullable: true }
        withdrawalEligibility:
          $ref: '#/components/schemas/WithdrawalEligibility'
        withdrawalResult:
          $ref: '#/components/schemas/WithdrawalResult'
        periodTotalPrice:
          type: number
          description: Total gross price charged per billing period for this subscription
        meters:
          type: array
          description: Plan- and addon-scoped meter summaries for the current billing period
          items:
            $ref: '#/components/schemas/SubscriptionMeterSummary'
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    WithdrawSubscriptionDto:
      type: object
      properties:
        reason:
          type: string
          maxLength: 500
    InstantCancelSubscriptionDto:
      type: object
      properties:
        reason:
          type: string
          maxLength: 500
    WithdrawalPolicy:
      type: object
      required:
        - periodDays
        - allowedAfterProvisioning
        - unprovisionedAlwaysWithdrawable
        - provisionedRefundPolicy
      properties:
        periodDays:
          type: integer
          description: Statutory withdrawal window in days after provisioning (from BILLING_STATUTORY_WITHDRAWAL_PERIOD_DAYS).
        allowedAfterProvisioning:
          type: boolean
          description: False when the service type disallows post-provisioning withdrawal.
        unprovisionedAlwaysWithdrawable:
          type: boolean
          enum: [true]
        provisionedRefundPolicy:
          type: string
          enum: [unused_period_prorated]
    WithdrawalEligibility:
      type: object
      properties:
        canWithdraw: { type: boolean }
        phase:
          type: string
          description: unprovisioned, withdrawal_period, expired, or excluded_by_service_type
        deadline: { type: string, format: date-time, nullable: true }
        reason: { type: string, nullable: true }
        estimatedRefundGross: { type: number, nullable: true }
    WithdrawalResult:
      type: object
      properties:
        refundNet: { type: number, nullable: true }
        refundGross: { type: number, nullable: true }
        creditNoteNumber: { type: string, nullable: true }
        paymentRefundStatus:
          type: string
          enum: [not_applicable, pending, succeeded, failed]
    CancelSubscriptionDto:
      type: object
      properties:
        reason: { type: string }
    ResumeSubscriptionDto:
      type: object
      properties:
        reason: { type: string }
    ConfigChangeRequestDto:
      type: object
      description: |
        Mid-life configuration change for an active subscription. Location is intentionally not
        changeable: moving a provisioned server across regions is a re-provision, not an in-place change.
      properties:
        serverType:
          type: string
          description: Target provider server type; must be offered by the plan.
        addAddonIds:
          type: array
          items: { type: string, format: uuid }
        removeAddonIds:
          type: array
          items: { type: string, format: uuid }
        addonConfigs:
          type: object
          description: Per-addon config keyed by addon UUID; only allowed for addons being added.
          additionalProperties:
            type: object
            additionalProperties: { type: string }
    ConfigChangeEligibility:
      type: object
      required:
        - canRequestChange
        - hasPendingChange
        - allowedServerTypes
        - supportsServerTypeUpgrade
        - supportsServerTypeDowngrade
        - availableAddonIds
        - activeAddonIds
      properties:
        canRequestChange: { type: boolean }
        reasonCode:
          type: string
          nullable: true
          enum:
            - CONFIG_CHANGE_NOT_ELIGIBLE
            - CONFIG_CHANGE_NOOP
            - CONFIG_CHANGE_SERVER_TYPE_UNSUPPORTED
            - CONFIG_CHANGE_SERVER_TYPE_LATERAL_UNSUPPORTED
            - CONFIG_CHANGE_ADDON_INVALID
            - CONFIG_CHANGE_ADDON_CONFIG_IMMUTABLE
            - CONFIG_CHANGE_FAILED
        reason: { type: string, nullable: true }
        hasPendingChange: { type: boolean }
        currentServerType: { type: string, nullable: true }
        allowedServerTypes:
          type: array
          items: { type: string }
        supportsServerTypeUpgrade: { type: boolean }
        supportsServerTypeDowngrade: { type: boolean }
        availableAddonIds:
          type: array
          items: { type: string, format: uuid }
        activeAddonIds:
          type: array
          items: { type: string, format: uuid }
    ConfigChangeAmounts:
      type: object
      properties:
        currency: { type: string }
        currentPeriodNet: { type: number }
        newPeriodNet: { type: number }
        periodDeltaNet: { type: number }
        immediateAdjustmentNet:
          type: number
          description: |
            Prorated net settled with the change; positive is charged, negative is credited.
            Prepaid with an unbilled recurring period charge uses `-periodDeltaNet × elapsed`;
            prepaid with the period already invoiced uses `periodDeltaNet × remaining`.
            Post-usage uses the elapsed share of the old period net. End state matches a new
            booking (`old×elapsed + new×remaining`).
        remainingPeriodRatio:
          type: number
          description: Share of the current billing period still unconsumed, in [0, 1].
    ConfigChangeDisclaimer:
      type: object
      properties:
        kind: { type: string, enum: [charge, credit, none] }
        effectiveAt: { type: string, format: date-time }
        notes:
          type: array
          items: { type: string }
          description: |
            Human-readable settlement notes (e.g. next-invoice charge/credit, or pending period
            reprice with elapsed correction). Not a cash refund promise.
    ConfigChangeDiscount:
      type: object
      properties:
        redemptionId: { type: string, format: uuid }
        code: { type: string }
        advantageType: { type: string }
        remainingBillingPeriods: { type: integer, nullable: true }
        remainingAmountNet: { type: number, nullable: true }
    ConfigChangePreviewResponse:
      type: object
      properties:
        eligibility: { $ref: '#/components/schemas/ConfigChangeEligibility' }
        amounts: { $ref: '#/components/schemas/ConfigChangeAmounts' }
        disclaimer: { $ref: '#/components/schemas/ConfigChangeDisclaimer' }
        discounts:
          type: array
          items: { $ref: '#/components/schemas/ConfigChangeDiscount' }
    ConfigChangeResponse:
      type: object
      description: Status view of a config change run; never contains the requested payload.
      properties:
        id: { type: string, format: uuid }
        status: { type: string, enum: [pending, processing, completed, failed] }
        errorCode: { type: string, nullable: true }
        errorMessage: { type: string, nullable: true }
        appliedSteps:
          type: array
          items: { type: string }
        billingOutcome:
          type: string
          nullable: true
          enum: [none, charged, credited, deferred]
          description: |
            One-shot settlement result. `credited` may be an open-position correction (next invoice)
            or a partial credit document (PDF/eInvoice; DATEV on later export) when the prepaid
            period charge was already invoiced.
        requestedAt: { type: string, format: date-time }
        processedAt: { type: string, format: date-time, nullable: true }
    SubscriptionItemResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        subscriptionId: { type: string, format: uuid }
        serviceTypeId:
          type: string
          format: uuid
          nullable: true
          description: Service type UUID, or null when the parent plan has no service type
        serviceTypeName:
          {
            type: string,
            description: User-facing service type name from the catalog (billing_service_types.name); empty when serviceTypeId is null,
          }
        provisioningStatus: { type: string, enum: [pending, active, failed] }
        hostname:
          {
            type: string,
            nullable: true,
            description: Single-level subdomain when provisioned (e.g. awesome-armadillo-abc12),
          }
        service:
          {
            type: string,
            enum:
              [agenstra-controller, agenstra-manager, decabill-billing, custom],
            description: Product service from config snapshot; agenstra-controller (full stack),
            agenstra-manager (agent manager only),
            decabill-billing (Decabill billing stack),
            or custom Docker template. Defaults to agenstra-controller.,
          }
        sshAccessGranted:
          {
            type: boolean,
            description: True after the customer has revealed the provisioning SSH private key at least once,
          }
    SubscriptionSshAccessKeyResponse:
      type: object
      required: [privateKey]
      properties:
        privateKey:
          {
            type: string,
            description: OpenSSH private key for root access. Returned only once per item. Never log this value.,
          }
    ServerInfoResponse:
      type: object
      description: Server info for display (status, IPs, etc.). Provider-specific IDs are not exposed.
      properties:
        name: { type: string, description: Server name in provider console }
        publicIp: { type: string, description: Public IP address }
        privateIp:
          {
            type: string,
            nullable: true,
            description: Private IP if applicable,
          }
        status: { type: string, description: e.g. running, starting, stopped }
        metadata:
          {
            type: object,
            nullable: true,
            description: Provider-specific metadata (e.g. location,
            locationName,
            region,
            regionName,
            datacenter,
            provider),
          }
        hostname:
          {
            type: string,
            nullable: true,
            description: Single-level subdomain when DNS is provisioned,
          }
        hostnameFqdn:
          {
            type: string,
            nullable: true,
            description: Full DNS name for display (e.g. awesome-armadillo-abc12.spirde.com),
          }
    ServerActionResponse:
      type: object
      properties:
        success:
          { type: boolean, description: True when the action was accepted }
    BackorderResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        serviceTypeId: { type: string, format: uuid }
        planId: { type: string, format: uuid }
        status:
          {
            type: string,
            enum: [pending, retrying, fulfilled, cancelled, failed],
          }
        failureReason: { type: string, nullable: true }
        requestedConfigSnapshot: { type: object }
        providerErrors: { type: object }
        preferredAlternatives: { type: object }
        retryAfter: { type: string, format: date-time, nullable: true }
        periodTotalPrice:
          type: number
          description: Total gross price charged per billing period for this backorder
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    BackorderRetryDto:
      type: object
      properties:
        reason: { type: string }
        overrideConfig: { type: object }
    BackorderCancelDto:
      type: object
      properties:
        reason: { type: string }
    PricingPreviewDto:
      type: object
      properties:
        planId: { type: string, format: uuid }
        requestedConfig: { type: object }
        addonIds:
          type: array
          items: { type: string, format: uuid }
    PricingPreviewResponse:
      type: object
      properties:
        basePrice: { type: number }
        marginPercent: { type: number }
        marginFixed: { type: number }
        totalPrice:
          {
            type: number,
            description: Plan net price per billing period (excl. VAT),
          }
        addonLines:
          type: array
          items:
            type: object
            properties:
              addonId: { type: string, format: uuid }
              name: { type: string }
              periodPrice: { type: number }
        addonsTotal: { type: number, description: Sum of addon period prices }
        grandTotal:
          {
            type: number,
            description: Plan totalPrice plus addonsTotal (excl. VAT base for tax enrichment),
          }
        taxTotal:
          { type: number, description: VAT amount for the billing period }
        totalGross:
          {
            type: number,
            description: Gross price per billing period (incl. VAT),
          }
        taxRate: { type: number, description: Applied VAT rate in percent }
        taxCategory:
          {
            type: string,
            enum: [standard, reduced],
            description: Plan VAT category used for this preview,
          }
    AddonResponse:
      type: object
      required:
        [
          id,
          key,
          name,
          implementationType,
          configSchema,
          compatibleProviders,
          isActive,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        implementationType: { type: string, enum: [module, cloud_init_script] }
        moduleKey: { type: string, nullable: true }
        scriptTemplate: { type: string, nullable: true }
        deprovisionScriptTemplate: { type: string, nullable: true }
        configSchema: { type: object }
        defaultValues: { type: object, additionalProperties: { type: string } }
        compatibleProviders:
          type: array
          items: { type: string }
        basePrice: { type: string, nullable: true }
        priceIntervalType:
          { type: string, enum: [hour, day, month, year], nullable: true }
        priceIntervalValue: { type: integer, nullable: true }
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        meters:
          type: array
          items:
            $ref: '#/components/schemas/AttachedMeterResponse'
    PlanAddonOption:
      type: object
      required:
        [id, key, name, implementationType, periodPrice, orderFields, meters]
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        implementationType: { type: string, enum: [module, cloud_init_script] }
        basePrice: { type: string, nullable: true }
        priceIntervalType:
          { type: string, enum: [hour, day, month, year], nullable: true }
        priceIntervalValue: { type: integer, nullable: true }
        periodPrice: { type: number }
        orderFields:
          type: array
          items:
            $ref: '#/components/schemas/CloudInitConfigOrderField'
        meters:
          type: array
          items:
            $ref: '#/components/schemas/AttachedMeterResponse'
    CreateAddonDto:
      type: object
      required: [key, name, implementationType]
      properties:
        key: { type: string }
        name: { type: string }
        description: { type: string }
        implementationType: { type: string, enum: [module, cloud_init_script] }
        moduleKey: { type: string }
        scriptTemplate: { type: string }
        deprovisionScriptTemplate: { type: string }
        configSchema: { type: object }
        defaultValues: { type: object, additionalProperties: { type: string } }
        compatibleProviders:
          type: array
          items: { type: string }
        basePrice: { type: string }
        priceIntervalType: { type: string, enum: [hour, day, month, year] }
        priceIntervalValue: { type: integer, minimum: 1 }
        isActive: { type: boolean }
    UpdateAddonDto:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        implementationType: { type: string, enum: [module, cloud_init_script] }
        moduleKey: { type: string, nullable: true }
        scriptTemplate: { type: string, nullable: true }
        deprovisionScriptTemplate: { type: string, nullable: true }
        configSchema: { type: object }
        defaultValues: { type: object, additionalProperties: { type: string } }
        compatibleProviders:
          type: array
          items: { type: string }
        basePrice: { type: string, nullable: true }
        priceIntervalType:
          { type: string, enum: [hour, day, month, year], nullable: true }
        priceIntervalValue: { type: integer, nullable: true }
        isActive: { type: boolean }
    TaxPreviewRequest:
      type: object
      properties:
        userId:
          {
            type: string,
            format: uuid,
            description: Customer user id (admin). When omitted,
            issuer domestic rates apply.,
          }
        lineItems:
          type: array
          items:
            type: object
            required: [quantity, unitPriceNet]
            properties:
              description: { type: string }
              quantity: { type: number }
              unitPriceNet: { type: number }
              taxCategory: { type: string, enum: [standard, reduced] }
    TaxPreviewResponse:
      type: object
      required:
        [taxMode, taxCountryCode, chargeVat, rates, einvoiceTaxCategoryCode]
      properties:
        taxMode: { type: string }
        taxCountryCode: { type: string }
        chargeVat: { type: boolean }
        taxNote: { type: string, nullable: true }
        einvoiceTaxCategoryCode: { type: string }
        rates:
          type: object
          required: [standard, reduced]
          properties:
            standard: { type: number }
            reduced: { type: number }
        subtotalNet: { type: number }
        taxTotal: { type: number }
        totalGross: { type: number }
        lines:
          type: array
          items:
            type: object
            properties:
              description: { type: string }
              quantity: { type: number }
              unitPriceNet: { type: number }
              taxCategory: { type: string, enum: [standard, reduced] }
              taxRate: { type: number }
              lineNet: { type: number }
              lineTax: { type: number }
              lineGross: { type: number }
    CustomerProfileDto:
      type: object
      properties:
        firstName: { type: string }
        lastName: { type: string }
        company: { type: string }
        addressLine1: { type: string }
        addressLine2: { type: string }
        postalCode: { type: string }
        city: { type: string }
        state: { type: string }
        country: { type: string, minLength: 2, maxLength: 2 }
        email: { type: string, format: email }
        phone: { type: string }
        customerType: { type: string, enum: [business, consumer] }
        vatId: { type: string, nullable: true, maxLength: 32 }
    CustomerProfileResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        firstName: { type: string, nullable: true }
        lastName: { type: string, nullable: true }
        company: { type: string, nullable: true }
        customerType:
          { type: string, enum: [business, consumer], nullable: true }
        vatId: { type: string, nullable: true }
        vatIdValidationStatus:
          { type: string, enum: [none, pending, valid, invalid, unavailable] }
        vatIdValidatedAt: { type: string, format: date-time, nullable: true }
        vatIdValidationSource:
          {
            type: string,
            enum: [vies_sync, vies_async, admin, format_only],
            nullable: true,
          }
        addressLine1: { type: string, nullable: true }
        addressLine2: { type: string, nullable: true }
        postalCode: { type: string, nullable: true }
        city: { type: string, nullable: true }
        state: { type: string, nullable: true }
        country: { type: string, nullable: true }
        email: { type: string, format: email, nullable: true }
        phone: { type: string, nullable: true }
        stripeCustomerId: { type: string, nullable: true }
        autoBillingEnabled: { type: boolean }
        hasPaymentMethodOnFile: { type: boolean }
        supportsAutoPayment: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AutoBillingSetupResponse:
      type: object
      required: [setupUrl]
      properties:
        setupUrl: { type: string, format: uri }
    CreateInvoiceDto:
      type: object
      properties:
        description: { type: string }
    InvoiceResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        subscriptionId: { type: string, format: uuid }
        invoiceNumber: { type: string, nullable: true }
        status: { type: string, nullable: true }
        balance: { type: number, nullable: true, description: Balance due }
        totalGross:
          { type: number, nullable: true, description: Invoice total (gross) }
        subscriptionNumber: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        dueDate: { type: string, format: date, nullable: true }
        canPay: { type: boolean }
        canDownload: { type: boolean }
        canPreview: { type: boolean }
        canDownloadVoidDocument: { type: boolean, nullable: true }
        canDownloadTimeReport: { type: boolean, nullable: true }
        voidDocumentNumber: { type: string, nullable: true }
        autoPaymentStatus:
          {
            type: string,
            enum:
              [
                idle,
                scheduled,
                in_progress,
                retrying,
                succeeded,
                exhausted,
                canceled,
              ],
            nullable: true,
          }
    InvoiceDetailResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        subscriptionId: { type: string, format: uuid }
        invoiceNumber: { type: string, nullable: true }
        status: { type: string }
        currency: { type: string }
        subtotalNet: { type: number }
        taxTotal: { type: number }
        totalGross: { type: number }
        balanceDue: { type: number }
        lineItems:
          type: array
          items:
            $ref: '#/components/schemas/InvoiceLineItemResponse'
        taxBreakdown:
          type: array
          items:
            $ref: '#/components/schemas/InvoiceTaxBreakdown'
        issuedAt: { type: string, format: date-time, nullable: true }
        dueDate: { type: string, format: date, nullable: true }
        createdAt: { type: string, format: date-time }
        canPay: { type: boolean }
        canDownload: { type: boolean }
        canPreview: { type: boolean }
        canDownloadVoidDocument: { type: boolean, nullable: true }
        canDownloadTimeReport: { type: boolean, nullable: true }
        voidDocumentNumber: { type: string, nullable: true }
        autoPaymentStatus:
          {
            type: string,
            enum:
              [
                idle,
                scheduled,
                in_progress,
                retrying,
                succeeded,
                exhausted,
                canceled,
              ],
            nullable: true,
          }
    InvoiceLineItemResponse:
      type: object
      properties:
        description: { type: string }
        quantity: { type: number }
        unitPriceNet: { type: number }
        taxCategory: { type: string, enum: [standard, reduced] }
        taxRate: { type: number }
        lineNet: { type: number }
        lineTax: { type: number }
        lineGross: { type: number }
    InvoiceTaxBreakdown:
      type: object
      properties:
        taxCategory: { type: string, enum: [standard, reduced] }
        taxRate: { type: number }
        taxAmount: { type: number }
    CreateInvoiceResponse:
      type: object
      properties:
        invoiceRefId: { type: string, format: uuid }
        invoiceNumber: { type: string, nullable: true }
    InitiatePaymentResponse:
      type: object
      properties:
        checkoutUrl: { type: string, format: uri }
    InvoicesSummaryResponse:
      type: object
      description: Count and total balance of open and overdue invoices for the current user, plus the user's billing day.
      properties:
        openOverdueCount:
          type: integer
          minimum: 0
          description: Number of open and overdue invoices
        openOverdueTotal:
          type: number
          minimum: 0
          description: Sum of balances due for those invoices
        billingDayOfMonth:
          type: integer
          minimum: 1
          maximum: 28
          description: Day of month (1-28) when the user is billed for open positions (default from registration day, capped at 28)
        unbilledTotal:
          type: number
          minimum: 0
          description: Total amount of unbilled open positions (to be invoiced on the next billing day)
        minCheckoutPaymentAmount:
          type: number
          exclusiveMinimum: 0
          description: Minimum balance due required for Checkout or saved payment method charges
      required:
        - openOverdueCount
        - openOverdueTotal
        - billingDayOfMonth
        - unbilledTotal
        - minCheckoutPaymentAmount
    UsageSummary:
      type: object
      properties:
        subscriptionId: { type: string, format: uuid }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        usagePayload: { type: object }
    CreateUsageRecordDto:
      type: object
      required: [subscriptionId, periodStart, periodEnd]
      properties:
        subscriptionId: { type: string, format: uuid }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        usagePayload:
          type: object
          description: Legacy payload cost fields when the subscription has no meter attachments
        meterId:
          type: string
          format: uuid
          description: Required when the subscription has plan or addon meter attachments
        value:
          type: number
          description: Metered value; required when meterId is set
        attachmentType:
          type: string
          enum: [plan, addon]
          default: plan
        addonId:
          type: string
          format: uuid
          description: Required when attachmentType is addon
    UpdateUsageMeterEntryDto:
      type: object
      properties:
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        value: { type: number, minimum: 0 }
        usagePayload: { type: object }
        attachmentType: { type: string, enum: [plan, addon] }
        meterId: { type: string, format: uuid }
        addonId: { type: string, format: uuid, nullable: true }
    CreateSubscriptionMeterEntryDto:
      type: object
      required: [periodStart, periodEnd]
      properties:
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        usagePayload: { type: object }
        meterId: { type: string, format: uuid }
        value: { type: number }
        attachmentType: { type: string, enum: [plan, addon], default: plan }
        addonId: { type: string, format: uuid }
    MeterResponse:
      type: object
      required:
        [
          id,
          key,
          name,
          aggregator,
          defaultUnitPriceNet,
          isActive,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        unitLabel: { type: string, nullable: true }
        aggregator:
          {
            type: string,
            enum: [max, min, avg, first, last, sum, sum_positive_deltas],
          }
        defaultUnitPriceNet: { type: number }
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateMeterDto:
      type: object
      required: [key, name, aggregator, defaultUnitPriceNet]
      properties:
        key: { type: string }
        name: { type: string }
        description: { type: string }
        unitLabel: { type: string }
        aggregator:
          {
            type: string,
            enum: [max, min, avg, first, last, sum, sum_positive_deltas],
          }
        defaultUnitPriceNet: { type: number }
        isActive: { type: boolean }
    UpdateMeterDto:
      type: object
      properties:
        name: { type: string }
        description: { type: string, nullable: true }
        unitLabel: { type: string, nullable: true }
        aggregator:
          {
            type: string,
            enum: [max, min, avg, first, last, sum, sum_positive_deltas],
          }
        defaultUnitPriceNet: { type: number }
        isActive: { type: boolean }
    AttachMeterDto:
      type: object
      required: [meterId]
      properties:
        meterId: { type: string, format: uuid }
        unitPriceNet: { type: number, nullable: true }
    UpdateAttachedMeterDto:
      type: object
      properties:
        unitPriceNet: { type: number, nullable: true }
    AttachedMeterResponse:
      type: object
      required:
        [
          meterId,
          key,
          name,
          aggregator,
          defaultUnitPriceNet,
          effectiveUnitPriceNet,
          isActive,
          source,
          required,
        ]
      properties:
        meterId: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        description: { type: string, nullable: true }
        unitLabel: { type: string, nullable: true }
        aggregator:
          {
            type: string,
            enum: [max, min, avg, first, last, sum, sum_positive_deltas],
          }
        defaultUnitPriceNet: { type: number }
        unitPriceNetOverride: { type: number, nullable: true }
        effectiveUnitPriceNet: { type: number }
        isActive: { type: boolean }
        source:
          type: string
          enum: [manual, module, provider]
          description: How the attachment was created.
        required:
          type: boolean
          description: When true, detach is rejected.
        inherited:
          type: boolean
          description: |
            True when listed on a plan but sourced from the plan's service type
            (not a direct plan attachment).
    SubscriptionMeterSummary:
      type: object
      required:
        [
          meterId,
          key,
          name,
          aggregator,
          attachmentType,
          effectiveUnitPriceNet,
          aggregatedValue,
          estimatedChargeNet,
          entryCount,
        ]
      properties:
        meterId: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        unitLabel: { type: string, nullable: true }
        aggregator:
          {
            type: string,
            enum: [max, min, avg, first, last, sum, sum_positive_deltas],
          }
        attachmentType: { type: string, enum: [plan, addon] }
        addonId: { type: string, format: uuid, nullable: true }
        addonName: { type: string, nullable: true }
        effectiveUnitPriceNet: { type: number }
        aggregatedValue: { type: number }
        estimatedChargeNet: { type: number }
        entryCount: { type: integer }
        periodStart: { type: string, format: date-time, nullable: true }
        periodEnd: { type: string, format: date-time, nullable: true }
    UsageMeterEntryResponse:
      type: object
      required:
        [
          id,
          subscriptionId,
          meterId,
          value,
          attachmentType,
          periodStart,
          periodEnd,
          usageSource,
          usagePayload,
          createdAt,
        ]
      properties:
        id: { type: string, format: uuid }
        subscriptionId: { type: string, format: uuid }
        meterId: { type: string, format: uuid }
        value: { type: number }
        attachmentType: { type: string, enum: [plan, addon] }
        addonId: { type: string, format: uuid, nullable: true }
        periodStart: { type: string, format: date-time }
        periodEnd: { type: string, format: date-time }
        usageSource: { type: string }
        usagePayload: { type: object }
        createdAt: { type: string, format: date-time }
    UsageRecordResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
    AdminBillingSummaryResponse:
      type: object
      properties:
        activeSubscriptionsCount: { type: integer, minimum: 0 }
        openOverdueCount: { type: integer, minimum: 0 }
        openOverdueTotal: { type: number, minimum: 0 }
        unbilledTotal: { type: number, minimum: 0 }
    AdminBillNowDto:
      type: object
      properties:
        userId:
          {
            type: string,
            format: uuid,
            description: Omit to bill all users with unbilled positions,
          }
    AdminBillNowError:
      type: object
      properties:
        userId: { type: string, format: uuid }
        message: { type: string }
    AdminBillNowResponse:
      type: object
      required: [queued, requestId, userCount]
      properties:
        queued: { type: boolean }
        requestId: { type: string, format: uuid }
        userCount: { type: integer }
    BillingCapabilitiesResponse:
      type: object
      required: [datevExportEnabled, unifiedExportAllowed]
      properties:
        datevExportEnabled:
          type: boolean
          description: Mirrors BILLING_DATEV_EXPORT_ENABLED
        unifiedExportAllowed:
          type: boolean
          description: True when unified export is enabled and request tenant is in BILLING_DATEV_UNIFIED_EXPORT_ALLOWED_TENANTS
    DatevExportScope:
      type: string
      enum: [tenant, unified]
    DatevExportStatus:
      type: string
      enum: [pending, running, completed, failed]
    AdminDatevExportListItem:
      type: object
      required:
        - id
        - scope
        - tenantId
        - periodYear
        - periodMonth
        - status
        - bookingCount
        - invoiceCount
        - debtorCount
        - createdAt
      properties:
        id: { type: string, format: uuid }
        scope: { $ref: '#/components/schemas/DatevExportScope' }
        tenantId: { type: string }
        periodYear: { type: integer }
        periodMonth: { type: integer, minimum: 1, maximum: 12 }
        status: { $ref: '#/components/schemas/DatevExportStatus' }
        fileName: { type: string, nullable: true }
        bookingCount: { type: integer }
        invoiceCount: { type: integer }
        debtorCount: { type: integer }
        includedTenantIds:
          type: array
          items: { type: string }
          nullable: true
        errorMessage: { type: string, nullable: true }
        triggeredBy: { type: string, nullable: true }
        startedAt: { type: string, format: date-time, nullable: true }
        completedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
    PaginatedAdminDatevExportsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminDatevExportListItem'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    TriggerDatevExportDto:
      type: object
      required: [year, month]
      properties:
        year: { type: integer }
        month: { type: integer, minimum: 1, maximum: 12 }
        scope: { $ref: '#/components/schemas/DatevExportScope' }
        force:
          {
            type: boolean,
            description: Re-export even when a completed export exists,
          }
    TriggerDatevExportResponse:
      type: object
      required: [queued, scope, year, month]
      properties:
        queued: { type: boolean }
        scope: { $ref: '#/components/schemas/DatevExportScope' }
        year: { type: integer }
        month: { type: integer }
    AdminInvoiceListItem:
      allOf:
        - $ref: '#/components/schemas/InvoiceResponse'
        - type: object
          properties:
            userId: { type: string, format: uuid }
            userEmail: { type: string, format: email, nullable: true }
    PaginatedAdminInvoicesResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminInvoiceListItem'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    AdminSubscriptionListItem:
      allOf:
        - $ref: '#/components/schemas/SubscriptionResponse'
        - type: object
          properties:
            userEmail: { type: string, format: email, nullable: true }
            planName: { type: string, nullable: true }
    PaginatedAdminSubscriptionsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminSubscriptionListItem'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    MarkInvoicePaymentStatusDto:
      type: object
      properties:
        reason: { type: string }
    BillingAuditLogResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        process: { type: string }
        level: { type: string }
        message: { type: string }
        invoiceId: { type: string, format: uuid, nullable: true }
        userId: { type: string, format: uuid, nullable: true }
        context: { type: object }
        createdAt: { type: string, format: date-time }
    PaginatedBillingAuditLogsResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BillingAuditLogResponse'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    BillingStatisticsSeriesPoint:
      type: object
      properties:
        period: { type: string }
        totalGross: { type: number }
    BillingStatisticsSummary:
      type: object
      properties:
        series:
          type: array
          items:
            $ref: '#/components/schemas/BillingStatisticsSeriesPoint'
        totalGross: { type: number }
        paidCount: { type: integer }
        from: { type: string, format: date }
        to: { type: string, format: date }
        groupBy: { type: string, enum: [day, month] }
    BillingStatisticsByProductItem:
      type: object
      properties:
        planId:
          {
            type: string,
            description: 'Plan UUID, project:<uuid>, or UNKNOWN when unassigned',
          }
        planName:
          {
            type: string,
            description: 'Service plan name, project name, or Unknown',
          }
        totalGross: { type: number }
    BillingStatisticsByProduct:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BillingStatisticsByProductItem'
        totalGross: { type: number }
        from: { type: string, format: date }
        to: { type: string, format: date }
    BillingStatisticsByCountryItem:
      type: object
      properties:
        countryCode: { type: string }
        countryName: { type: string }
        totalGross: { type: number }
    BillingStatisticsByCountry:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/BillingStatisticsByCountryItem'
        totalGross: { type: number }
        from: { type: string, format: date }
        to: { type: string, format: date }
    ManualInvoiceLineItemDto:
      type: object
      required: [description, quantity, unitPriceNet]
      properties:
        description: { type: string }
        quantity: { type: number }
        unitPriceNet: { type: number }
        taxCategory: { type: string, enum: [standard, reduced] }
    CreateManualInvoiceDto:
      type: object
      required: [userId, lineItems]
      properties:
        userId: { type: string, format: uuid }
        subscriptionId: { type: string, format: uuid, nullable: true }
        lineItems:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/ManualInvoiceLineItemDto'
        currency: { type: string }
    UpdateManualInvoiceDto:
      type: object
      required: [lineItems]
      properties:
        lineItems:
          type: array
          minItems: 1
          items:
            $ref: '#/components/schemas/ManualInvoiceLineItemDto'
    IssueManualInvoiceDto:
      type: object
      properties:
        dueInDays: { type: integer, minimum: 1 }
    ManualInvoiceDetailResponse:
      allOf:
        - $ref: '#/components/schemas/InvoiceDetailResponse'
        - type: object
          properties:
            userId: { type: string, format: uuid }
            userEmail: { type: string, format: email, nullable: true }
    AdminCustomerProfileListItem:
      type: object
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        userEmail: { type: string, format: email, nullable: true }
        firstName: { type: string, nullable: true }
        lastName: { type: string, nullable: true }
        company: { type: string, nullable: true }
        email: { type: string, format: email, nullable: true }
        country: { type: string, nullable: true }
        isComplete: { type: boolean }
        stripeCustomerId: { type: string, nullable: true }
        trustScore: { type: integer, nullable: true }
        trustLevel: { type: string, enum: [green, yellow, red], nullable: true }
        trustScoreUpdatedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PaginatedAdminCustomerProfilesResponse:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminCustomerProfileListItem'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    CreateAdminCustomerProfileDto:
      allOf:
        - $ref: '#/components/schemas/CustomerProfileDto'
        - type: object
          required: [userId]
          properties:
            userId: { type: string, format: uuid }
    AdminCustomerProfileDetailResponse:
      allOf:
        - $ref: '#/components/schemas/CustomerProfileResponse'
        - type: object
          properties:
            userEmail: { type: string, format: email, nullable: true }
            isComplete: { type: boolean }
            trustScore: { type: integer, nullable: true }
            trustLevel:
              { type: string, enum: [green, yellow, red], nullable: true }
            trustScoreUpdatedAt:
              { type: string, format: date-time, nullable: true }
            customData:
              type: object
              description: >
                Admin-only encrypted custom key/value map for external-system linkage.
                Never returned on customer self-service profile endpoints.
              additionalProperties:
                type: string
              default: {}
    AddCustomerProfileCustomDataDto:
      type: object
      required: [key, value]
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 64
          pattern: '^[a-zA-Z0-9._-]+$'
        value:
          type: string
          maxLength: 4096
    UpdateCustomerProfileCustomDataDto:
      type: object
      required: [value]
      properties:
        value:
          type: string
          maxLength: 4096
    CustomerTrustScoreFactor:
      type: object
      properties:
        id: { type: string }
        label: { type: string }
        description: { type: string }
        points: { type: integer }
        source: { type: string }
        metadata:
          type: object
          nullable: true
          additionalProperties: true
    CustomerTrustScoreResponse:
      type: object
      properties:
        profileId: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        score: { type: integer }
        level: { type: string, enum: [green, yellow, red] }
        baseScore: { type: integer }
        factors:
          type: array
          items:
            $ref: '#/components/schemas/CustomerTrustScoreFactor'
        computedAt: { type: string, format: date-time }
        sources:
          type: array
          items: { type: string }
    CloudInitConfigEnvVariableDefinition:
      type: object
      required: [key, label, showInOrderForm, hasDefault]
      properties:
        key: { type: string }
        label: { type: string }
        description: { type: string, nullable: true }
        showInOrderForm: { type: boolean }
        hasDefault:
          {
            type: boolean,
            description: Whether an admin default value exists (value not returned in list responses).,
          }
        useRandomDefault:
          {
            type: boolean,
            description: When true,
            generates a password-like value at order time instead of using a static default.,
          }
        randomDefaultLength:
          {
            type: integer,
            minimum: 21,
            description: Length for generated random defaults.,
          }
        randomDefaultSpecialChars:
          {
            type: boolean,
            description: Include special characters in generated random defaults.,
          }
    CloudInitConfigResponse:
      type: object
      properties:
        id: { type: string, format: uuid }
        key: { type: string }
        name: { type: string }
        provisioningMode:
          type: string
          enum: [simple, compose-template, user-data-template]
        description: { type: string, nullable: true }
        dockerImage: { type: string, nullable: true }
        containerPort: { type: integer }
        hostPort: { type: integer }
        workDir: { type: string }
        dockerComposeTemplate: { type: string, nullable: true }
        userDataTemplate: { type: string, nullable: true }
        environmentVariables:
          type: array
          items:
            $ref: '#/components/schemas/CloudInitConfigEnvVariableDefinition'
        defaultValues:
          type: object
          additionalProperties: { type: string }
          description: Decrypted admin defaults. Included on admin GET by id and create/update responses only.
        isActive: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CloudInitConfigOrderField:
      type: object
      required: [key, label, required, hasDefault]
      properties:
        key: { type: string }
        label: { type: string }
        description: { type: string, nullable: true }
        required:
          {
            type: boolean,
            description: True when the variable has no admin default and must be supplied at order time.,
          }
        hasDefault:
          {
            type: boolean,
            description: True when the admin configured a static or server-generated default. The value is not exposed to customers.,
          }
    CreateCloudInitConfigDto:
      type: object
      required: [key, name]
      properties:
        key: { type: string }
        name: { type: string }
        description: { type: string }
        provisioningMode:
          type: string
          enum: [simple, compose-template, user-data-template]
        dockerImage: { type: string }
        containerPort: { type: integer, minimum: 1, maximum: 65535 }
        hostPort: { type: integer, minimum: 1, maximum: 65535 }
        workDir: { type: string }
        dockerComposeTemplate: { type: string }
        userDataTemplate: { type: string }
        environmentVariables:
          type: array
          items:
            type: object
            required: [key, label, showInOrderForm]
            properties:
              key: { type: string }
              label: { type: string }
              description: { type: string }
              showInOrderForm: { type: boolean }
        defaultValues:
          type: object
          additionalProperties: { type: string }
        isActive: { type: boolean }
    ProjectStatus:
      type: string
      enum: [active, archived]
    ProjectTicketStatus:
      type: string
      enum: [draft, todo, in_progress, prototype, done, closed]
    ProjectTicketPriority:
      type: string
      enum: [low, medium, high, critical]
    ProjectResponse:
      type: object
      required:
        [
          id,
          userId,
          name,
          status,
          hourlyRateNet,
          currency,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        name: { type: string }
        description: { type: string, nullable: true }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        hourlyRateNet: { type: number }
        targetHours: { type: number, nullable: true }
        currency: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ProjectListItem:
      allOf:
        - $ref: '#/components/schemas/ProjectResponse'
        - type: object
          required: [unbilledMinutes, openBillableAmountNet]
          properties:
            unbilledMinutes: { type: integer, minimum: 0 }
            openBillableAmountNet: { type: number, minimum: 0 }
    PaginatedProjectsResponse:
      type: object
      required: [items, total, limit, offset]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/ProjectListItem' }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    AdminProjectListItem:
      allOf:
        - $ref: '#/components/schemas/ProjectListItem'
        - type: object
          properties:
            userEmail: { type: string }
    PaginatedAdminProjectsResponse:
      type: object
      required: [items, total, limit, offset]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AdminProjectListItem' }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    ProjectSummaryResponse:
      type: object
      required:
        [
          projectId,
          totalTrackedMinutes,
          unbilledMinutes,
          openBillableAmountNet,
          billedAmountNet,
          openTicketCount,
          doneTicketCount,
          milestoneCount,
          openMilestoneCount,
        ]
      properties:
        projectId: { type: string, format: uuid }
        totalTrackedMinutes: { type: integer }
        unbilledMinutes: { type: integer }
        openBillableAmountNet: { type: number }
        billedAmountNet: { type: number }
        openTicketCount: { type: integer }
        doneTicketCount: { type: integer }
        milestoneCount: { type: integer }
        openMilestoneCount: { type: integer }
    AdminProjectDetailResponse:
      allOf:
        - $ref: '#/components/schemas/AdminProjectListItem'
        - type: object
          properties:
            summary: { $ref: '#/components/schemas/ProjectSummaryResponse' }
    CreateAdminProjectDto:
      type: object
      required: [userId, name, hourlyRateNet]
      properties:
        userId: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        hourlyRateNet: { type: number }
        targetHours: { type: number, nullable: true }
        currency: { type: string }
    UpdateAdminProjectDto:
      type: object
      properties:
        userId: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        status: { $ref: '#/components/schemas/ProjectStatus' }
        hourlyRateNet: { type: number }
        targetHours: { type: number, nullable: true }
        currency: { type: string }
    BillProjectTimeResponse:
      type: object
      required: [invoiceId, billedMinutes, amountNet]
      properties:
        invoiceId: { type: string, format: uuid }
        invoiceNumber: { type: string }
        billedMinutes: { type: integer }
        amountNet: { type: number }
    BillProjectTimeRequest:
      type: object
      required: [from, to]
      properties:
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        subscriptionId: { type: string, format: uuid }
        lineItems:
          type: array
          items:
            $ref: '#/components/schemas/ManualInvoiceLineItemDto'
    ProjectUnbilledTimeBounds:
      type: object
      required: [entryCount]
      properties:
        from: { type: string, format: date-time, nullable: true }
        to: { type: string, format: date-time, nullable: true }
        entryCount: { type: integer, minimum: 0 }
    ProjectTimeReportRequest:
      type: object
      required: [from, to]
      properties:
        from: { type: string, format: date-time }
        to: { type: string, format: date-time }
        unbilledOnly: { type: boolean, default: false }
    ProjectTicketResponse:
      type: object
      required:
        [
          id,
          projectId,
          title,
          status,
          priority,
          shas,
          tasks,
          locked,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string, format: uuid }
        projectId: { type: string, format: uuid }
        parentId: { type: string, format: uuid, nullable: true }
        milestoneId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        content: { type: string, nullable: true }
        status: { $ref: '#/components/schemas/ProjectTicketStatus' }
        priority: { $ref: '#/components/schemas/ProjectTicketPriority' }
        shas:
          type: object
          properties:
            short: { type: string }
            long: { type: string }
        tasks:
          type: object
          properties:
            open: { type: integer }
            done: { type: integer }
            children:
              type: object
              properties:
                open: { type: integer }
                done: { type: integer }
        createdByUserId: { type: string, format: uuid, nullable: true }
        createdByEmail: { type: string }
        locked: { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        children:
          type: array
          items: { $ref: '#/components/schemas/ProjectTicketResponse' }
    CreateProjectTicketDto:
      type: object
      required: [title]
      properties:
        parentId: { type: string, format: uuid }
        milestoneId: { type: string, format: uuid }
        title: { type: string }
        content: { type: string }
        priority: { $ref: '#/components/schemas/ProjectTicketPriority' }
        status: { $ref: '#/components/schemas/ProjectTicketStatus' }
    UpdateProjectTicketDto:
      type: object
      properties:
        parentId: { type: string, format: uuid, nullable: true }
        milestoneId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        content: { type: string, nullable: true }
        priority: { $ref: '#/components/schemas/ProjectTicketPriority' }
        status: { $ref: '#/components/schemas/ProjectTicketStatus' }
        locked: { type: boolean }
    ProjectTicketCommentResponse:
      type: object
      required: [id, ticketId, userId, body, createdAt]
      properties:
        id: { type: string, format: uuid }
        ticketId: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        userEmail: { type: string }
        body: { type: string }
        createdAt: { type: string, format: date-time }
    CreateProjectTicketCommentDto:
      type: object
      required: [body]
      properties:
        body: { type: string }
    ProjectTicketActivityResponse:
      type: object
      required: [id, ticketId, occurredAt, actorType, actionType, payload]
      properties:
        id: { type: string, format: uuid }
        ticketId: { type: string, format: uuid }
        occurredAt: { type: string, format: date-time }
        actorType: { type: string }
        actorUserId: { type: string, format: uuid, nullable: true }
        actionType: { type: string }
        payload:
          type: object
          additionalProperties: true
    ProjectMilestoneResponse:
      type: object
      required:
        [
          id,
          projectId,
          name,
          sortOrder,
          progressPercent,
          openTicketCount,
          doneTicketCount,
          createdAt,
          updatedAt,
        ]
      properties:
        id: { type: string, format: uuid }
        projectId: { type: string, format: uuid }
        name: { type: string }
        description: { type: string, nullable: true }
        targetDate: { type: string, format: date, nullable: true }
        sortOrder: { type: integer }
        lockedAt: { type: string, format: date-time, nullable: true }
        progressPercent: { type: integer }
        openTicketCount: { type: integer }
        doneTicketCount: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    CreateProjectMilestoneDto:
      type: object
      required: [name]
      properties:
        name: { type: string }
        description: { type: string }
        targetDate: { type: string, format: date }
        sortOrder: { type: integer, minimum: 0 }
    UpdateProjectMilestoneDto:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        targetDate: { type: string, format: date, nullable: true }
        sortOrder: { type: integer, minimum: 0 }
    ProjectTimeEntryResponse:
      type: object
      required:
        [
          id,
          projectId,
          recordedByUserId,
          durationMinutes,
          startedAt,
          endedAt,
          recordedAt,
          createdAt,
        ]
      properties:
        id: { type: string, format: uuid }
        projectId: { type: string, format: uuid }
        ticketId: { type: string, format: uuid, nullable: true }
        recordedByUserId: { type: string, format: uuid }
        durationMinutes: { type: integer }
        description: { type: string, nullable: true }
        startedAt: { type: string, format: date-time }
        endedAt: { type: string, format: date-time }
        recordedAt:
          type: string
          format: date-time
          deprecated: true
          description: Deprecated; use startedAt
        invoiceId: { type: string, format: uuid, nullable: true }
        billedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
    CreateProjectTimeEntryDto:
      type: object
      required: [startedAt, endedAt]
      properties:
        ticketId: { type: string, format: uuid }
        description: { type: string, maxLength: 500 }
        startedAt: { type: string, format: date-time }
        endedAt: { type: string, format: date-time }
    UpdateProjectTimeEntryDto:
      type: object
      properties:
        ticketId: { type: string, format: uuid, nullable: true }
        description: { type: string, maxLength: 500, nullable: true }
        startedAt: { type: string, format: date-time }
        endedAt: { type: string, format: date-time }
    PaginatedProjectTimeEntriesResponse:
      type: object
      required: [items, total, limit, offset]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/ProjectTimeEntryResponse' }
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    UpdateCloudInitConfigDto:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        provisioningMode:
          type: string
          enum: [simple, compose-template, user-data-template]
        dockerImage: { type: string }
        containerPort: { type: integer, minimum: 1, maximum: 65535 }
        hostPort: { type: integer, minimum: 1, maximum: 65535 }
        workDir: { type: string }
        dockerComposeTemplate: { type: string, nullable: true }
        userDataTemplate: { type: string, nullable: true }
        environmentVariables:
          type: array
          items:
            $ref: '#/components/schemas/CloudInitConfigEnvVariableDefinition'
        defaultValues:
          type: object
          additionalProperties: { type: string }
        isActive: { type: boolean }
    ValidatePromotionDto:
      type: object
      required: [code, redemptionContext]
      properties:
        code: { type: string }
        redemptionContext: { type: string, enum: [new, existing] }
        subscriptionId: { type: string, format: uuid }
        planId: { type: string, format: uuid }
    RedeemPromotionDto:
      type: object
      required: [code, redemptionContext, subscriptionId]
      properties:
        code: { type: string }
        redemptionContext: { type: string, enum: [new, existing] }
        subscriptionId: { type: string, format: uuid }
        benefitStartsAt: { type: string, format: date-time }
    PromotionValidationResponseDto:
      type: object
      properties:
        valid: { type: boolean }
        message: { type: string }
        code: { type: string }
        promotionName: { type: string }
        advantageSummary: { type: string }
        amountNet: { type: number }
        currency: { type: string }
        days: { type: integer }
        periods: { type: integer }
        benefitStartsAt: { type: string, format: date-time }
        benefitEndsAt: { type: string, format: date-time, nullable: true }
        chargePeriodStart: { type: string, format: date-time }
        chargePeriodEnd: { type: string, format: date-time }
    PromotionRedemptionResponseDto:
      type: object
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        promotionName: { type: string }
        subscriptionId: { type: string, format: uuid }
        subscriptionNumber: { type: string }
        planName: { type: string }
        redemptionContext: { type: string, enum: [new, existing] }
        status: { type: string, enum: [active, exhausted, expired] }
        redeemedAt: { type: string, format: date-time }
        advantageType: { type: string }
        advantageSummary: { type: string }
        validFrom: { type: string, format: date-time }
        validTo: { type: string, format: date-time, nullable: true }
        remainingMonetaryNet: { type: number }
        remainingPeriods: { type: integer }
    PaginatedPromotionRedemptionsResponseDto:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PromotionRedemptionResponseDto'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    AdminPromotionResponseDto:
      type: object
      properties:
        id: { type: string, format: uuid }
        code: { type: string }
        name: { type: string }
        description: { type: string }
        redeemableFrom: { type: string, format: date-time }
        redeemableTo: { type: string, format: date-time }
        maxTotalRedemptions: { type: integer }
        maxPerUserRedemptions: { type: integer }
        isActive: { type: boolean }
        advantageType: { type: string }
        advantageConfig: { type: object, additionalProperties: true }
        applicablePlanIds:
          type: array
          items: { type: string, format: uuid }
        subscriptionEligibility: { type: string, enum: [new, existing, both] }
        redemptionCount: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PaginatedAdminPromotionsResponseDto:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AdminPromotionResponseDto'
        total: { type: integer }
        limit: { type: integer }
        offset: { type: integer }
    CreateAdminPromotionDto:
      type: object
      required:
        [
          code,
          name,
          redeemableFrom,
          redeemableTo,
          maxPerUserRedemptions,
          isActive,
          advantageType,
          advantageConfig,
          subscriptionEligibility,
        ]
      properties:
        code: { type: string }
        name: { type: string }
        description: { type: string }
        redeemableFrom: { type: string, format: date-time }
        redeemableTo: { type: string, format: date-time }
        maxTotalRedemptions: { type: integer }
        maxPerUserRedemptions: { type: integer }
        isActive: { type: boolean }
        advantageType:
          {
            type: string,
            enum: [fixed_amount_net, free_days, free_billing_periods],
          }
        advantageConfig: { type: object, additionalProperties: true }
        applicablePlanIds:
          type: array
          items: { type: string, format: uuid }
        subscriptionEligibility: { type: string, enum: [new, existing, both] }
    UpdateAdminPromotionDto:
      allOf:
        - $ref: '#/components/schemas/CreateAdminPromotionDto'
    WebhookEventTypeResponseDto:
      type: object
      properties:
        type: { type: string }
        description: { type: string }
    UpdateState:
      type: string
      enum: [up_to_date, update_available, unknown]
    DependencyHealthStatus:
      type: string
      enum: [healthy, degraded, unknown, not_applicable]
    InstanceDependencyHealthDto:
      type: object
      properties:
        redis: { $ref: '#/components/schemas/DependencyHealthStatus' }
        queue: { $ref: '#/components/schemas/DependencyHealthStatus' }
        database: { $ref: '#/components/schemas/DependencyHealthStatus' }
    ChangelogEntryDto:
      type: object
      properties:
        text: { type: string }
        scope: { type: string, enum: [product, shared] }
        product: { type: string, enum: [agenstra, decabill] }
        category: { type: string }
    ScopedChangelogDto:
      type: object
      properties:
        product:
          type: array
          items: { $ref: '#/components/schemas/ChangelogEntryDto' }
        shared:
          type: array
          items: { $ref: '#/components/schemas/ChangelogEntryDto' }
    ServiceInstanceRecordDto:
      type: object
      properties:
        instanceId: { type: string }
        serviceName: { type: string }
        role: { type: string }
        hostname: { type: string }
        installedVersion: { type: string }
        updateState: { $ref: '#/components/schemas/UpdateState' }
        lastHeartbeatAt: { type: string, format: date-time }
        dependencies:
          { $ref: '#/components/schemas/InstanceDependencyHealthDto' }
    ReleaseSnapshotDto:
      type: object
      properties:
        tagName: { type: string }
        name: { type: string }
        body: { type: string }
        htmlUrl: { type: string, format: uri }
        publishedAt: { type: string, format: date-time }
        changelog:
          type: array
          items: { $ref: '#/components/schemas/ChangelogEntryDto' }
        scopedChangelog: { $ref: '#/components/schemas/ScopedChangelogDto' }
    UpdatesStatusSummaryDto:
      type: object
      properties:
        installedVersion: { type: string }
        latestVersion: { type: string, nullable: true }
        updateState: { $ref: '#/components/schemas/UpdateState' }
        lastCheckAt: { type: string, format: date-time, nullable: true }
        lastCheckStatus:
          { type: string, enum: [success, failed, pending, unknown] }
        instanceCount: { type: integer }
        outdatedInstanceCount: { type: integer }
    UpdatesFullStateDto:
      allOf:
        - $ref: '#/components/schemas/UpdatesStatusSummaryDto'
        - type: object
          properties:
            release:
              {
                $ref: '#/components/schemas/ReleaseSnapshotDto',
                nullable: true,
              }
            instances:
              type: array
              items: { $ref: '#/components/schemas/ServiceInstanceRecordDto' }
            scopedChangelog: { $ref: '#/components/schemas/ScopedChangelogDto' }
    UpdateCheckTriggerResultDto:
      type: object
      properties:
        jobId: { type: string }
        enqueuedAt: { type: string, format: date-time }
    CreateWebhookEndpointDto:
      type: object
      required: [name, url, httpMethod, subscribedEvents, authType]
      properties:
        name: { type: string }
        url: { type: string, format: uri }
        httpMethod: { type: string, enum: [POST, GET] }
        subscribedEvents:
          type: array
          items: { type: string }
        enabled: { type: boolean }
        authType:
          {
            type: string,
            enum: [none, authorization, custom_header, query_param],
          }
        authHeaderName: { type: string }
        authValue: { type: string }
        clientId: { type: string, format: uuid, nullable: true }
        deliveryLogRetentionDays: { type: integer, minimum: 1, maximum: 365 }
        deliveryLogMaxEntries: { type: integer, minimum: 1, maximum: 10000 }
    UpdateWebhookEndpointDto:
      type: object
      properties:
        name: { type: string }
        url: { type: string, format: uri }
        httpMethod: { type: string, enum: [POST, GET] }
        subscribedEvents:
          type: array
          items: { type: string }
        enabled: { type: boolean }
        authType:
          {
            type: string,
            enum: [none, authorization, custom_header, query_param],
          }
        authHeaderName: { type: string }
        authValue: { type: string }
        clientId: { type: string, format: uuid, nullable: true }
        deliveryLogRetentionDays:
          { type: integer, minimum: 1, maximum: 365, nullable: true }
        deliveryLogMaxEntries:
          { type: integer, minimum: 1, maximum: 10000, nullable: true }
    WebhookEndpointResponseDto:
      type: object
      properties:
        id: { type: string, format: uuid }
        scopeKey: { type: string }
        clientId: { type: string, format: uuid, nullable: true }
        name: { type: string }
        url: { type: string }
        httpMethod: { type: string, enum: [POST, GET] }
        subscribedEvents:
          type: array
          items: { type: string }
        enabled: { type: boolean }
        authType: { type: string }
        authHeaderName: { type: string, nullable: true }
        hasAuthValue: { type: boolean }
        consecutiveFailures: { type: integer }
        disabledReason: { type: string, nullable: true }
        deliveryLogRetentionDays: { type: integer, nullable: true }
        deliveryLogMaxEntries: { type: integer, nullable: true }
        signingSecret: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    WebhookDeliveryResponseDto:
      type: object
      properties:
        id: { type: string, format: uuid }
        endpointId: { type: string, format: uuid }
        eventId: { type: string, format: uuid }
        eventType: { type: string }
        payload: { type: object, additionalProperties: true }
        httpStatus: { type: integer, nullable: true }
        responseBody: { type: string, nullable: true }
        success: { type: boolean }
        attempt: { type: integer }
        errorMessage: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    PaginatedWebhookDeliveriesResponseDto:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WebhookDeliveryResponseDto'
        total: { type: integer }
