openapi: 3.1.0
info:
  title: Agent Controller API
  version: 1.0.0
  description: HTTP API for managing clients and proxied agent operations
  contact:
    name: ForePath
    email: hi@forepath.io
    url: https://forepath.io
servers:
  - url: http://localhost:3100/api
    description: Local development
  - url: http://localhost:3100
    description: Root (OpenTelemetry metrics; outside /api prefix)
security:
  - bearerAuth: []
paths:
  /auth/login:
    post:
      summary: Login (users auth only)
      description: |
        Interactive console login with account password only.
        Personal access tokens are not accepted; use POST /auth/token instead.
      operationId: login
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LoginDto'
      responses:
        '200':
          description: JWT access token and user info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          description: |
            Invalid credentials, account locked, or email not confirmed.
            When the password is valid but the email is not confirmed, the body includes
            `code: EMAIL_NOT_CONFIRMED` so the client can resume confirmation.
  /auth/token:
    post:
      summary: Exchange personal access token for machine JWT
      description: |
        Token-only exchange (no email). Returns a JWT with `amr: ["pat"]` and capability `scopes`.
        Must not be used for console sessions.
      operationId: exchangePersonalAccessToken
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExchangePersonalAccessTokenDto'
      responses:
        '200':
          description: JWT access token, user info, and scopes
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PatLoginResponse'
        '401':
          description: Invalid, expired, or revoked token
  /auth/register:
    post:
      summary: Register new user (users auth only)
      description: |
        Registers a new user. When DISABLE_SIGNUP is true, returns 503 Service Unavailable.
      operationId: register
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RegisterDto'
      responses:
        '201':
          description: User created (first user auto-confirmed, others receive 6-character alphanumeric confirmation code via email)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResponse'
        '409':
          description: Email already registered
        '503':
          description: Signup is disabled (DISABLE_SIGNUP=true)
  /auth/confirm-email:
    post:
      summary: Confirm email with 6-character code (users auth only)
      operationId: confirmEmail
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConfirmEmailDto'
      responses:
        '200':
          description: Email confirmed
        '400':
          description: Invalid or expired confirmation code
  /auth/request-password-reset:
    post:
      summary: Request password reset (users auth only)
      operationId: requestPasswordReset
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestPasswordResetDto'
      responses:
        '200':
          description: If an account exists with this email, a 6-character password reset code will be sent
  /auth/reset-password:
    post:
      summary: Reset password with 6-character code (users auth only)
      operationId: resetPassword
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResetPasswordDto'
      responses:
        '200':
          description: Password reset
        '400':
          description: Invalid or expired reset code
  /auth/change-password:
    post:
      summary: Change password (authenticated users)
      operationId: changePassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangePasswordDto'
      responses:
        '200':
          description: Password changed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangePasswordResponse'
        '400':
          description: Passwords do not match or invalid
        '401':
          description: Current password incorrect
  /auth/logout:
    post:
      summary: Log out (users auth only; revokes current session by default)
      operationId: logout
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LogoutDto'
      responses:
        '204':
          description: Session invalidated
        '401':
          description: Unauthorized
  /auth/token-scopes:
    get:
      summary: List grantable PAT scopes for the current user
      operationId: listPersonalAccessTokenScopes
      responses:
        '200':
          description: Scope catalog filtered by role
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessTokenScopeDto'
        '401':
          description: Unauthorized
        '403':
          description: Password session required
  /auth/tokens:
    get:
      summary: List own personal access tokens
      operationId: listPersonalAccessTokens
      responses:
        '200':
          description: Active tokens (no hashes)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '401':
          description: Unauthorized
        '403':
          description: Password session required
    post:
      summary: Create a personal access token
      operationId: createPersonalAccessToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePersonalAccessTokenDto'
      responses:
        '201':
          description: Token created; plaintext returned once
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '400':
          description: Invalid scopes or name
        '401':
          description: Unauthorized
        '403':
          description: Password session required or cannot grant scopes
  /auth/tokens/{id}:
    patch:
      summary: Update own personal access token name and scopes
      operationId: updatePersonalAccessToken
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePersonalAccessTokenDto'
      responses:
        '200':
          description: Token updated (secret unchanged)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '400':
          description: Invalid scopes or name
        '401':
          description: Unauthorized
        '403':
          description: Password session required or cannot grant scopes
        '404':
          description: Token not found
    delete:
      summary: Revoke own personal access token
      operationId: revokePersonalAccessToken
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Revoked
        '401':
          description: Unauthorized
        '403':
          description: Password session required
        '404':
          description: Token not found
  /users/{userId}/tokens:
    get:
      summary: List personal access tokens for a user (admin)
      operationId: listUserPersonalAccessTokens
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Tokens for user
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PersonalAccessTokenResponseDto'
        '401':
          description: Unauthorized
        '403':
          description: Admin + password session required
  /users/{userId}/tokens/{tokenId}:
    delete:
      summary: Revoke a user's personal access token (admin)
      operationId: revokeUserPersonalAccessToken
      parameters:
        - in: path
          name: userId
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: tokenId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Revoked
        '401':
          description: Unauthorized
        '403':
          description: Admin + password session required
        '404':
          description: Token not found
  /users:
    get:
      summary: List users (admin only)
      operationId: listUsers
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
      responses:
        '200':
          description: Array of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserResponseDto'
    post:
      summary: Create user (admin only)
      operationId: createUser
      description: >
        Creates a new user. First user is auto-confirmed. Non-first users receive a
        confirmation email (same as self-registration); they must confirm before logging in.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserDto'
      responses:
        '201':
          description: User created (first user auto-confirmed, others receive confirmation email)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponseDto'
        '409':
          description: Email already exists
  /users/{id}:
    get:
      summary: Get user by id (admin only)
      operationId: getUser
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponseDto'
        '404':
          description: User not found
    post:
      summary: Update user (admin only)
      operationId: updateUser
      description: >
        Updates a user. When email is changed, the account is set unconfirmed and a
        confirmation email is sent to the new address (same as self-registration).
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserDto'
      responses:
        '200':
          description: User updated (email change triggers confirmation email to new address)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponseDto'
        '404':
          description: User not found
    delete:
      summary: Delete user (admin only)
      operationId: deleteUser
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: User deleted
        '404':
          description: User not found
  /users/{id}/lock:
    post:
      summary: Lock user account (admin only)
      operationId: lockUser
      description: >
        Locks the target user account immediately. Locked users cannot authenticate
        in users-auth mode. Admins cannot lock themselves.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User locked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponseDto'
        '400':
          description: Self-lock is not allowed
        '404':
          description: User not found
  /users/{id}/unlock:
    post:
      summary: Unlock user account (admin only)
      operationId: unlockUser
      description: >
        Unlocks the target user account by clearing lock timestamp.
        Admins cannot unlock themselves.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: User unlocked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserResponseDto'
        '400':
          description: Self-unlock is not allowed
        '404':
          description: User not found
  /filter-rules:
    get:
      summary: List regex filter rules (admin only)
      operationId: listFilterRules
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            default: 10
          description: Maximum number of rules to return
        - in: query
          name: offset
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
          description: Number of rules to skip (pagination)
      responses:
        '200':
          description: Rules with per-workspace sync summary
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FilterRuleResponseDto'
        '403':
          description: Forbidden
    post:
      summary: Create filter rule (admin only)
      operationId: createFilterRule
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateFilterRuleDto'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilterRuleResponseDto'
        '400':
          description: Validation error
        '403':
          description: Forbidden
  /filter-rules/{id}:
    get:
      summary: Get filter rule (admin only)
      operationId: getFilterRule
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Rule
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilterRuleResponseDto'
        '403':
          description: Forbidden
        '404':
          description: Not found
    put:
      summary: Update filter rule (admin only)
      operationId: updateFilterRule
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateFilterRuleDto'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FilterRuleResponseDto'
        '400':
          description: Validation error
        '403':
          description: Forbidden
        '404':
          description: Not found
    delete:
      summary: Delete filter rule (admin only)
      operationId: deleteFilterRule
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Deleted
        '403':
          description: Forbidden
        '404':
          description: Not found
  /imports/atlassian/connections:
    get:
      summary: List Atlassian site connections (admin only)
      operationId: listAtlassianSiteConnections
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            default: 10
          description: Maximum number of connections to return
        - in: query
          name: offset
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
          description: Number of connections to skip (pagination)
      responses:
        '200':
          description: Connections (API tokens never returned)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AtlassianSiteConnectionResponseDto'
        '403':
          description: Forbidden
    post:
      summary: Create Atlassian site connection (admin only)
      operationId: createAtlassianSiteConnection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAtlassianSiteConnectionDto'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AtlassianSiteConnectionResponseDto'
        '400':
          description: Validation error
        '403':
          description: Forbidden
  /imports/atlassian/connections/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get Atlassian site connection (admin only)
      operationId: getAtlassianSiteConnection
      responses:
        '200':
          description: Connection
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AtlassianSiteConnectionResponseDto'
        '403':
          description: Forbidden
        '404':
          description: Not found
    put:
      summary: Update Atlassian site connection (admin only)
      operationId: updateAtlassianSiteConnection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAtlassianSiteConnectionDto'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AtlassianSiteConnectionResponseDto'
        '403':
          description: Forbidden
        '404':
          description: Not found
    delete:
      summary: Delete Atlassian site connection (admin only)
      operationId: deleteAtlassianSiteConnection
      responses:
        '204':
          description: Deleted
        '403':
          description: Forbidden
        '404':
          description: Not found
  /imports/atlassian/connections/{id}/test:
    post:
      summary: Test Atlassian connection (admin only)
      operationId: testAtlassianSiteConnection
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AtlassianConnectionTestResultDto'
        '403':
          description: Forbidden
  /imports/atlassian/configs:
    get:
      summary: List external import configs (admin only)
      operationId: listExternalImportConfigs
      parameters:
        - in: query
          name: limit
          required: false
          schema:
            type: integer
            minimum: 1
            default: 10
          description: Maximum number of configs to return
        - in: query
          name: offset
          required: false
          schema:
            type: integer
            minimum: 0
            default: 0
          description: Number of configs to skip (pagination)
      responses:
        '200':
          description: Configs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ExternalImportConfigResponseDto'
        '403':
          description: Forbidden
    post:
      summary: Create import config (admin only)
      operationId: createExternalImportConfig
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateExternalImportConfigDto'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalImportConfigResponseDto'
        '400':
          description: Validation error
        '403':
          description: Forbidden
  /imports/atlassian/configs/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get import config (admin only)
      operationId: getExternalImportConfig
      responses:
        '200':
          description: Config
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalImportConfigResponseDto'
        '403':
          description: Forbidden
        '404':
          description: Not found
    put:
      summary: Update import config (admin only)
      operationId: updateExternalImportConfig
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateExternalImportConfigDto'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExternalImportConfigResponseDto'
        '403':
          description: Forbidden
        '404':
          description: Not found
    delete:
      summary: Delete import config (admin only)
      operationId: deleteExternalImportConfig
      responses:
        '204':
          description: Deleted
        '403':
          description: Forbidden
        '404':
          description: Not found
  /imports/atlassian/configs/{id}/run:
    post:
      summary: Run import once for config (admin only)
      operationId: runExternalImportConfig
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '202':
          description: Accepted
        '403':
          description: Forbidden
  /imports/atlassian/configs/{id}/markers:
    delete:
      summary: Clear sync markers for config (admin only)
      operationId: clearExternalImportMarkers
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Cleared
        '403':
          description: Forbidden
  /knowledge:
    get:
      summary: List knowledge nodes for a workspace
      description: |
        Requires `knowledge:read`. Returns flat node list for the given `clientId`.
        For realtime tree coherence, subscribe to Socket.IO namespace **pages** (AsyncAPI `pages/*`).
      operationId: listKnowledgeNodes
      parameters:
        - in: query
          name: clientId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Knowledge nodes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/KnowledgeNodeResponseDto'
    post:
      summary: Create knowledge node
      description: Requires `knowledge:write`.
      operationId: createKnowledgeNode
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateKnowledgeNodeDto'
      responses:
        '201':
          description: Created node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeNodeResponseDto'
  /knowledge/tree:
    get:
      summary: Get knowledge tree for a workspace
      description: Requires `knowledge:read`. Returns nested folder/page tree.
      operationId: getKnowledgeTree
      parameters:
        - in: query
          name: clientId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Nested knowledge tree
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/KnowledgeNodeResponseDto'
  /knowledge/by-sha:
    get:
      summary: Get knowledge node by content SHA
      description: Requires `knowledge:read`.
      operationId: getKnowledgeNodeBySha
      parameters:
        - in: query
          name: clientId
          required: true
          schema: { type: string, format: uuid }
        - in: query
          name: sha
          required: true
          schema: { type: string }
      responses:
        '200':
          description: Matching node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeNodeResponseDto'
        '404':
          description: Not found
  /knowledge/relations:
    get:
      summary: List knowledge relations for a source
      description: Requires `knowledge:read`.
      operationId: listKnowledgeRelations
      parameters:
        - in: query
          name: clientId
          required: true
          schema: { type: string, format: uuid }
        - in: query
          name: sourceType
          required: true
          schema: { type: string, enum: [ticket, page] }
        - in: query
          name: sourceId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Relations
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/KnowledgeRelationResponseDto'
    post:
      summary: Create knowledge relation
      description: Requires `knowledge:write`. Emits `knowledgeRelationChanged` on pages and tickets namespaces.
      operationId: createKnowledgeRelation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateKnowledgeRelationDto'
      responses:
        '201':
          description: Created relation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeRelationResponseDto'
  /knowledge/relations/prompt-context:
    get:
      summary: Collect prompt context sections for a source
      description: Requires `knowledge:read`.
      operationId: getKnowledgePromptContext
      parameters:
        - in: query
          name: clientId
          required: true
          schema: { type: string, format: uuid }
        - in: query
          name: sourceType
          required: true
          schema: { type: string, enum: [ticket, page] }
        - in: query
          name: sourceId
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Prompt sections
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgePromptContextResponseDto'
  /knowledge/relations/{id}:
    delete:
      summary: Delete knowledge relation
      description: Requires `knowledge:write`.
      operationId: deleteKnowledgeRelation
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Deleted
  /knowledge/{id}:
    get:
      summary: Get knowledge node by id
      description: Requires `knowledge:read`.
      operationId: getKnowledgeNode
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeNodeResponseDto'
        '404':
          description: Not found
    patch:
      summary: Update knowledge node
      description: Requires `knowledge:write`.
      operationId: updateKnowledgeNode
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateKnowledgeNodeDto'
      responses:
        '200':
          description: Updated node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeNodeResponseDto'
    delete:
      summary: Delete knowledge node
      description: |
        Requires `knowledge:write`. Optional `releaseExternalSyncMarker=true` clears external sync markers.
      operationId: deleteKnowledgeNode
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
        - in: query
          name: releaseExternalSyncMarker
          schema: { type: string, enum: ['true', '1', 'false', '0'] }
      responses:
        '204':
          description: Deleted
  /knowledge/{id}/activity:
    get:
      summary: List knowledge page activity
      description: Requires `knowledge:read`.
      operationId: listKnowledgePageActivity
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
        - in: query
          name: limit
          schema: { type: integer, default: 50 }
        - in: query
          name: offset
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Activity rows
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/KnowledgePageActivityResponseDto'
  /knowledge/{id}/reorder:
    post:
      summary: Reorder knowledge node
      description: Requires `knowledge:write`.
      operationId: reorderKnowledgeNode
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ReorderKnowledgeNodeDto'
      responses:
        '200':
          description: Reordered node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeNodeResponseDto'
  /knowledge/{id}/duplicate:
    post:
      summary: Duplicate knowledge node
      description: Requires `knowledge:write`.
      operationId: duplicateKnowledgeNode
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Duplicated node
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnowledgeNodeResponseDto'
  /tickets:
    get:
      summary: List project tickets
      description: |
        Returns tickets for workspaces the user can access; global admins see all.
        For realtime list coherence after mutations, subscribe to the Socket.IO **tickets** namespace
        (see AsyncAPI `tickets/*` channels in `spec/asyncapi.yaml`); REST remains the source of truth for loads.
      operationId: listTickets
      parameters:
        - in: query
          name: clientId
          schema:
            type: string
            format: uuid
        - in: query
          name: status
          schema:
            type: string
            enum: [draft, todo, in_progress, prototype, done, closed]
        - in: query
          name: parentId
          schema:
            type: string
          description: Use literal `null` for root tickets only
      responses:
        '200':
          description: Ticket list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TicketResponseDto'
        '403':
          description: Forbidden
    post:
      summary: Create ticket
      description: |
        Optional `creationTemplate` is request-only and not stored. Value `specification` creates four spec-driven subtasks
        under the new root ticket in one transaction; invalid with `parentId` (400).
      operationId: createTicket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTicketDto'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateTicketResponseDto'
  /tickets/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: Get ticket
      operationId: getTicket
      parameters:
        - in: query
          name: includeDescendants
          schema:
            type: boolean
      responses:
        '200':
          description: Ticket
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketResponseDto'
        '404':
          description: Not found
    patch:
      summary: Update ticket
      operationId: updateTicket
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTicketDto'
      responses:
        '200':
          description: Ticket updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketResponseDto'
    delete:
      summary: Delete ticket
      operationId: deleteTicket
      parameters:
        - in: query
          name: releaseExternalSyncMarker
          required: false
          schema:
            type: boolean
            default: false
          description: >
            When true, deletes external import sync markers tied to this ticket so the same remote item may be imported again.
            When false (default), markers are kept with a null local id so scheduled import skips re-creating the row until markers are cleared.
      responses:
        '204':
          description: Deleted
        '409':
          description: Has subtasks
  /tickets/{id}/prototype-prompt:
    get:
      summary: Build prototype prompt for ticket tree
      operationId: getTicketPrototypePrompt
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Prototype prompt
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrototypePromptResponseDto'
  /tickets/{id}/comments:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
          format: uuid
    get:
      summary: List ticket comments
      operationId: listTicketComments
      responses:
        '200':
          description: Ticket comments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TicketCommentResponseDto'
    post:
      summary: Add ticket comment
      operationId: addTicketComment
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTicketCommentDto'
      responses:
        '201':
          description: Comment added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketCommentResponseDto'
  /tickets/{id}/activity:
    get:
      summary: List ticket activity
      operationId: listTicketActivity
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: limit
          schema:
            type: integer
            default: 50
        - in: query
          name: offset
          schema:
            type: integer
            default: 0
      responses:
        '200':
          description: Ticket activity
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TicketActivityResponseDto'
  /tickets/{id}/body-generation-sessions:
    post:
      summary: Start AI body generation session
      operationId: startTicketBodyGenerationSession
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StartBodyGenerationSessionDto'
      responses:
        '200':
          description: AI body generation session started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StartBodyGenerationSessionResponseDto'
  /tickets/{id}/apply-generated-body:
    post:
      summary: Apply AI-generated ticket body
      operationId: applyTicketGeneratedBody
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ApplyGeneratedBodyDto'
      responses:
        '200':
          description: AI-generated ticket body applied
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketResponseDto'
  /tickets/{id}/migrate:
    post:
      summary: Migrate ticket subtree to another workspace
      description: |
        Moves the resolved root ticket and all of its descendant tickets that share the source workspace to
        `targetClientId`. The caller may pass any ticket id in the subtree; the server always migrates the full tree.
        Requires workspace management (see `canManageWorkspaceConfiguration` on clients) on **both** the source and
        target workspaces. Emits `ticketRemoved` for each id on the source workspace room and `ticketUpsert` on the
        target workspace room (tickets Socket.IO namespace). `targetClientId` must differ from the ticket workspace (400 otherwise).
      operationId: migrateTicketToWorkspace
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MigrateTicketDto'
      responses:
        '200':
          description: Migrated root ticket (includes `children` when descendants exist)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MigrateTicketResponseDto'
        '400':
          description: Invalid request (e.g. target workspace equals source)
        '403':
          description: Missing workspace access or insufficient workspace role for source or target
        '404':
          description: Ticket not found
  /tickets/{ticketId}/automation:
    get:
      summary: Get ticket automation configuration
      operationId: getTicketAutomation
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Automation row for the ticket
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAutomationResponseDto'
    patch:
      summary: Update ticket automation configuration
      description: >
        Applies partial updates. Only fields present in the body are considered; unchanged values do not
        persist a new row or emit activity. Activity rows use action types such as AUTOMATION_ELIGIBILITY_CHANGED,
        AUTOMATION_APPROVAL_REQUIREMENT_CHANGED, AUTOMATION_SETTINGS_UPDATED, and AUTOMATION_APPROVAL_INVALIDATED
        only when prior approval was meaningful (requiresApproval was true and approval existed), except that
        turning off the approval requirement alone does not emit AUTOMATION_APPROVAL_INVALIDATED.
      operationId: patchTicketAutomation
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateTicketAutomationDto'
      responses:
        '200':
          description: Updated automation configuration
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAutomationResponseDto'
  /tickets/{ticketId}/automation/approve:
    post:
      summary: Approve automation run (when requiresApproval is true)
      operationId: approveTicketAutomation
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Approval recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAutomationResponseDto'
  /tickets/{ticketId}/automation/unapprove:
    post:
      summary: Revoke automation approval (when requiresApproval is true)
      operationId: unapproveTicketAutomation
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Approval revoked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAutomationResponseDto'
  /tickets/{ticketId}/automation/runs:
    get:
      summary: List automation runs for a ticket
      operationId: listTicketAutomationRuns
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Run history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/TicketAutomationRunResponseDto'
  /tickets/{ticketId}/automation/runs/{runId}:
    get:
      summary: Get automation run detail including steps
      operationId: getTicketAutomationRun
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: runId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Run detail
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAutomationRunResponseDto'
  /tickets/{ticketId}/automation/runs/{runId}/cancel:
    post:
      summary: Cancel a pending or running automation run
      operationId: cancelTicketAutomationRun
      parameters:
        - in: path
          name: ticketId
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: runId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Run cancelled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TicketAutomationRunResponseDto'
  /clients/{id}/agent-autonomy/enabled-agent-ids:
    get:
      summary: List agent IDs with prototype autonomy enabled for this client
      description: |
        Only agents returned here can be picked by the autonomous ticket scheduler for this workspace
        (intersection with each ticket's allowed-agent list). Matches `client_agent_autonomy.enabled = true`.
      operationId: listEnabledAutonomyAgentIds
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: UUIDs of agents with autonomy enabled
          content:
            application/json:
              schema:
                type: object
                required:
                  - agentIds
                properties:
                  agentIds:
                    type: array
                    items:
                      type: string
                      format: uuid
  /clients/{id}/agents/{agentId}/autonomy:
    get:
      summary: Get per-agent autonomy settings
      operationId: getClientAgentAutonomy
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Autonomy configuration
    put:
      summary: Create or update per-agent autonomy settings
      description: |
        Requires workspace management rights (see ClientResponseDto.canManageWorkspaceConfiguration).
        Interactive user context required; API key auth may manage when applicable.
      operationId: upsertClientAgentAutonomy
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Saved autonomy configuration
        '403':
          description: |
            No client access, or workspace management rights required (plain client member cannot update autonomy).
  /clients:
    get:
      summary: List clients
      description: |
        Returns all clients the user has access to. In api-key mode, returns all clients.
        For regular users, only returns clients where:
        - The user is the creator (user_id matches)
        - The user is in the client_users relationship table
        Global admins see all clients.
      operationId: listClients
      parameters:
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
          description: Maximum number of clients to return (default 10)
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
          description: Number of clients to skip (default 0)
      responses:
        '200':
          description: Array of clients
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ClientResponseDto'
    post:
      summary: Create client
      description: |
        Creates a new client. The user_id is automatically set to the logged-in user.
        In api-key mode, user_id is set to null.
      operationId: createClient
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateClientDto'
      responses:
        '201':
          description: Created client with generated API key (if applicable)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateClientResponseDto'
  /clients/{id}:
    get:
      summary: Get client by id
      description: |
        Returns a client by ID. Only accessible if the user has access to the client.
        Access is granted if:
        - The user is a global admin
        - The user is the creator (user_id matches)
        - The user is in the client_users relationship table
        In api-key mode, access is always granted.
      operationId: getClient
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Client
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientResponseDto'
        '403':
          description: User does not have access to this client
    post:
      summary: Update client
      description: |
        Updates a client. Requires workspace management rights: global admin, workspace creator,
        client_users role `admin`, API key authentication, or api-key server mode (not plain client_users `user`).
        See `canManageWorkspaceConfiguration` on ClientResponseDto.
      operationId: updateClient
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateClientDto'
      responses:
        '200':
          description: Updated client
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientResponseDto'
        '403':
          description: |
            Forbidden — no access to this client, or caller lacks workspace management rights
            (same rule as POST for updates).
    delete:
      summary: Delete client
      description: |
        Deletes a client. Same workspace management requirement as POST /clients/{id} (not available to plain client_users `user`).
      operationId: deleteClient
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '204':
          description: Deleted
        '403':
          description: |
            Forbidden — no access to this client, or caller lacks workspace management rights.
  /clients/{id}/users:
    get:
      summary: List users associated with a client
      description: |
        Returns all users associated with a client through the client_users relationship table.
        Only accessible if the user has access to the client.
        Note: In api-key mode, users do not play a role; this endpoint is intended for keycloak and users authentication.
      operationId: getClientUsers
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
      responses:
        '200':
          description: Array of client-user relationships
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ClientUserResponseDto'
        '403':
          description: User does not have access to this client
    post:
      summary: Add a user to a client
      description: |
        Adds a user to a client by email address. The user is identified by their email.
        Permissions:
        - Global admins can add any role
        - Client creators can add any role
        - Client admins can only add 'user' role
        - Client users cannot add anyone
        Note: In api-key mode, users do not play a role; this endpoint is not applicable. Use keycloak or users authentication.
      operationId: addClientUser
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddClientUserDto'
      responses:
        '201':
          description: User added to client
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ClientUserResponseDto'
        '400':
          description: User not found, relationship already exists, or permission denied
        '403':
          description: User does not have access to this client
  /clients/{id}/users/{relationshipId}:
    delete:
      summary: Remove a user from a client
      description: |
        Removes a user from a client by deleting the client_users relationship.
        Permissions:
        - Global admins can remove anyone
        - Client creators can remove anyone
        - Client admins can remove users (but not other admins)
        - Client users cannot remove anyone
        Note: In api-key mode, users do not play a role; this endpoint is not applicable. Use keycloak or users authentication.
      operationId: removeClientUser
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: relationshipId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client_users relationship to remove
      responses:
        '204':
          description: User removed from client
        '400':
          description: Relationship not found or permission denied
        '403':
          description: User does not have access to this client
  /clients/{id}/agents:
    get:
      summary: List agents for a client
      operationId: listClientAgents
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
          description: Maximum number of agents to return (default 10)
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
          description: Number of agents to skip (default 0)
      responses:
        '200':
          description: Array of agents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AgentResponseDto'
        '403':
          description: User does not have access to this client
    post:
      summary: Create agent for a client
      description: Requires workspace management rights (see ClientResponseDto.canManageWorkspaceConfiguration).
      operationId: createClientAgent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAgentDto'
      responses:
        '201':
          description: Created agent with generated password
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CreateAgentResponseDto'
        '403':
          description: No client access, or workspace management rights required
  /clients/{id}/agents/{agentId}:
    get:
      summary: Get agent by id for a client
      operationId: getClientAgent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Agent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponseDto'
        '403':
          description: User does not have access to this client
    post:
      summary: Update agent for a client
      description: Requires workspace management rights (see ClientResponseDto.canManageWorkspaceConfiguration).
      operationId: updateClientAgent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAgentDto'
      responses:
        '200':
          description: Updated agent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponseDto'
        '403':
          description: No client access, or workspace management rights required
    delete:
      summary: Delete agent for a client
      description: Requires workspace management rights.
      operationId: deleteClientAgent
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent to delete
      responses:
        '204':
          description: Deleted
        '403':
          description: No client access, or workspace management rights required
  /clients/{id}/agents/{agentId}/models:
    get:
      summary: List models for an agent (proxied)
      operationId: listClientAgentModels
      description: |
        Proxies to the remote agent-manager `GET /api/agents/{agentId}/models`. Returns a map of model id to
        display name (possibly empty). Uses the same hybrid authentication as the rest of this API (Bearer /
        Keycloak / users); caller must have access to the client. Remote errors (e.g. 400 when the provider
        does not support listing models) are forwarded as appropriate.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Map of model id to display name (may be empty)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentModelsResponseDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Agent or resource not found on remote agent-manager
        '400':
          description: Bad request from remote (e.g. provider does not support listing models)
  /clients/{id}/agents/{agentId}/start:
    post:
      summary: Start agent environment for a client
      operationId: startClientAgent
      description: Start all Docker containers for the agent (main, VNC, SSH). Proxied to agent-manager.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Agent environment started
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponseDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Agent not found
  /clients/{id}/agents/{agentId}/stop:
    post:
      summary: Stop agent environment for a client
      operationId: stopClientAgent
      description: Stop all Docker containers for the agent (main, VNC, SSH). Proxied to agent-manager.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Agent environment stopped
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponseDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Agent not found
  /clients/{id}/agents/{agentId}/restart:
    post:
      summary: Restart agent environment for a client
      operationId: restartClientAgent
      description: Restart all Docker containers for the agent (main, VNC, SSH). Proxied to agent-manager.
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Agent environment restarted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AgentResponseDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Agent not found
  /clients/{id}/agents/{agentId}/files:
    get:
      summary: List directory contents (proxied)
      operationId: listClientAgentDirectory
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: query
          name: path
          schema:
            type: string
          description: Directory path relative to the selected context root (defaults to '.')
        - $ref: '#/components/parameters/fileManagerContext'
      responses:
        '200':
          description: Array of file nodes
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FileNodeDto'
        '400':
          description: Invalid context or agent type does not support configuration file access (proxied from agent-manager)
        '403':
          description: User does not have access to this client, or lacks workspace management rights when context is config
        '404':
          description: Client, agent, or directory not found
  /clients/{id}/agents/{agentId}/files/{path}:
    get:
      summary: Read file content (proxied)
      operationId: readClientAgentFile
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: path
          required: true
          schema:
            type: string
          description: File path relative to the selected context root (supports nested paths)
        - $ref: '#/components/parameters/fileManagerContext'
      responses:
        '200':
          description: File content (base64-encoded)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FileContentDto'
        '400':
          description: Invalid path, invalid context, or agent type does not support configuration file access
        '403':
          description: User does not have access to this client, or lacks workspace management rights when context is config
        '404':
          description: Client, agent, or file not found
    put:
      summary: Write file content (proxied)
      operationId: writeClientAgentFile
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: path
          required: true
          schema:
            type: string
          description: File path relative to the selected context root (supports nested paths)
        - $ref: '#/components/parameters/fileManagerContext'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WriteFileDto'
      responses:
        '204':
          description: File written successfully
        '400':
          description: Invalid path, content too large, invalid context, or agent type does not support configuration file access
        '403':
          description: User does not have access to this client, or lacks workspace management rights when context is config
        '404':
          description: Client or agent not found
    delete:
      summary: Delete file or directory (proxied)
      operationId: deleteClientAgentFileOrDirectory
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: path
          required: true
          schema:
            type: string
          description: File or directory path relative to the selected context root (supports nested paths)
        - $ref: '#/components/parameters/fileManagerContext'
      responses:
        '204':
          description: File or directory deleted successfully
        '400':
          description: Invalid path, invalid context, or agent type does not support configuration file access
        '403':
          description: User does not have access to this client, or lacks workspace management rights when context is config
        '404':
          description: Client, agent, or file/directory not found
    patch:
      summary: Move file or directory (proxied)
      operationId: moveClientAgentFileOrDirectory
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: path
          required: true
          schema:
            type: string
          description: Source file or directory path relative to the selected context root (supports nested paths)
        - $ref: '#/components/parameters/fileManagerContext'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MoveFileDto'
      responses:
        '204':
          description: File or directory moved successfully
        '400':
          description: Invalid path, destination, invalid context, or agent type does not support configuration file access
        '403':
          description: User does not have access to this client, or lacks workspace management rights when context is config
        '404':
          description: Client, agent, or source file/directory not found
    post:
      summary: Create file or directory (proxied)
      operationId: createClientAgentFileOrDirectory
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: path
          required: true
          schema:
            type: string
          description: File or directory path relative to the selected context root (supports nested paths)
        - $ref: '#/components/parameters/fileManagerContext'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateFileDto'
      responses:
        '201':
          description: File or directory created
        '400':
          description: Invalid request, invalid context, or agent type does not support configuration file access
        '403':
          description: User does not have access to this client, or lacks workspace management rights when context is config
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/environment:
    get:
      summary: List environment variables for an agent (proxied)
      operationId: listClientAgentEnvironmentVariables
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
          description: Maximum number of environment variables to return (default 50)
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
          description: Number of environment variables to skip (default 0)
      responses:
        '200':
          description: Array of environment variables
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/EnvironmentVariableResponseDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
    post:
      summary: Create environment variable for an agent (proxied)
      description: Requires workspace management rights.
      operationId: createClientAgentEnvironmentVariable
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateEnvironmentVariableDto'
      responses:
        '201':
          description: Created environment variable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnvironmentVariableResponseDto'
        '400':
          description: Invalid request
        '403':
          description: No client access, or workspace management rights required
        '404':
          description: Client or agent not found
    delete:
      summary: Delete all environment variables for an agent (proxied)
      description: Requires workspace management rights.
      operationId: deleteAllClientAgentEnvironmentVariables
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Number of environment variables deleted
          content:
            application/json:
              schema:
                type: object
                required: [deletedCount]
                properties:
                  deletedCount:
                    type: integer
        '403':
          description: No client access, or workspace management rights required
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/environment/count:
    get:
      summary: Get count of environment variables for an agent (proxied)
      operationId: countClientAgentEnvironmentVariables
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Count of environment variables
          content:
            application/json:
              schema:
                type: object
                required: [count]
                properties:
                  count:
                    type: integer
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/environment/{envVarId}:
    put:
      summary: Update environment variable for an agent (proxied)
      description: Requires workspace management rights.
      operationId: updateClientAgentEnvironmentVariable
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: envVarId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the environment variable to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateEnvironmentVariableDto'
      responses:
        '200':
          description: Updated environment variable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EnvironmentVariableResponseDto'
        '400':
          description: Invalid request
        '403':
          description: No client access, or workspace management rights required
        '404':
          description: Client, agent, or environment variable not found
    delete:
      summary: Delete environment variable for an agent (proxied)
      description: Requires workspace management rights.
      operationId: deleteClientAgentEnvironmentVariable
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: envVarId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the environment variable to delete
      responses:
        '204':
          description: Environment variable deleted successfully
        '403':
          description: No client access, or workspace management rights required
        '404':
          description: Client, agent, or environment variable not found
  /clients/{id}/configuration-overrides:
    get:
      summary: List effective workspace configuration overrides (proxied)
      description: Requires workspace management rights.
      operationId: listClientWorkspaceConfigurationOverrides
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
      responses:
        '200':
          description: Effective workspace configuration settings
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WorkspaceConfigurationSettingResponseDto'
        '403':
          description: Workspace management rights required
        '404':
          description: Client not found
  /clients/{id}/configuration-overrides/{settingKey}:
    put:
      summary: Create or update a workspace configuration override (proxied)
      description: Requires workspace management rights.
      operationId: upsertClientWorkspaceConfigurationOverride
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: settingKey
          required: true
          schema:
            $ref: '#/components/schemas/WorkspaceConfigurationSettingKey'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpsertWorkspaceConfigurationOverrideDto'
      responses:
        '200':
          description: Updated effective workspace configuration setting
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WorkspaceConfigurationSettingResponseDto'
        '400':
          description: Invalid request or unsupported setting key
        '403':
          description: Workspace management rights required
        '404':
          description: Client not found
    delete:
      summary: Delete a workspace configuration override (proxied)
      description: Requires workspace management rights.
      operationId: deleteClientWorkspaceConfigurationOverride
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: settingKey
          required: true
          schema:
            $ref: '#/components/schemas/WorkspaceConfigurationSettingKey'
      responses:
        '204':
          description: Override deleted successfully
        '403':
          description: Workspace management rights required
        '404':
          description: Client not found
  /clients/{id}/agents/{agentId}/vcs/status:
    get:
      summary: Get git status (proxied)
      operationId: getClientAgentGitStatus
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Git status information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitStatusDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/branches:
    get:
      summary: List all branches (proxied)
      operationId: getClientAgentGitBranches
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Array of branch information
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/GitBranchDto'
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
    post:
      summary: Create a new branch (proxied)
      operationId: createClientAgentGitBranch
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateBranchDto'
      responses:
        '201':
          description: Branch created successfully
        '400':
          description: Invalid branch name or parameters
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/branches/{branch}:
    delete:
      summary: Delete a branch (proxied)
      operationId: deleteClientAgentGitBranch
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: branch
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Branch deleted successfully
        '400':
          description: Cannot delete current branch
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or branch not found
  /clients/{id}/agents/{agentId}/vcs/branches/{branch}/switch:
    post:
      summary: Switch to a branch (proxied)
      operationId: switchClientAgentGitBranch
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: branch
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Branch switched successfully
        '400':
          description: Invalid branch or uncommitted changes
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or branch not found
  /clients/{id}/agents/{agentId}/vcs/diff:
    get:
      summary: Get file diff (proxied)
      operationId: getClientAgentGitFileDiff
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: query
          name: path
          required: true
          schema:
            type: string
          description: File path relative to repository root
      responses:
        '200':
          description: File diff information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitDiffDto'
        '400':
          description: Invalid file path
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or file not found
  /clients/{id}/agents/{agentId}/vcs/stage:
    post:
      summary: Stage files (proxied)
      operationId: stageClientAgentFiles
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StageFilesDto'
      responses:
        '204':
          description: Files staged successfully
        '400':
          description: Invalid file paths
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/unstage:
    post:
      summary: Unstage files (proxied)
      operationId: unstageClientAgentFiles
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnstageFilesDto'
      responses:
        '204':
          description: Files unstaged successfully
        '400':
          description: Invalid file paths
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/commit:
    post:
      summary: Commit staged changes (proxied)
      operationId: commitClientAgentChanges
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CommitDto'
      responses:
        '204':
          description: Changes committed successfully
        '400':
          description: No staged changes or invalid commit message
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/push:
    post:
      summary: Push changes to remote (proxied)
      operationId: pushClientAgentChanges
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PushOptionsDto'
      responses:
        '204':
          description: Changes pushed successfully
        '400':
          description: Nothing to push or push failed
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/pull:
    post:
      summary: Pull changes from remote (proxied)
      operationId: pullClientAgentChanges
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '204':
          description: Changes pulled successfully
        '400':
          description: Pull failed or conflicts
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/fetch:
    post:
      summary: Fetch changes from remote (proxied)
      operationId: fetchClientAgentChanges
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '204':
          description: Changes fetched successfully
        '403':
          description: User does not have access to this client
        '404':
          description: Client or agent not found
  /clients/{id}/agents/{agentId}/vcs/workspace/prepare-clean:
    post:
      summary: Prepare clean workspace (fetch, reset --hard, clean) — proxied
      operationId: prepareCleanClientAgentWorkspace
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [baseBranch]
              properties:
                baseBranch:
                  type: string
      responses:
        '204':
          description: Workspace reset
        '403':
          description: Forbidden
        '404':
          description: Not found
  /clients/{id}/agents/{agentId}/automation/verify-commands:
    post:
      summary: Run verifier shell commands in agent container (proxied)
      operationId: runClientAgentVerifierCommands
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: true
      responses:
        '200':
          description: Command results with exit codes
  /clients/{id}/agents/{agentId}/vcs/rebase:
    post:
      summary: Rebase current branch (proxied)
      operationId: rebaseClientAgentBranch
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RebaseDto'
      responses:
        '204':
          description: Rebase completed successfully
        '400':
          description: Rebase failed or conflicts
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or branch not found
  /clients/{id}/agents/{agentId}/vcs/conflicts/resolve:
    post:
      summary: Resolve merge conflict (proxied)
      operationId: resolveClientAgentConflict
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResolveConflictDto'
      responses:
        '204':
          description: Conflict resolved successfully
        '400':
          description: Invalid conflict resolution strategy
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or file not found
  /clients/provisioning/providers:
    get:
      summary: List available provisioning providers
      operationId: listProvisioningProviders
      responses:
        '200':
          description: Array of available provisioning providers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProvisioningProviderInfo'
  /clients/provisioning/providers/{providerType}/server-types:
    get:
      summary: Get available server types for a provisioning provider
      operationId: getServerTypes
      parameters:
        - in: path
          name: providerType
          required: true
          schema:
            type: string
            enum: [hetzner, digital-ocean]
          description: The provisioning provider type (e.g., 'hetzner', 'digital-ocean')
      responses:
        '200':
          description: Array of server types with specifications and pricing
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ServerType'
        '400':
          description: Provider type not found or invalid
  /clients/provisioning/providers/{providerType}/locations:
    get:
      summary: Get available geography options for a provisioning provider
      operationId: getProviderLocations
      parameters:
        - in: path
          name: providerType
          required: true
          schema:
            type: string
            enum: [hetzner, digital-ocean]
          description: The provisioning provider type (e.g., 'hetzner', 'digital-ocean')
      responses:
        '200':
          description: Array of locations/regions with human-readable labels
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ProviderLocation'
        '400':
          description: Provider type not found or invalid
  /clients/provisioning/provision:
    post:
      summary: Provision a new server and create a client
      operationId: provisionServer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProvisionServerDto'
      responses:
        '201':
          description: Provisioned server with client information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ProvisionedServerResponseDto'
        '400':
          description: Invalid request or provisioning failed
  /clients/{id}/provisioning/info:
    get:
      summary: Get server information for a provisioned client
      operationId: getServerInfo
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
      responses:
        '200':
          description: Server information
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServerInfo'
        '404':
          description: No provisioning reference found for client or client not found
  /clients/{id}/provisioning:
    delete:
      summary: Delete a provisioned server and its associated client
      operationId: deleteProvisionedServer
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
      responses:
        '204':
          description: Server and client deleted successfully
        '400':
          description: No provisioning reference found for client
        '404':
          description: Client not found
  /clients/{id}/agents/{agentId}/deployments/configuration:
    get:
      summary: Get deployment configuration for an agent (proxied)
      operationId: getClientAgentDeploymentConfiguration
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Deployment configuration
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Client, agent, or deployment configuration not found
    post:
      summary: Create or update deployment configuration for an agent (proxied)
      operationId: upsertClientAgentDeploymentConfiguration
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Deployment configuration created or updated
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Invalid request
        '404':
          description: Client or agent not found
    delete:
      summary: Delete deployment configuration for an agent (proxied)
      operationId: deleteClientAgentDeploymentConfiguration
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '204':
          description: Deployment configuration deleted successfully
        '404':
          description: Client, agent, or deployment configuration not found
  /clients/{id}/agents/{agentId}/deployments/repositories:
    get:
      summary: List repositories accessible with the agent's deployment configuration (proxied)
      operationId: listClientAgentDeploymentRepositories
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      responses:
        '200':
          description: Array of repositories
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or deployment configuration not found
  /clients/{id}/agents/{agentId}/deployments/repositories/{repositoryId}/branches:
    get:
      summary: List branches for a repository (proxied)
      operationId: listClientAgentDeploymentBranches
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: repositoryId
          required: true
          schema:
            type: string
          description: Repository identifier (e.g., 'owner/repo' for GitHub)
      responses:
        '200':
          description: Array of branches
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        '403':
          description: User does not have access to this client
        '404':
          description: Client, agent, or deployment configuration not found
  /clients/{id}/agents/{agentId}/deployments/repositories/{repositoryId}/workflows:
    get:
      summary: List workflows for a repository (proxied)
      operationId: listClientAgentDeploymentWorkflows
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: repositoryId
          required: true
          schema:
            type: string
          description: Repository identifier (e.g., 'owner/repo' for GitHub)
        - in: query
          name: branch
          schema:
            type: string
          description: Branch name (optional, defaults to default branch)
      responses:
        '200':
          description: Array of workflows
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        '404':
          description: Client, agent, or deployment configuration not found
  /clients/{id}/agents/{agentId}/deployments/workflows/trigger:
    post:
      summary: Trigger a workflow run (proxied)
      operationId: triggerClientAgentDeploymentWorkflow
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '200':
          description: Workflow run triggered
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Invalid request
        '404':
          description: Client, agent, or deployment configuration not found
  /clients/{id}/agents/{agentId}/deployments/runs:
    get:
      summary: List deployment runs for an agent (proxied)
      operationId: listClientAgentDeploymentRuns
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
          description: Maximum number of runs to return (default 50)
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
          description: Number of runs to skip (default 0)
      responses:
        '200':
          description: Array of deployment runs
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        '404':
          description: Client, agent, or deployment configuration not found
  /clients/{id}/agents/{agentId}/deployments/runs/{runId}:
    get:
      summary: Get the status of a pipeline run (proxied)
      operationId: getClientAgentDeploymentRunStatus
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: runId
          required: true
          schema:
            type: string
          description: Provider-specific run identifier
      responses:
        '200':
          description: Pipeline run status
          content:
            application/json:
              schema:
                type: object
        '404':
          description: Client, agent, deployment configuration, or run not found
  /clients/{id}/agents/{agentId}/deployments/runs/{runId}/logs:
    get:
      summary: Get logs for a pipeline run (proxied)
      operationId: getClientAgentDeploymentRunLogs
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: runId
          required: true
          schema:
            type: string
          description: Provider-specific run identifier
      responses:
        '200':
          description: Pipeline run logs
          content:
            application/json:
              schema:
                type: object
                required: [logs]
                properties:
                  logs:
                    type: string
                    description: Pipeline run logs as string
        '404':
          description: Client, agent, deployment configuration, or run not found
  /clients/{id}/agents/{agentId}/deployments/runs/{runId}/jobs:
    get:
      summary: List jobs/steps for a pipeline run (proxied)
      operationId: listClientAgentDeploymentRunJobs
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: runId
          required: true
          schema:
            type: string
          description: Provider-specific run identifier
      responses:
        '200':
          description: Array of jobs/steps
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
        '404':
          description: Client, agent, deployment configuration, or run not found
  /clients/{id}/agents/{agentId}/deployments/runs/{runId}/jobs/{jobId}/logs:
    get:
      summary: Get logs for a specific job/step (proxied)
      operationId: getClientAgentDeploymentJobLogs
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: runId
          required: true
          schema:
            type: string
          description: Provider-specific run identifier
        - in: path
          name: jobId
          required: true
          schema:
            type: string
          description: Job identifier
      responses:
        '200':
          description: Job logs
          content:
            application/json:
              schema:
                type: object
                required: [logs]
                properties:
                  logs:
                    type: string
                    description: Job logs as string
        '404':
          description: Client, agent, deployment configuration, run, or job not found
  /clients/{id}/agents/{agentId}/deployments/runs/{runId}/cancel:
    post:
      summary: Cancel a running pipeline (proxied)
      operationId: cancelClientAgentDeploymentRun
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: path
          name: agentId
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the agent
        - in: path
          name: runId
          required: true
          schema:
            type: string
          description: Provider-specific run identifier
      responses:
        '204':
          description: Pipeline cancelled successfully
        '404':
          description: Client, agent, deployment configuration, or run not found
  /clients/{id}/statistics/summary:
    get:
      summary: Get statistics summary for a client
      description: |
        Returns aggregate statistics (messages, words, chars, filter drops) for a client.
        Requires access to the client (creator, client_users, or admin/api-key).
      operationId: getClientStatisticsSummary
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
          description: The UUID of the client
        - in: query
          name: from
          schema:
            type: string
            format: date-time
          description: Start of time range (ISO 8601)
        - in: query
          name: to
          schema:
            type: string
            format: date-time
          description: End of time range (ISO 8601)
        - in: query
          name: groupBy
          schema:
            type: string
            enum: [day, hour]
          description: Optional time-series aggregation
      responses:
        '200':
          description: Statistics summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsSummaryDto'
        '403':
          description: Access denied to this client
  /clients/{id}/statistics/chat-io:
    get:
      summary: Get chat I/O records for a client
      operationId: getClientStatisticsChatIo
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: agentId
          schema:
            type: string
            format: uuid
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: direction
          schema:
            type: string
            enum: [input, output]
        - in: query
          name: interactionKind
          schema:
            type: string
            enum:
              [
                chat,
                prompt_enhancement,
                ticket_body_generation,
                auto_context_enrichment,
                autonomous_ticket_run,
                autonomous_ticket_run_turn,
                autonomous_ticket_commit_message,
              ]
          description: Filter by interaction type (normal chat vs prompt enhancement metrics)
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across id, direction, interactionKind, wordCount, charCount
      responses:
        '200':
          description: Paginated chat I/O records
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsChatIoListDto'
        '403':
          description: Access denied to this client
  /clients/{id}/statistics/filter-drops:
    get:
      summary: Get filter drops for a client
      operationId: getClientStatisticsFilterDrops
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: agentId
          schema:
            type: string
            format: uuid
        - in: query
          name: filterType
          schema:
            type: string
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across filterType, filterDisplayName, filterReason, direction
      responses:
        '200':
          description: Paginated filter drops
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsFilterDropListDto'
        '403':
          description: Access denied to this client
  /clients/{id}/statistics/filter-flags:
    get:
      summary: Get filter flags for a client
      description: |
        Returns paginated filter flags (messages flagged/modified by filters but NOT dropped).
        These messages were allowed through (possibly modified) and appear in Chat I/O.
      operationId: getClientStatisticsFilterFlags
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: agentId
          schema:
            type: string
            format: uuid
        - in: query
          name: filterType
          schema:
            type: string
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across filterType, filterDisplayName, filterReason, direction
      responses:
        '200':
          description: Paginated filter flags
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsFilterFlagListDto'
        '403':
          description: Access denied to this client
  /clients/{id}/statistics/entity-events:
    get:
      summary: Get entity lifecycle events for a client
      operationId: getClientStatisticsEntityEvents
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
        - in: query
          name: entityType
          schema:
            type: string
            enum: [user, client, agent, client_user, provisioning_reference]
        - in: query
          name: eventType
          schema:
            type: string
            enum: [created, updated, deleted]
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across entityType, eventType, originalEntityId
      responses:
        '200':
          description: Paginated entity events
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsEntityEventListDto'
        '403':
          description: Access denied to this client
  /statistics/summary:
    get:
      summary: Get aggregate statistics summary
      description: |
        Returns aggregate statistics across all accessible clients. When clientId is provided,
        validates access and returns data for that client only. 403 when clientId provided but no access.
      operationId: getStatisticsSummary
      parameters:
        - in: query
          name: clientId
          schema:
            type: string
            format: uuid
          description: Optional client filter; must be accessible
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: groupBy
          schema:
            type: string
            enum: [day, hour]
      responses:
        '200':
          description: Statistics summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsSummaryDto'
        '403':
          description: Access denied to requested client
  /statistics/chat-io:
    get:
      summary: Get chat I/O records (aggregate or by client)
      operationId: getStatisticsChatIo
      parameters:
        - in: query
          name: clientId
          schema:
            type: string
            format: uuid
        - in: query
          name: agentId
          schema:
            type: string
            format: uuid
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: direction
          schema:
            type: string
            enum: [input, output]
        - in: query
          name: interactionKind
          schema:
            type: string
            enum:
              [
                chat,
                prompt_enhancement,
                ticket_body_generation,
                auto_context_enrichment,
                autonomous_ticket_run,
                autonomous_ticket_run_turn,
                autonomous_ticket_commit_message,
              ]
          description: Filter by interaction type (normal chat vs prompt enhancement metrics)
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across id, direction, interactionKind, wordCount, charCount
      responses:
        '200':
          description: Paginated chat I/O records
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsChatIoListDto'
        '403':
          description: Access denied to requested client
  /statistics/filter-drops:
    get:
      summary: Get filter drops (aggregate or by client)
      operationId: getStatisticsFilterDrops
      parameters:
        - in: query
          name: clientId
          schema:
            type: string
            format: uuid
        - in: query
          name: agentId
          schema:
            type: string
            format: uuid
        - in: query
          name: filterType
          schema:
            type: string
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across filterType, filterDisplayName, filterReason, direction
      responses:
        '200':
          description: Paginated filter drops
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsFilterDropListDto'
        '403':
          description: Access denied to requested client
  /statistics/filter-flags:
    get:
      summary: Get filter flags (aggregate or by client)
      description: |
        Returns paginated filter flags (messages flagged/modified by filters but NOT dropped).
        These messages were allowed through (possibly modified) and appear in Chat I/O.
      operationId: getStatisticsFilterFlags
      parameters:
        - in: query
          name: clientId
          schema:
            type: string
            format: uuid
        - in: query
          name: agentId
          schema:
            type: string
            format: uuid
        - in: query
          name: filterType
          schema:
            type: string
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across filterType, filterDisplayName, filterReason, direction
      responses:
        '200':
          description: Paginated filter flags
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsFilterFlagListDto'
        '403':
          description: Access denied to requested client
  /statistics/entity-events:
    get:
      summary: Get entity events (aggregate or by client)
      operationId: getStatisticsEntityEvents
      parameters:
        - in: query
          name: clientId
          schema:
            type: string
            format: uuid
        - in: query
          name: entityType
          schema:
            type: string
            enum: [user, client, agent, client_user, provisioning_reference]
        - in: query
          name: eventType
          schema:
            type: string
            enum: [created, updated, deleted]
        - in: query
          name: from
          schema:
            type: string
            format: date-time
        - in: query
          name: to
          schema:
            type: string
            format: date-time
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 0
        - in: query
          name: offset
          schema:
            type: integer
            minimum: 0
        - in: query
          name: search
          schema:
            type: string
          description: Case-insensitive search across entityType, eventType, originalEntityId
      responses:
        '200':
          description: Paginated entity events
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatisticsEntityEventListDto'
        '403':
          description: Access denied to requested client
  /admin/webhooks/event-types:
    get:
      tags: [Admin]
      summary: List webhook event types (admin)
      operationId: listAgenstraWebhookEventTypes
      responses:
        '200':
          description: Event catalog
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookEventTypeResponseDto'
  /admin/webhooks:
    get:
      tags: [Admin]
      summary: List webhook endpoints (admin)
      operationId: listAgenstraWebhookEndpoints
      parameters:
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Webhook endpoints
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WebhookEndpointResponseDto'
    post:
      tags: [Admin]
      summary: Create webhook endpoint (admin)
      operationId: createAgenstraWebhookEndpoint
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateWebhookEndpointDto'
      responses:
        '201':
          description: Created endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointResponseDto'
  /admin/webhooks/{id}:
    get:
      tags: [Admin]
      summary: Get webhook endpoint (admin)
      operationId: getAgenstraWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Webhook endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointResponseDto'
    put:
      tags: [Admin]
      summary: Update webhook endpoint (admin)
      operationId: updateAgenstraWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateWebhookEndpointDto'
      responses:
        '200':
          description: Updated endpoint
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookEndpointResponseDto'
    delete:
      tags: [Admin]
      summary: Delete webhook endpoint (admin)
      operationId: deleteAgenstraWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204':
          description: Deleted
  /admin/webhooks/{id}/test:
    post:
      tags: [Admin]
      summary: Send test webhook delivery (admin)
      operationId: testAgenstraWebhookEndpoint
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Delivery result
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WebhookDeliveryResponseDto'
  /admin/webhooks/{id}/deliveries:
    get:
      tags: [Admin]
      summary: List webhook deliveries (admin)
      operationId: listAgenstraWebhookDeliveries
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          schema: { type: integer, default: 10 }
        - name: offset
          in: query
          schema: { type: integer, default: 0 }
      responses:
        '200':
          description: Paginated deliveries
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedWebhookDeliveriesResponseDto'
  /admin/updates:
    get:
      tags: [Admin]
      summary: Get full application updates state (admin)
      operationId: getAgenstraUpdatesFullState
      responses:
        '200':
          description: Full updates state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatesFullStateDto'
  /admin/updates/status:
    get:
      tags: [Admin]
      summary: Get application updates status summary (admin)
      operationId: getAgenstraUpdatesStatusSummary
      responses:
        '200':
          description: Updates status summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdatesStatusSummaryDto'
  /admin/updates/check:
    post:
      tags: [Admin]
      summary: Trigger application update check (admin)
      operationId: triggerAgenstraUpdateCheck
      responses:
        '202':
          description: Update check enqueued
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UpdateCheckTriggerResultDto'
  /otel/metrics:
    get:
      summary: Prometheus metrics scrape
      description: |
        OpenTelemetry Prometheus exposition endpoint. Mounted at the HTTP root, **outside** the `/api` global prefix.
        Path is configurable via `OTEL_METRICS_PATH` (default `/otel/metrics`). Requires HTTP Basic authentication
        with `OTEL_USERNAME` and `OTEL_PASSWORD`. Disabled when `OTEL_ENABLED` is not exactly `true`.
      operationId: getOtelMetrics
      servers:
        - url: http://localhost:3100
      security:
        - basicAuth: []
      responses:
        '200':
          description: Prometheus text exposition format
          content:
            text/plain:
              schema:
                type: string
        '401':
          description: Missing or invalid Basic auth credentials
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    basicAuth:
      type: http
      scheme: basic
  parameters:
    fileManagerContext:
      name: context
      in: query
      required: false
      schema:
        type: string
        enum: [app, config]
        default: app
      description: |
        Proxied to agent-manager. `app` (default) is the workspace root; `config` is the provider configuration directory when supported.
        When `config` is set, the caller must have workspace management rights (same as other workspace configuration APIs).
  schemas:
    AuthenticationType:
      type: string
      enum:
        - api_key
        - keycloak
    AuthenticationMethod:
      type: string
      enum:
        - api-key
        - keycloak
        - users
      description: Server authentication method (api-key, keycloak, or users)
    LoginDto:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
    RegisterDto:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
    ConfirmEmailDto:
      type: object
      required: [email, code]
      properties:
        email:
          type: string
          format: email
          description: User's email address
        code:
          type: string
          pattern: '^[A-Z0-9]{6}$'
          description: 6-character alphanumeric confirmation code from email (A-Z, 0-9)
    RequestPasswordResetDto:
      type: object
      required: [email]
      properties:
        email:
          type: string
          format: email
    ResetPasswordDto:
      type: object
      required: [email, code, newPassword]
      properties:
        email:
          type: string
          format: email
          description: User's email address
        code:
          type: string
          pattern: '^[A-Z0-9]{6}$'
          description: 6-character alphanumeric reset code from email (A-Z, 0-9)
        newPassword:
          type: string
          minLength: 8
    ChangePasswordDto:
      type: object
      required: [currentPassword, newPassword, newPasswordConfirmation]
      properties:
        currentPassword:
          type: string
        newPassword:
          type: string
          minLength: 8
        newPasswordConfirmation:
          type: string
          minLength: 8
    ChangePasswordResponse:
      type: object
      properties:
        message:
          type: string
        access_token:
          type: string
    LogoutDto:
      type: object
      properties:
        invalidateAllSessions:
          type: boolean
          description: When true, invalidates every active JWT session for the user. Default is false (current session only).
    LoginResponse:
      type: object
      required: [access_token, user]
      properties:
        access_token:
          type: string
          description: JWT bearer token
        user:
          type: object
          required: [id, email, role]
          properties:
            id:
              type: string
              format: uuid
            email:
              type: string
            role:
              $ref: '#/components/schemas/UserRole'
    PatLoginResponse:
      allOf:
        - $ref: '#/components/schemas/LoginResponse'
        - type: object
          properties:
            scopes:
              type: array
              items:
                type: string
    ExchangePersonalAccessTokenDto:
      type: object
      required: [token]
      properties:
        token:
          type: string
          description: Full personal access token (`fp_pat_…`)
    CreatePersonalAccessTokenDto:
      type: object
      required: [name, scopes]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        scopes:
          type: array
          minItems: 1
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
    UpdatePersonalAccessTokenDto:
      type: object
      required: [name, scopes]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 255
        scopes:
          type: array
          minItems: 1
          items:
            type: string
    PersonalAccessTokenScopeDto:
      type: object
      required: [scope]
      properties:
        scope:
          type: string
    PersonalAccessTokenResponseDto:
      type: object
      required: [id, name, tokenPrefix, scopes, createdAt]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        tokenPrefix:
          type: string
        scopes:
          type: array
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
          nullable: true
        revokedAt:
          type: string
          format: date-time
          nullable: true
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
        token:
          type: string
          description: Plaintext token; only present on create
    RegisterResponse:
      type: object
      required: [user, message]
      properties:
        user:
          type: object
          required: [id, email, role]
          properties:
            id:
              type: string
              format: uuid
            email:
              type: string
            role:
              $ref: '#/components/schemas/UserRole'
        message:
          type: string
    UserRole:
      type: string
      enum:
        - user
        - admin
    UserResponseDto:
      type: object
      required: [id, email, role, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
        role:
          $ref: '#/components/schemas/UserRole'
        emailConfirmedAt:
          type: string
          format: date-time
        lockedAt:
          type:
            - string
            - 'null'
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateUserDto:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
        role:
          $ref: '#/components/schemas/UserRole'
    UpdateUserDto:
      type: object
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
        role:
          $ref: '#/components/schemas/UserRole'
    ClientResponseDto:
      type: object
      required:
        [
          id,
          name,
          endpoint,
          authenticationType,
          isAutoProvisioned,
          canManageWorkspaceConfiguration,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        endpoint:
          type: string
          format: uri
        authenticationType:
          $ref: '#/components/schemas/AuthenticationType'
        agentWsPort:
          type: integer
          minimum: 1
          maximum: 65535
          description: Optional Socket.IO port for the client's agents gateway
        config:
          $ref: '#/components/schemas/ConfigResponseDto'
          description: Configuration from the agent-manager instance (optional, may be undefined if agent-manager is unreachable)
        isAutoProvisioned:
          type: boolean
          description: Indicates whether the client was auto-provisioned via server provisioning. Used to determine if server info endpoints should be called.
        canManageWorkspaceConfiguration:
          type: boolean
          description: |
            True if the current caller may change prototype autonomy, agent environment variables, agent CRUD,
            and workspace (client) update/delete. Requires global admin, workspace creator, client_users admin,
            API key authentication, or api-key server mode.
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateClientDto:
      type: object
      required: [name, endpoint, authenticationType]
      properties:
        name:
          type: string
        description:
          type: string
        endpoint:
          type: string
          format: uri
        authenticationType:
          $ref: '#/components/schemas/AuthenticationType'
        apiKey:
          type: string
          description: API key (optional, will be auto-generated if not provided for API_KEY type)
        keycloakClientId:
          type: string
          description: Keycloak client ID (required for KEYCLOAK authentication type)
        keycloakClientSecret:
          type: string
          description: Keycloak client secret (required for KEYCLOAK authentication type)
        keycloakRealm:
          type: string
          description: Keycloak realm (optional, uses environment default if not provided)
        agentWsPort:
          type: integer
          minimum: 1
          maximum: 65535
          description: Optional Socket.IO port for the client's agents gateway (defaults to 8080 if not set)
    UpdateClientDto:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
        endpoint:
          type: string
          format: uri
        authenticationType:
          $ref: '#/components/schemas/AuthenticationType'
        apiKey:
          type: string
          description: API key (can be updated but will never be included in responses)
        keycloakClientId:
          type: string
          description: Keycloak client ID
        keycloakClientSecret:
          type: string
          description: Keycloak client secret
        keycloakRealm:
          type: string
          description: Keycloak realm
        agentWsPort:
          type: integer
          minimum: 1
          maximum: 65535
          description: Optional Socket.IO port for the client's agents gateway
    CreateClientResponseDto:
      allOf:
        - $ref: '#/components/schemas/ClientResponseDto'
        - type: object
          properties:
            apiKey:
              type: string
              description: The API key for the client (only for API_KEY authentication type, only returned once during creation)
    CreateAgentDto:
      type: object
      required: [name]
      properties:
        name:
          type: string
        description:
          type: string
        gitRepositorySetupMode:
          type: string
          enum: [clone, empty]
          description: How to initialize the agent workspace Git repository
        gitRepositoryUrl:
          type: [string, 'null']
          description: Git repository URL to clone when creating the agent
        createVirtualWorkspace:
          type: boolean
          default: true
          description: Whether to create a VNC virtual workspace container (defaults to true)
        createSshConnection:
          type: boolean
          default: true
          description: Whether to create an SSH connection container (defaults to true)
    UpdateAgentDto:
      type: object
      properties:
        name:
          type: string
        description:
          type: string
    AgentModelsResponseDto:
      type: object
      additionalProperties:
        type: string
      description: Model catalog for an agent; keys are model identifiers, values are display names
    AgentResponseDto:
      type: object
      required: [id, name, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        description:
          type: string
        agentType:
          type: string
          description: Agent provider type identifier
        capabilities:
          $ref: '#/components/schemas/AgentTypeCapabilities'
        git:
          type: [object, 'null']
          properties:
            repositoryUrl:
              type: string
              description: Git repository URL associated with the agent (clone mode only)
            setupMode:
              type: string
              enum: [clone, empty]
              description: How the workspace repository was initialized
          description: Git information when the agent has an associated repository or empty local repo
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateAgentResponseDto:
      allOf:
        - $ref: '#/components/schemas/AgentResponseDto'
        - type: object
          required: [password]
          properties:
            password:
              type: string
    AgentTypeInfo:
      type: object
      required: [type, displayName, capabilities]
      properties:
        type:
          type: string
          description: The unique type identifier (e.g., 'cursor', 'opencode')
        displayName:
          type: string
          description: Human-readable display name (e.g., 'Cursor', 'OpenCode')
        capabilities:
          $ref: '#/components/schemas/AgentTypeCapabilities'
    AgentTypeCapabilities:
      type: object
      required:
        [supportsChat, supportsStreaming, supportsToolEvents, supportsQuestions]
      properties:
        transport:
          type: string
          enum: [acp]
          description: Wire transport for agent messaging (Agent Client Protocol over stdio)
        supportsChat:
          type: boolean
        supportsStreaming:
          type: boolean
        supportsToolEvents:
          type: boolean
        supportsQuestions:
          type: boolean
    ConfigResponseDto:
      type: object
      required: [agentTypes]
      properties:
        gitRepositoryUrl:
          type: [string, 'null']
          description: Git repository URL used for cloning repositories when creating agents
        gitRepositorySetupMode:
          type: string
          enum: [clone, empty]
          description: Default setup mode for new agents on this instance
        agentTypes:
          type: array
          items:
            $ref: '#/components/schemas/AgentTypeInfo'
          description: Array of available agent provider types with display names
    FileContentDto:
      type: object
      required: [content, encoding]
      properties:
        content:
          type: string
          description: File content as base64-encoded string (supports both text and binary files)
        encoding:
          type: string
          enum: [utf-8, base64]
          description: Encoding indicator - 'utf-8' for text files, 'base64' for binary files
    FileNodeDto:
      type: object
      required: [name, type, path]
      properties:
        name:
          type: string
          description: File or directory name
        type:
          type: string
          enum: [file, directory]
          description: Node type
        path:
          type: string
          description: Relative path from /app
        size:
          type: [integer, 'null']
          description: File size in bytes (only for files)
        modifiedAt:
          type: [string, 'null']
          format: date-time
          description: Last modification timestamp
    WriteFileDto:
      type: object
      required: [content]
      properties:
        content:
          type: string
          description: File content as base64-encoded string (supports both text and binary files)
        encoding:
          type: string
          enum: [utf-8, base64]
          description: Optional encoding indicator - 'utf-8' for text files, 'base64' for binary files (defaults to 'utf-8')
    CreateFileDto:
      type: object
      required: [type]
      properties:
        type:
          type: string
          enum: [file, directory]
          description: Type of node to create
        content:
          type: [string, 'null']
          description: Optional content for file creation (base64-encoded)
    MoveFileDto:
      type: object
      required: [destination]
      properties:
        destination:
          type: string
          description: Destination path relative to /app (supports nested paths)
    GitStatusDto:
      type: object
      required:
        [
          currentBranch,
          isClean,
          hasUnpushedCommits,
          aheadCount,
          behindCount,
          files,
        ]
      properties:
        currentBranch:
          type: string
          description: Current branch name
        isClean:
          type: boolean
          description: Whether the working tree is clean (no changes)
        hasUnpushedCommits:
          type: boolean
          description: Whether there are unpushed commits (ahead of remote)
        aheadCount:
          type: integer
          description: Number of commits ahead of remote
        behindCount:
          type: integer
          description: Number of commits behind remote
        files:
          type: array
          items:
            $ref: '#/components/schemas/GitFileStatusDto'
          description: List of files with their git status
    GitFileStatusDto:
      type: object
      required: [path, status, type]
      properties:
        path:
          type: string
          description: File path relative to repository root
        status:
          type: string
          description: Git status code (e.g., 'M', 'A', 'D', '??', 'MM', etc.)
        type:
          type: string
          enum: [staged, unstaged, untracked, both]
          description: Status type
        isBinary:
          type: boolean
          description: Whether this is a binary file
        size:
          type: integer
          description: File size in bytes (for binary files)
    GitBranchDto:
      type: object
      required: [name, ref, isCurrent, isRemote, commit, message]
      properties:
        name:
          type: string
          description: Branch name (without refs/heads/ prefix)
        ref:
          type: string
          description: Full branch reference (e.g., 'refs/heads/main')
        isCurrent:
          type: boolean
          description: Whether this is the current branch
        isRemote:
          type: boolean
          description: Whether this is a remote branch
        remote:
          type: string
          description: Remote name (if remote branch)
        commit:
          type: string
          description: Commit hash (short)
        message:
          type: string
          description: Commit message (first line)
        aheadCount:
          type: integer
          description: Number of commits ahead of remote (for local branches)
        behindCount:
          type: integer
          description: Number of commits behind remote (for local branches)
    GitDiffDto:
      type: object
      required: [path, originalContent, modifiedContent, encoding, isBinary]
      properties:
        path:
          type: string
          description: File path relative to repository root
        originalContent:
          type: string
          description: Original file content (base64-encoded for text, empty for binary)
        modifiedContent:
          type: string
          description: Modified file content (base64-encoded for text, empty for binary)
        encoding:
          type: string
          enum: [utf-8, base64]
          description: Encoding indicator
        isBinary:
          type: boolean
          description: Whether this is a binary file
        originalSize:
          type: integer
          description: Original file size in bytes (for binary files)
        modifiedSize:
          type: integer
          description: Modified file size in bytes (for binary files)
    StageFilesDto:
      type: object
      required: [files]
      properties:
        files:
          type: array
          items:
            type: string
          description: Array of file paths to stage (empty array stages all)
    UnstageFilesDto:
      type: object
      required: [files]
      properties:
        files:
          type: array
          items:
            type: string
          description: Array of file paths to unstage (empty array unstages all)
    CommitDto:
      type: object
      required: [message]
      properties:
        message:
          type: string
          description: Commit message
    PushOptionsDto:
      type: object
      properties:
        force:
          type: boolean
          description: >
            When true, performs a `git push --force-with-lease`. Defaults to false.
    CreateBranchDto:
      type: object
      required: [name]
      properties:
        name:
          type: string
          description: Branch name (will be prefixed with conventional commit prefix if useConventionalPrefix is true)
        useConventionalPrefix:
          type: boolean
          description: Whether to use conventional commit prefix (feat/, fix/, chore/, etc.)
        conventionalType:
          type: string
          enum: [feat, fix, chore, docs, style, refactor, test, perf]
          description: Conventional commit type (only used if useConventionalPrefix is true)
        baseBranch:
          type: string
          description: Base branch to create from (defaults to current branch)
    RebaseDto:
      type: object
      required: [branch]
      properties:
        branch:
          type: string
          description: Branch name to rebase onto
    ResolveConflictDto:
      type: object
      required: [path, strategy]
      properties:
        path:
          type: string
          description: File path with conflict (relative to repository root)
        strategy:
          type: string
          enum: [yours, mine, both]
          description: Merge strategy - 'yours' (accept incoming), 'mine' (accept current), 'both' (keep both)
    ProvisioningProviderInfo:
      type: object
      required: [type, displayName]
      properties:
        type:
          type: string
          enum: [hetzner, digital-ocean]
          description: The unique provider type identifier (e.g., 'hetzner', 'digital-ocean')
        displayName:
          type: string
          description: Human-readable display name (e.g., 'Hetzner Cloud', 'DigitalOcean')
    ProviderLocation:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
          description: Technical slug used for provisioning (e.g. fsn1, fra1)
        name:
          type: string
          description: Human-readable display name from provider API or static fallback
        city:
          type: string
          description: City when provided by the provider API (Hetzner)
        country:
          type: string
          description: ISO country code when provided by the provider API
    ServerType:
      type: object
      required: [id, name, cores, memory, disk]
      properties:
        id:
          type: string
          description: Unique identifier for the server type (e.g., 'cx11', 'cpx11')
        name:
          type: string
          description: Human-readable name (e.g., 'CX11', 'CPX11')
        cores:
          type: integer
          description: Number of CPU cores
        memory:
          type: number
          description: Amount of RAM in GB
        disk:
          type: integer
          description: Disk size in GB
        priceMonthly:
          type: number
          description: Price per month in EUR (or provider's currency)
        priceHourly:
          type: number
          description: Price per hour in EUR (or provider's currency)
        description:
          type: string
          description: Additional description or notes
    ProvisionServerDto:
      type: object
      required: [providerType, serverType, name, authenticationType]
      properties:
        providerType:
          type: string
          enum: [hetzner, digital-ocean]
          description: The provisioning provider type (e.g., 'hetzner', 'digital-ocean')
        serverType:
          type: string
          description: Server type identifier (e.g., 'cx11', 'cpx11' for Hetzner; 's-1vcpu-1gb', 's-2vcpu-2gb' for DigitalOcean)
        name:
          type: string
          description: Server name/label
        description:
          type: string
          description: Optional description
        location:
          type: string
          description: Location/datacenter identifier (e.g., 'nbg1', 'fsn1' for Hetzner; 'fra1', 'nyc3' for DigitalOcean)
        authenticationType:
          $ref: '#/components/schemas/AuthenticationType'
        apiKey:
          type: string
          description: API key (optional, will be auto-generated if not provided for API_KEY type)
        keycloakClientId:
          type: string
          description: Keycloak client ID (required for KEYCLOAK authentication type)
        keycloakClientSecret:
          type: string
          description: Keycloak client secret (required for KEYCLOAK authentication type)
        keycloakRealm:
          type: string
          description: Keycloak realm (optional, uses environment default if not provided)
        agentWsPort:
          type: integer
          minimum: 1
          maximum: 65535
          description: Optional Socket.IO port for the client's agents gateway (defaults to 8080 if not set)
        gitRepositorySetupMode:
          type: string
          enum: [clone, empty]
          description: |
            Default Git workspace setup for agents on the provisioned instance. `empty` runs `git init` without a remote;
            omit git URL and credentials when using empty mode.
        gitRepositoryUrl:
          type: string
          description: Optional Git repository URL for agent workspace (clone mode)
        gitUsername:
          type: string
        gitToken:
          type: string
        gitPassword:
          type: string
        gitPrivateKey:
          type: string
        cursorApiKey:
          type: string
        agentDefaultImage:
          type: string
        keycloakAuthServerUrl:
          type: string
        autoEnrichEnabledGlobal:
          type: string
          enum: ['true', 'false']
          description: >
            Sets AUTO_ENRICH_ENABLED_GLOBAL on the provisioned agent-manager (`false` disables prompt auto-enrichment
            for that workspace unless a chat turn explicitly enables it).
        autoEnrichVectorMaxCosineDistance:
          type: number
          minimum: 0
          maximum: 2
          description: >
            Sets AUTO_ENRICH_VECTOR_MAX_COSINE_DISTANCE on the provisioned agent-manager (pgvector cosine distance `<=>`;
            only knowledge chunks with distance <= this value qualify for vector auto-enrichment; default on controller is 1).
    ProvisionedServerResponseDto:
      allOf:
        - $ref: '#/components/schemas/ClientResponseDto'
        - type: object
          required: [providerType, serverId, serverName, publicIp, serverStatus]
          properties:
            providerType:
              type: string
              enum: [hetzner, digital-ocean]
              description: Provider type used for provisioning (e.g., 'hetzner', 'digital-ocean')
            serverId:
              type: string
              description: Provider-specific server ID
            serverName:
              type: string
              description: Server name
            publicIp:
              type: string
              description: Public IP address of the server
            privateIp:
              type: string
              description: Private IP address of the server (if applicable)
            serverStatus:
              type: string
              description: Server status (e.g., 'running', 'starting')
    ServerInfo:
      type: object
      required: [serverId, providerType]
      properties:
        serverId:
          type: string
          description: Provider-specific server ID
        serverName:
          type: string
          description: Server name
        publicIp:
          type: string
          description: Public IP address of the server
        privateIp:
          type: string
          description: Private IP address of the server (if applicable)
        serverStatus:
          type: string
          description: Server status (e.g., 'running', 'starting')
        providerType:
          type: string
          enum: [hetzner, digital-ocean]
          description: Provider type used for provisioning (e.g., 'hetzner', 'digital-ocean')
    CreateEnvironmentVariableDto:
      type: object
      required: [variable, content]
      properties:
        variable:
          type: string
          description: Environment variable name
        content:
          type: string
          description: Environment variable content
    UpdateEnvironmentVariableDto:
      type: object
      required: [variable, content]
      properties:
        variable:
          type: string
          description: Environment variable name
        content:
          type: string
          description: Environment variable content
    EnvironmentVariableResponseDto:
      type: object
      required: [id, agentId, variable, content, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
        agentId:
          type: string
          format: uuid
        variable:
          type: string
        content:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    WorkspaceConfigurationSettingKey:
      type: string
      enum:
        [
          gitRepositoryUrl,
          gitUsername,
          gitToken,
          gitPassword,
          gitPrivateKey,
          cursorApiKey,
          agentDefaultImage,
          autoEnrichEnabledGlobal,
          autoEnrichVectorMaxCosineDistance,
        ]
    UpsertWorkspaceConfigurationOverrideDto:
      type: object
      required: [value]
      properties:
        value:
          type: string
          description: Override value
    WorkspaceConfigurationSettingResponseDto:
      type: object
      required: [settingKey, envVarName, source, hasOverride]
      properties:
        settingKey:
          $ref: '#/components/schemas/WorkspaceConfigurationSettingKey'
        envVarName:
          type: string
        value:
          type: string
        source:
          type: string
          enum: [override, default_env, unset]
        hasOverride:
          type: boolean
    AddClientUserDto:
      type: object
      required: [email, role]
      properties:
        email:
          type: string
          format: email
          description: Email address of the user to add
        role:
          type: string
          enum: [admin, user]
          description: Role for the user in this client (admin or user)
    ClientUserResponseDto:
      type: object
      required: [id, userId, clientId, role, createdAt, updatedAt]
      properties:
        id:
          type: string
          format: uuid
          description: The UUID of the client_users relationship
        userId:
          type: string
          format: uuid
          description: The UUID of the user
        clientId:
          type: string
          format: uuid
          description: The UUID of the client
        role:
          type: string
          enum: [admin, user]
          description: Role for the user in this client
        userEmail:
          type: string
          format: email
          description: Email address of the user
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    StatisticsSummaryDto:
      type: object
      required:
        [
          totalMessages,
          totalWords,
          totalChars,
          avgWordsPerMessage,
          autoEnrichmentRuns,
          autoEnrichmentContexts,
          autoEnrichmentChars,
          filterDropCount,
          filterTypesBreakdown,
        ]
      properties:
        totalMessages:
          type: integer
        totalWords:
          type: integer
        totalChars:
          type: integer
        avgWordsPerMessage:
          type: number
        autoEnrichmentRuns:
          type: integer
          description: Number of turns where auto-enrichment injected at least one context section
        autoEnrichmentContexts:
          type: integer
          description: Total number of auto-injected context sections
        autoEnrichmentChars:
          type: integer
          description: Total character volume of auto-injected context sections
        filterDropCount:
          type: integer
        filterTypesBreakdown:
          type: array
          items:
            type: object
            required: [filterType, direction, count]
            properties:
              filterType:
                type: string
              direction:
                type: string
                enum: [incoming, outgoing]
              count:
                type: integer
        filterFlagCount:
          type: integer
          description: Count of messages flagged/modified (excluding dropped)
        filterFlagsBreakdown:
          type: array
          items:
            type: object
            required: [filterType, direction, count]
            properties:
              filterType:
                type: string
              direction:
                type: string
                enum: [incoming, outgoing]
              count:
                type: integer
        series:
          type: array
          items:
            type: object
            properties:
              period:
                type: string
                format: date-time
              count:
                type: integer
              wordCount:
                type: integer
              charCount:
                type: integer
    StatisticsChatIoDto:
      type: object
      required:
        [
          id,
          clientId,
          direction,
          interactionKind,
          wordCount,
          charCount,
          occurredAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        agentId:
          type: string
          format: uuid
        clientName:
          type: string
          description: Client name for display; link to chat when agentId present
        agentName:
          type: string
          description: Agent name for display; link to chat when agentId present
        originalUserId:
          type: string
          format: uuid
          description: Original user ID from identity system; use to resolve user email
        direction:
          type: string
          enum: [input, output]
        interactionKind:
          type: string
          enum:
            [
              chat,
              prompt_enhancement,
              ticket_body_generation,
              auto_context_enrichment,
              autonomous_ticket_run,
              autonomous_ticket_run_turn,
              autonomous_ticket_commit_message,
            ]
          description: Normal chat vs prompt enhancement (magic wand) metrics
        wordCount:
          type: integer
        charCount:
          type: integer
        occurredAt:
          type: string
          format: date-time
    StatisticsChatIoListDto:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/StatisticsChatIoDto'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    StatisticsFilterDropDto:
      type: object
      required:
        [
          id,
          clientId,
          filterType,
          filterDisplayName,
          direction,
          wordCount,
          charCount,
          occurredAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        agentId:
          type: string
          format: uuid
        clientName:
          type: string
          description: Client name for display; link to chat when agentId present
        agentName:
          type: string
          description: Agent name for display; link to chat when agentId present
        originalUserId:
          type: string
          format: uuid
          description: Original user ID from identity system; use to resolve user email
        filterType:
          type: string
        filterDisplayName:
          type: string
        filterReason:
          type: string
        direction:
          type: string
          enum: [incoming, outgoing]
        wordCount:
          type: integer
        charCount:
          type: integer
        occurredAt:
          type: string
          format: date-time
    StatisticsFilterDropListDto:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/StatisticsFilterDropDto'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    StatisticsFilterFlagDto:
      type: object
      required:
        [
          id,
          clientId,
          filterType,
          filterDisplayName,
          direction,
          wordCount,
          charCount,
          occurredAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        agentId:
          type: string
          format: uuid
        clientName:
          type: string
          description: Client name for display; link to chat when agentId present
        agentName:
          type: string
          description: Agent name for display; link to chat when agentId present
        originalUserId:
          type: string
          format: uuid
          description: Original user ID from identity system; use to resolve user email
        filterType:
          type: string
        filterDisplayName:
          type: string
        filterReason:
          type: string
        direction:
          type: string
          enum: [incoming, outgoing]
        wordCount:
          type: integer
        charCount:
          type: integer
        occurredAt:
          type: string
          format: date-time
    StatisticsFilterFlagListDto:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/StatisticsFilterFlagDto'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    StatisticsEntityEventDto:
      type: object
      required: [id, entityType, eventType, originalEntityId, occurredAt]
      properties:
        id:
          type: string
          format: uuid
        entityType:
          type: string
          enum: [user, client, agent, client_user, provisioning_reference]
        eventType:
          type: string
          enum: [created, updated, deleted]
        originalEntityId:
          type: string
          format: uuid
        originalUserId:
          type: string
          format: uuid
          description: Original user ID of the actor who performed the event
        clientId:
          type: string
          format: uuid
          description: Client ID for agent events; link to chat when agentId present
        agentId:
          type: string
          format: uuid
          description: Agent ID for agent events; link to chat when present
        clientName:
          type: string
          description: Client name for agent events; display "client --> agent"
        agentName:
          type: string
          description: Agent name for agent events; display "client --> agent"
        occurredAt:
          type: string
          format: date-time
    StatisticsEntityEventListDto:
      type: object
      required: [data, total, limit, offset]
      properties:
        data:
          type: array
          items:
            $ref: '#/components/schemas/StatisticsEntityEventDto'
        total:
          type: integer
        limit:
          type: integer
        offset:
          type: integer
    TicketPriority:
      type: string
      enum: [low, medium, high, critical]
    TicketStatus:
      type: string
      enum: [draft, todo, in_progress, prototype, done, closed]
    TicketActorType:
      type: string
      enum: [human, ai, system]
    TicketActionType:
      type: string
      description: Ticket activity row discriminator; stored as varchar(64) in the database.
      enum:
        - CREATED
        - FIELD_UPDATED
        - STATUS_CHANGED
        - PRIORITY_CHANGED
        - WORKSPACE_MOVED
        - PARENT_CHANGED
        - DELETED
        - COMMENT_ADDED
        - CONTENT_APPLIED_FROM_AI
        - BODY_GENERATION_STARTED
        - PROTOTYPE_PROMPT_GENERATED
        - AUTOMATION_CLAIMED
        - AUTOMATION_STARTED
        - AUTOMATION_SUCCEEDED
        - AUTOMATION_FAILED
        - AUTOMATION_TIMED_OUT
        - AUTOMATION_ESCALATED
        - AUTOMATION_REQUEUED
        - AUTOMATION_APPROVAL_INVALIDATED
        - AUTOMATION_CANCELLED
        - AUTOMATION_APPROVED
        - AUTOMATION_UNAPPROVED
        - AUTOMATION_ELIGIBILITY_CHANGED
        - AUTOMATION_APPROVAL_REQUIREMENT_CHANGED
        - AUTOMATION_SETTINGS_UPDATED
    TicketMarkdownCheckboxTaskCountsDto:
      type: object
      required: [open, done]
      properties:
        open:
          type: integer
          minimum: 0
          description: Count of `[ ]` style open task checkboxes in the described scope.
        done:
          type: integer
          minimum: 0
          description: Count of `[x]` or `[X]` done checkboxes in the described scope.
    TicketTasksDto:
      type: object
      required: [open, done, children]
      properties:
        open:
          type: integer
          minimum: 0
          description: Open checkboxes in this ticket's `content`.
        done:
          type: integer
          minimum: 0
          description: Done checkboxes in this ticket's `content`.
        children:
          description: |
            Sums of open/done checkboxes on all descendant tickets (direct and nested), excluding this ticket's body.
          $ref: '#/components/schemas/TicketMarkdownCheckboxTaskCountsDto'
    TicketShasDto:
      type: object
      required: [short, long]
      properties:
        short:
          type: string
          description: Stable short ticket reference derived from ticket id hash.
        long:
          type: string
          description: Stable full ticket reference derived from ticket id hash.
    TicketResponseDto:
      type: object
      required:
        [
          id,
          shas,
          clientId,
          title,
          priority,
          status,
          automationEligible,
          createdAt,
          updatedAt,
          tasks,
        ]
      properties:
        id:
          type: string
          format: uuid
        shas:
          $ref: '#/components/schemas/TicketShasDto'
        clientId:
          type: string
          format: uuid
        parentId:
          type: string
          format: uuid
        title:
          type: string
        content:
          type: string
        priority:
          $ref: '#/components/schemas/TicketPriority'
        status:
          $ref: '#/components/schemas/TicketStatus'
        createdByUserId:
          type: string
          format: uuid
        createdByEmail:
          type: string
        preferredChatAgentId:
          type: string
          format: uuid
          description: Preferred workspace agent for chat/AI when viewing this ticket (agent-manager agent id).
        automationEligible:
          type: boolean
          description: Whether autonomous prototyping is enabled for this ticket (ticket_automation.eligible).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        tasks:
          $ref: '#/components/schemas/TicketTasksDto'
        children:
          type: array
          items:
            $ref: '#/components/schemas/TicketResponseDto'
    CreateTicketDto:
      type: object
      required: [title]
      properties:
        clientId:
          type: string
          format: uuid
        parentId:
          type: string
          format: uuid
        title:
          type: string
        content:
          type: string
        priority:
          $ref: '#/components/schemas/TicketPriority'
        status:
          $ref: '#/components/schemas/TicketStatus'
        creationTemplate:
          type: string
          enum: [empty, specification]
          description: |
            Not persisted. `specification` only allowed when creating a root ticket (omit `parentId`);
            creates Proposal, Specifications, Technical design, and Implementation plan subtasks.
    CreateTicketResponseDto:
      allOf:
        - $ref: '#/components/schemas/TicketResponseDto'
        - type: object
          properties:
            createdChildTickets:
              type: array
              description: Present when `creationTemplate` was `specification` (four subtasks).
              items:
                $ref: '#/components/schemas/TicketResponseDto'
    MigrateTicketDto:
      type: object
      required: [targetClientId]
      properties:
        targetClientId:
          type: string
          format: uuid
          description: Destination workspace (client) id
    MigrateTicketResponseDto:
      type: object
      required: [ticket]
      properties:
        ticket:
          $ref: '#/components/schemas/TicketResponseDto'
          description: Root ticket after migration, including nested `children` when present
    UpdateTicketDto:
      type: object
      properties:
        clientId:
          type: string
          format: uuid
        parentId:
          type: string
          format: uuid
        title:
          type: string
        content:
          type: string
        priority:
          $ref: '#/components/schemas/TicketPriority'
        status:
          $ref: '#/components/schemas/TicketStatus'
        preferredChatAgentId:
          type: string
          format: uuid
          description: Persist preferred workspace agent for chat/AI; send null to clear.
    TicketCommentResponseDto:
      type: object
      required: [id, ticketId, body, createdAt]
      properties:
        id:
          type: string
          format: uuid
        ticketId:
          type: string
          format: uuid
        authorUserId:
          type: string
          format: uuid
        authorEmail:
          type: string
        body:
          type: string
        createdAt:
          type: string
          format: date-time
    CreateTicketCommentDto:
      type: object
      required: [body]
      properties:
        body:
          type: string
    TicketActivityResponseDto:
      type: object
      required: [id, ticketId, occurredAt, actorType, actionType, payload]
      properties:
        id:
          type: string
          format: uuid
        ticketId:
          type: string
          format: uuid
        occurredAt:
          type: string
          format: date-time
        actorType:
          $ref: '#/components/schemas/TicketActorType'
        actorUserId:
          type: string
          format: uuid
        actorEmail:
          type: string
        actionType:
          $ref: '#/components/schemas/TicketActionType'
        payload:
          type: object
          additionalProperties: true
          description: >
            Shape depends on actionType (e.g. automation eligibility changes include { eligible: boolean };
            AUTOMATION_SETTINGS_UPDATED includes { fields: string[] };
            AUTOMATION_APPROVAL_INVALIDATED may include { reason: string, fields: string[] }).
    VerifierCommandEntryDto:
      type: object
      required: [cmd]
      properties:
        cmd:
          type: string
          maxLength: 2048
        cwd:
          type: string
          maxLength: 2048
    TicketVerifierProfileDto:
      type: object
      required: [commands]
      properties:
        commands:
          type: array
          maxItems: 32
          items:
            $ref: '#/components/schemas/VerifierCommandEntryDto'
    UpdateTicketAutomationDto:
      type: object
      properties:
        eligible:
          type: boolean
        allowedAgentIds:
          type: array
          items:
            type: string
            format: uuid
        includeWorkspaceContext:
          type: boolean
        contextEnvironmentIds:
          type: array
          items:
            type: string
            format: uuid
        autoEnrichmentEnabled:
          type: boolean
        verifierProfile:
          $ref: '#/components/schemas/TicketVerifierProfileDto'
        requiresApproval:
          type: boolean
        defaultBranchOverride:
          type: string
          maxLength: 256
        automationBranchStrategy:
          type: string
          enum: [reuse_per_ticket, new_per_run]
          description: >
            `reuse_per_ticket` (default): reuse `automation/ticket-{ticketId}` when it exists. `new_per_run`: create a
            fresh `automation/{runIdPrefix}` branch for every run (legacy behaviour).
        forceNewAutomationBranchNextRun:
          type: boolean
          description: >
            When true with `reuse_per_ticket`, the next automation run uses an ephemeral branch; cleared when that run
            starts branch setup. Lets operators force a clean branch without switching strategy permanently.
    TicketAutomationResponseDto:
      type: object
      required:
        - ticketId
        - eligible
        - allowedAgentIds
        - includeWorkspaceContext
        - contextEnvironmentIds
        - autoEnrichmentEnabled
        - verifierProfile
        - requiresApproval
        - approvedAt
        - approvedByUserId
        - approvalBaselineTicketUpdatedAt
        - defaultBranchOverride
        - automationBranchStrategy
        - forceNewAutomationBranchNextRun
        - nextRetryAt
        - consecutiveFailureCount
        - createdAt
        - updatedAt
      properties:
        ticketId:
          type: string
          format: uuid
        eligible:
          type: boolean
        allowedAgentIds:
          type: array
          items:
            type: string
            format: uuid
        includeWorkspaceContext:
          type: boolean
        contextEnvironmentIds:
          type: array
          items:
            type: string
            format: uuid
        autoEnrichmentEnabled:
          type: boolean
        verifierProfile:
          oneOf:
            - $ref: '#/components/schemas/TicketVerifierProfileDto'
            - type: 'null'
        requiresApproval:
          type: boolean
        approvedAt:
          type: string
          format: date-time
        approvedByUserId:
          type: string
          format: uuid
        approvalBaselineTicketUpdatedAt:
          type: string
          format: date-time
        defaultBranchOverride:
          type: string
        automationBranchStrategy:
          type: string
          enum: [reuse_per_ticket, new_per_run]
        forceNewAutomationBranchNextRun:
          type: boolean
        nextRetryAt:
          type: string
          format: date-time
        consecutiveFailureCount:
          type: integer
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TicketAutomationRunStatus:
      type: string
      enum:
        [pending, running, succeeded, failed, timed_out, escalated, cancelled]
    TicketAutomationRunPhase:
      type: string
      enum: [pre_improve, workspace_prep, agent_loop, verify, finalize]
    TicketAutomationCancellationReason:
      type: string
      enum: [user_request, approval_invalidated, lease_expired, system_shutdown]
    TicketAutomationRunStepResponseDto:
      type: object
      required: [id, stepIndex, phase, kind, payload, excerpt, createdAt]
      properties:
        id:
          type: string
          format: uuid
        stepIndex:
          type: integer
        phase:
          type: string
        kind:
          type: string
        payload:
          type: object
          additionalProperties: true
        excerpt:
          type: string
        createdAt:
          type: string
          format: date-time
    TicketAutomationRunResponseDto:
      type: object
      required:
        - id
        - ticketId
        - clientId
        - agentId
        - status
        - phase
        - ticketStatusBefore
        - branchName
        - baseBranch
        - baseSha
        - startedAt
        - finishedAt
        - updatedAt
        - iterationCount
        - completionMarkerSeen
        - verificationPassed
        - failureCode
        - summary
        - cancelRequestedAt
        - cancelledByUserId
        - cancellationReason
      properties:
        id:
          type: string
          format: uuid
        ticketId:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        agentId:
          type: string
          format: uuid
        status:
          $ref: '#/components/schemas/TicketAutomationRunStatus'
        phase:
          $ref: '#/components/schemas/TicketAutomationRunPhase'
        ticketStatusBefore:
          type: string
        branchName:
          type: string
        baseBranch:
          type: string
        baseSha:
          type: string
        startedAt:
          type: string
          format: date-time
        finishedAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        iterationCount:
          type: integer
        completionMarkerSeen:
          type: boolean
        verificationPassed:
          type: boolean
        failureCode:
          type: string
          description: >
            Present when a run ends unsuccessfully. Includes values such as verify_command_failed,
            commit_failed (git commit after successful verification), push_failed (git push after that commit),
            agent_no_completion_marker, marker_without_verify, agent_provider_error, vcs_dirty_workspace,
            and others defined by the controller.
        summary:
          type: object
          additionalProperties: true
        cancelRequestedAt:
          type: string
          format: date-time
        cancelledByUserId:
          type: string
          format: uuid
        cancellationReason:
          oneOf:
            - $ref: '#/components/schemas/TicketAutomationCancellationReason'
            - type: 'null'
        steps:
          type: array
          items:
            $ref: '#/components/schemas/TicketAutomationRunStepResponseDto'
    TicketAutomationRunChatTicketSummaryDto:
      type: object
      description: Ticket fields included in chat timeline bubbles for automation runs.
      required:
        [
          id,
          clientId,
          title,
          priority,
          status,
          automationEligible,
          preferredChatAgentId,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        title:
          type: string
        priority:
          $ref: '#/components/schemas/TicketPriority'
        status:
          $ref: '#/components/schemas/TicketStatus'
        automationEligible:
          type: boolean
        preferredChatAgentId:
          type: string
          format: uuid
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    TicketAutomationRunChatOpenActionDto:
      type: object
      required: [type, ticketId, runId, label]
      properties:
        type:
          type: string
          enum: [openTicketAutomationRun]
        ticketId:
          type: string
          format: uuid
        runId:
          type: string
          format: uuid
        label:
          type: string
          description: Suggested button label (UI may use i18n instead).
    ContextInjectionDto:
      type: object
      properties:
        includeWorkspace:
          type: boolean
        environmentIds:
          type: array
          items:
            type: string
        ticketShas:
          type: array
          items:
            type: string
        knowledgeShas:
          type: array
          items:
            type: string
        autoEnrichmentEnabled:
          type: boolean
    TicketAutomationRunChatEventDto:
      type: object
      description: >
        Payload for WebSocket event `ticketAutomationRunChatUpsert` on namespace **clients** (not proxied from
        agent-manager). `timelineAt` uses `run.startedAt` when `hydrate` is true (historic replay after login) and
        `run.updatedAt` when `hydrate` is false (live updates). Clients merge with chat messages by this timestamp.
      required: [timelineAt, hydrate, ticket, run, actions]
      properties:
        timelineAt:
          type: string
          format: date-time
        hydrate:
          type: boolean
        ticket:
          $ref: '#/components/schemas/TicketAutomationRunChatTicketSummaryDto'
        run:
          $ref: '#/components/schemas/TicketAutomationRunResponseDto'
        actions:
          type: array
          items:
            $ref: '#/components/schemas/TicketAutomationRunChatOpenActionDto'
        contextInjection:
          $ref: '#/components/schemas/ContextInjectionDto'
    PrototypePromptResponseDto:
      type: object
      required: [prompt]
      properties:
        prompt:
          type: string
    StartBodyGenerationSessionDto:
      type: object
      properties:
        agentId:
          type: string
          format: uuid
    StartBodyGenerationSessionResponseDto:
      type: object
      required: [generationId, expiresAt, activity]
      properties:
        generationId:
          type: string
          format: uuid
        expiresAt:
          type: string
          format: date-time
        activity:
          $ref: '#/components/schemas/TicketActivityResponseDto'
    ApplyGeneratedBodyDto:
      type: object
      required: [generationId, content]
      properties:
        generationId:
          type: string
          format: uuid
        content:
          type: string
    FilterRuleSyncSummaryDto:
      type: object
      required: [pending, synced, failed]
      properties:
        pending:
          type: integer
          minimum: 0
        synced:
          type: integer
          minimum: 0
        failed:
          type: integer
          minimum: 0
    FilterRuleWorkspaceSyncDto:
      type: object
      required: [clientId, syncStatus]
      properties:
        clientId:
          type: string
          format: uuid
        syncStatus:
          type: string
          enum: [pending, synced, failed]
        lastError:
          type: string
    FilterRuleResponseDto:
      type: object
      required:
        [
          id,
          pattern,
          regexFlags,
          direction,
          filterType,
          priority,
          enabled,
          isGlobal,
          workspaceIds,
          sync,
          workspaceSync,
          createdAt,
          updatedAt,
        ]
      properties:
        id:
          type: string
          format: uuid
        pattern:
          type: string
        regexFlags:
          type: string
        direction:
          type: string
          enum: [incoming, outgoing, bidirectional]
        filterType:
          type: string
          enum: [none, filter, drop]
        replaceContent:
          type: string
        priority:
          type: integer
        enabled:
          type: boolean
        isGlobal:
          type: boolean
        workspaceIds:
          type: array
          items:
            type: string
            format: uuid
          description: Empty when isGlobal is true (applies to all workspaces)
        sync:
          $ref: '#/components/schemas/FilterRuleSyncSummaryDto'
        workspaceSync:
          type: array
          items:
            $ref: '#/components/schemas/FilterRuleWorkspaceSyncDto'
          description: Per-workspace agent-manager sync status for this rule
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateFilterRuleDto:
      type: object
      required: [pattern, direction, filterType, isGlobal]
      properties:
        pattern:
          type: string
        regexFlags:
          type: string
        direction:
          type: string
          enum: [incoming, outgoing, bidirectional]
        filterType:
          type: string
          enum: [none, filter, drop]
        replaceContent:
          type: string
        priority:
          type: integer
          minimum: 0
        enabled:
          type: boolean
        isGlobal:
          type: boolean
        workspaceIds:
          type: array
          items:
            type: string
            format: uuid
    UpdateFilterRuleDto:
      type: object
      properties:
        pattern:
          type: string
        regexFlags:
          type: string
        direction:
          type: string
          enum: [incoming, outgoing, bidirectional]
        filterType:
          type: string
          enum: [none, filter, drop]
        replaceContent:
          type: string
        priority:
          type: integer
          minimum: 0
        enabled:
          type: boolean
        isGlobal:
          type: boolean
        workspaceIds:
          type: array
          items:
            type: string
            format: uuid
    AtlassianSiteConnectionResponseDto:
      type: object
      description: Connection metadata only; API tokens are stored encrypted at rest and are never returned.
      properties:
        id:
          type: string
          format: uuid
        label:
          type: string
        baseUrl:
          type: string
        accountEmail:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateAtlassianSiteConnectionDto:
      type: object
      required: [baseUrl, accountEmail, apiToken]
      properties:
        label:
          type: string
        baseUrl:
          type: string
          format: uri
        accountEmail:
          type: string
          format: email
        apiToken:
          type: string
          description: Atlassian API token; persisted with AES-256-GCM encryption (never returned on GET list/get connection).
    UpdateAtlassianSiteConnectionDto:
      type: object
      properties:
        label:
          type: string
        baseUrl:
          type: string
          format: uri
        accountEmail:
          type: string
          format: email
        apiToken:
          type: string
          description: When omitted, token is unchanged. If set, stored with AES-256-GCM encryption.
    AtlassianConnectionTestResultDto:
      type: object
      properties:
        ok:
          type: boolean
        message:
          type: string
    ExternalImportProviderId:
      type: string
      enum: [atlassian]
    ExternalImportKind:
      type: string
      enum: [jira, confluence]
    ExternalImportConfigResponseDto:
      type: object
      properties:
        id:
          type: string
          format: uuid
        provider:
          $ref: '#/components/schemas/ExternalImportProviderId'
        importKind:
          $ref: '#/components/schemas/ExternalImportKind'
        atlassianConnectionId:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        enabled:
          type: boolean
        jiraBoardId:
          type: integer
        jql:
          type: string
        importTargetTicketStatus:
          $ref: '#/components/schemas/TicketStatus'
        confluenceSpaceKey:
          type: string
        confluenceRootPageId:
          type: string
        cql:
          type: string
        agenstraParentTicketId:
          type: string
          format: uuid
        agenstraParentFolderId:
          type: string
          format: uuid
        lastRunAt:
          type: string
          format: date-time
        lastError:
          type: string
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    CreateExternalImportConfigDto:
      type: object
      required: [provider, importKind, atlassianConnectionId, clientId]
      properties:
        provider:
          $ref: '#/components/schemas/ExternalImportProviderId'
        importKind:
          $ref: '#/components/schemas/ExternalImportKind'
        atlassianConnectionId:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        enabled:
          type: boolean
        jiraBoardId:
          type: integer
        jql:
          type: string
        importTargetTicketStatus:
          $ref: '#/components/schemas/TicketStatus'
        confluenceSpaceKey:
          type: string
        confluenceRootPageId:
          type: string
        cql:
          type: string
        agenstraParentTicketId:
          type: string
          format: uuid
        agenstraParentFolderId:
          type: string
          format: uuid
    UpdateExternalImportConfigDto:
      type: object
      properties:
        atlassianConnectionId:
          type: string
          format: uuid
        clientId:
          type: string
          format: uuid
        enabled:
          type: boolean
        jiraBoardId:
          type: integer
        jql:
          type: string
        importTargetTicketStatus:
          $ref: '#/components/schemas/TicketStatus'
        confluenceSpaceKey:
          type: string
        confluenceRootPageId:
          type: string
        cql:
          type: string
        agenstraParentTicketId:
          type: string
          format: uuid
        agenstraParentFolderId:
          type: string
          format: uuid
    WebhookEventTypeResponseDto:
      type: object
      properties:
        type: { type: string }
        description: { type: string }
    UpdateState:
      type: string
      enum: [up_to_date, update_available, unknown]
    DependencyHealthStatus:
      type: string
      enum: [healthy, degraded, unknown, not_applicable]
    InstanceDependencyHealthDto:
      type: object
      properties:
        redis: { $ref: '#/components/schemas/DependencyHealthStatus' }
        queue: { $ref: '#/components/schemas/DependencyHealthStatus' }
        database: { $ref: '#/components/schemas/DependencyHealthStatus' }
        opensearch: { $ref: '#/components/schemas/DependencyHealthStatus' }
    ChangelogEntryDto:
      type: object
      properties:
        text: { type: string }
        scope: { type: string, enum: [product, shared] }
        product: { type: string, enum: [agenstra, decabill] }
        category: { type: string }
    ScopedChangelogDto:
      type: object
      properties:
        product:
          type: array
          items: { $ref: '#/components/schemas/ChangelogEntryDto' }
        shared:
          type: array
          items: { $ref: '#/components/schemas/ChangelogEntryDto' }
    ServiceInstanceRecordDto:
      type: object
      properties:
        instanceId: { type: string }
        serviceName: { type: string }
        role: { type: string }
        hostname: { type: string }
        installedVersion: { type: string }
        updateState: { $ref: '#/components/schemas/UpdateState' }
        lastHeartbeatAt: { type: string, format: date-time }
        dependencies:
          { $ref: '#/components/schemas/InstanceDependencyHealthDto' }
    ReleaseSnapshotDto:
      type: object
      properties:
        tagName: { type: string }
        name: { type: string }
        body: { type: string }
        htmlUrl: { type: string, format: uri }
        publishedAt: { type: string, format: date-time }
        changelog:
          type: array
          items: { $ref: '#/components/schemas/ChangelogEntryDto' }
        scopedChangelog: { $ref: '#/components/schemas/ScopedChangelogDto' }
    UpdatesStatusSummaryDto:
      type: object
      properties:
        installedVersion: { type: string }
        latestVersion: { type: string, nullable: true }
        updateState: { $ref: '#/components/schemas/UpdateState' }
        lastCheckAt: { type: string, format: date-time, nullable: true }
        lastCheckStatus:
          { type: string, enum: [success, failed, pending, unknown] }
        instanceCount: { type: integer }
        outdatedInstanceCount: { type: integer }
    UpdatesFullStateDto:
      allOf:
        - $ref: '#/components/schemas/UpdatesStatusSummaryDto'
        - type: object
          properties:
            release:
              {
                $ref: '#/components/schemas/ReleaseSnapshotDto',
                nullable: true,
              }
            instances:
              type: array
              items: { $ref: '#/components/schemas/ServiceInstanceRecordDto' }
            scopedChangelog: { $ref: '#/components/schemas/ScopedChangelogDto' }
    UpdateCheckTriggerResultDto:
      type: object
      properties:
        jobId: { type: string }
        enqueuedAt: { type: string, format: date-time }
    CreateWebhookEndpointDto:
      type: object
      required: [name, url, httpMethod, subscribedEvents, authType]
      properties:
        name: { type: string }
        url: { type: string, format: uri }
        httpMethod: { type: string, enum: [POST, GET] }
        subscribedEvents:
          type: array
          items: { type: string }
        enabled: { type: boolean }
        authType:
          {
            type: string,
            enum: [none, authorization, custom_header, query_param],
          }
        authHeaderName: { type: string }
        authValue: { type: string }
        clientId: { type: string, format: uuid, nullable: true }
        deliveryLogRetentionDays: { type: integer, minimum: 1, maximum: 365 }
        deliveryLogMaxEntries: { type: integer, minimum: 1, maximum: 10000 }
    UpdateWebhookEndpointDto:
      type: object
      properties:
        name: { type: string }
        url: { type: string, format: uri }
        httpMethod: { type: string, enum: [POST, GET] }
        subscribedEvents:
          type: array
          items: { type: string }
        enabled: { type: boolean }
        authType:
          {
            type: string,
            enum: [none, authorization, custom_header, query_param],
          }
        authHeaderName: { type: string }
        authValue: { type: string }
        clientId: { type: string, format: uuid, nullable: true }
        deliveryLogRetentionDays:
          { type: integer, minimum: 1, maximum: 365, nullable: true }
        deliveryLogMaxEntries:
          { type: integer, minimum: 1, maximum: 10000, nullable: true }
    WebhookEndpointResponseDto:
      type: object
      properties:
        id: { type: string, format: uuid }
        scopeKey: { type: string }
        clientId: { type: string, format: uuid, nullable: true }
        name: { type: string }
        url: { type: string }
        httpMethod: { type: string, enum: [POST, GET] }
        subscribedEvents:
          type: array
          items: { type: string }
        enabled: { type: boolean }
        authType: { type: string }
        authHeaderName: { type: string, nullable: true }
        hasAuthValue: { type: boolean }
        consecutiveFailures: { type: integer }
        disabledReason: { type: string, nullable: true }
        deliveryLogRetentionDays: { type: integer, nullable: true }
        deliveryLogMaxEntries: { type: integer, nullable: true }
        signingSecret: { type: string }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    WebhookDeliveryResponseDto:
      type: object
      properties:
        id: { type: string, format: uuid }
        endpointId: { type: string, format: uuid }
        eventId: { type: string, format: uuid }
        eventType: { type: string }
        payload: { type: object, additionalProperties: true }
        httpStatus: { type: integer, nullable: true }
        responseBody: { type: string, nullable: true }
        success: { type: boolean }
        attempt: { type: integer }
        errorMessage: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    PaginatedWebhookDeliveriesResponseDto:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/WebhookDeliveryResponseDto'
        total: { type: integer }
    KnowledgeNodeResponseDto:
      type: object
      required:
        [id, shas, clientId, nodeType, title, sortOrder, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        shas:
          type: object
          required: [short, long]
          properties:
            short: { type: string }
            long: { type: string }
        clientId: { type: string, format: uuid }
        nodeType: { type: string, enum: [folder, page] }
        parentId: { type: string, format: uuid, nullable: true }
        title: { type: string }
        content: { type: string, nullable: true }
        sortOrder: { type: integer }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
        children:
          type: array
          items: { $ref: '#/components/schemas/KnowledgeNodeResponseDto' }
    CreateKnowledgeNodeDto:
      type: object
      required: [nodeType, title]
      properties:
        clientId: { type: string, format: uuid }
        parentId: { type: string, format: uuid, nullable: true }
        nodeType: { type: string, enum: [folder, page] }
        title: { type: string, maxLength: 500 }
        content: { type: string }
    UpdateKnowledgeNodeDto:
      type: object
      properties:
        parentId: { type: string, format: uuid, nullable: true }
        title: { type: string, maxLength: 500 }
        content: { type: string, nullable: true }
    ReorderKnowledgeNodeDto:
      type: object
      required: [sortOrder]
      properties:
        sortOrder: { type: integer, minimum: 0, maximum: 100000 }
    KnowledgeRelationResponseDto:
      type: object
      required: [id, clientId, sourceType, sourceId, targetType, createdAt]
      properties:
        id: { type: string, format: uuid }
        clientId: { type: string, format: uuid }
        sourceType: { type: string, enum: [ticket, page] }
        sourceId: { type: string, format: uuid }
        targetType: { type: string, enum: [ticket, folder, page] }
        targetNodeId: { type: string, format: uuid, nullable: true }
        targetTicketLongSha: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    CreateKnowledgeRelationDto:
      type: object
      required: [clientId, sourceType, sourceId, targetType]
      properties:
        clientId: { type: string, format: uuid }
        sourceType: { type: string, enum: [ticket, page] }
        sourceId: { type: string, format: uuid }
        targetType: { type: string, enum: [ticket, folder, page] }
        targetNodeId: { type: string, format: uuid }
        targetTicketSha: { type: string, maxLength: 40 }
    KnowledgePromptContextResponseDto:
      type: object
      required: [promptSections]
      properties:
        promptSections:
          type: array
          items: { type: string }
    KnowledgePageActivityResponseDto:
      type: object
      required: [id, pageId, occurredAt, actorType, actionType, payload]
      properties:
        id: { type: string, format: uuid }
        pageId: { type: string, format: uuid }
        occurredAt: { type: string, format: date-time }
        actorType: { type: string, enum: [human, ai, system] }
        actorUserId: { type: string, format: uuid, nullable: true }
        actorEmail: { type: string, nullable: true }
        actionType:
          type: string
          enum:
            [
              CREATED,
              FIELD_UPDATED,
              CONTENT_UPDATED,
              PARENT_CHANGED,
              SORT_ORDER_CHANGED,
              DELETED,
              DUPLICATED,
              RELATION_ADDED,
              RELATION_REMOVED,
            ]
        payload: { type: object, additionalProperties: true }
