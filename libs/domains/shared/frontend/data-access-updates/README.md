# shared-frontend-data-access-updates

NgRx data-access layer for the shared Updates admin page.

Provide `UPDATES_ADMIN_ENVIRONMENT` with `apiUrl` and `updatesBasePath`:

- Agenstra: `admin/updates`
- Decabill: `admin/billing/updates`

Use with `@forepath/shared/frontend/feature-updates`.
