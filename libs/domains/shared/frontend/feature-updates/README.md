# shared-frontend-feature-updates

Shared Updates admin UI (`UpdatesManagerComponent`) and route factory
`createUpdatesAdminRoutes()` / `updatesAdminProviders`.

Mirror of `@forepath/shared/frontend/feature-notifications` for webhooks.
Admin-only; wire with `[authGuard, adminGuard]`.
