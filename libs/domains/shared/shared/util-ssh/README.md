# shared-shared-util-ssh

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-shared-util-ssh` to build the library.

## Running unit tests

Run `nx test shared-shared-util-ssh` to execute the unit tests via [Jest](https://jestjs.io).
