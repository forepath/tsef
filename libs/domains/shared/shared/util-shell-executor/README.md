# shared-shared-util-shell-executor

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-shared-util-shell-executor` to build the library.

## Running unit tests

Run `nx test shared-shared-util-shell-executor` to execute the unit tests via [Jest](https://jestjs.io).
