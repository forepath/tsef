# shared-shared-util-config-loader

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-shared-util-config-loader` to build the library.

## Running unit tests

Run `nx test shared-shared-util-config-loader` to execute the unit tests via [Jest](https://jestjs.io).
