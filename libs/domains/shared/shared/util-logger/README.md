# shared-shared-util-logger

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-shared-util-logger` to build the library.

## Running unit tests

Run `nx test shared-shared-util-logger` to execute the unit tests via [Jest](https://jestjs.io).
