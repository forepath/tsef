# This file is generated by Nx.
# Build the docker image with `npx nx docker:build forepath-frontend-landingpage`.
# Tip: Modify "docker:build" options in project.json to change docker build args.
#
# Run the container with `nx docker:run forepath-frontend-landingpage -p 3000:3000`.
#
FROM docker.io/node:lts-alpine

ARG VERSION=0.0.0
ENV VERSION=${VERSION}

ENV HOST=0.0.0.0
ENV PORT=4000


WORKDIR /app

# CVE-2026-59873: upgrade npm so nested tar is >= 7.5.19
RUN npm install -g npm@11.18.0 && npm cache clean --force

COPY . /app



CMD [ "node", "server.mjs" ]
